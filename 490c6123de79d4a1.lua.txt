-- This file was generated with UnveilR V3.01 at discord.gg/threaded using the testing version.

local Debug_Info_Result, Debug_Info_Result_2, HumanoidRootPart, Workspace_Bases, UserId, Array, ActiveLuckyBlocks, Array_2, UFOEventParts, EventParts, Workspace_Misc, CFrame_New, TweenInfo_New, BodyVelocity, Vector3_New, BodyGyro, Heartbeat, Heartbeat_2, BodyVelocity_2, BodyGyro_2, UDim2_New_2, BackgroundColor3, BackgroundColor3_2, BackgroundColor3_3, BackgroundColor3_4, BackgroundColor3_5, StarterGui, Character_2, Character_3, Character_4, Humanoid, Stepped, TextLabel, Array_7, Array_8, Array_9, Array_10, Array_11, Array_12, Array_13, Array_14, Array_15, Array_16, Array_17, Array_18, Array_19, Array_20, Array_21, Array_22, Array_23, Array_24, Array_25, Array_26, Array_27, Array_28, Array_29, Array_30, Array_31, Array_32, Array_33, Array_34, Array_35, Array_36, Array_37, Array_38, Array_39, Array_40, Array_41, Array_42, Array_43, Array_44, Array_45, Array_46, Array_47, Array_48, Array_49, Array_50, Array_51, Array_52, Array_53, Array_54, Array_55, Array_56, Array_57, Array_58, Array_59, Array_60, Array_61, Array_62, Array_63, Array_64, Array_65, Array_66, Array_67, Array_68, Array_69, Array_70, Array_71, Array_72, Array_73, Array_74, Array_75, Array_76, Array_77, Array_78, Array_79, Array_80, Array_81, Array_82, Array_83, Array_84, Array_85, Array_86, Array_87, Array_88, Array_89, Array_90, Array_91, Array_92, Array_93, Array_94, Array_95, Array_96, Array_97, Array_98, Array_99, Array_100, Array_101, Array_102, Array_103, Array_104, Array_105, Array_106, Array_107, Array_108, Array_109, Array_110, Array_111, Array_112, Array_113, Array_114, Array_115, Array_116, Array_117, Array_118, Array_119, Array_120, Array_121, Array_122, Array_123, Array_124, Array_125, Array_126, Array_127, Array_128, Array_129, Array_130, Array_131, Array_132, Array_133, Array_134, Array_135, Array_136, Array_137, Array_138, Array_139, Array_140, Array_141, Array_142, Array_143, Array_144, Array_145, Array_146, Array_147, Array_148, Array_149, Array_150, Array_151, Array_152, Array_153, Array_154, Array_155, Array_156, Array_157, Array_158, Array_159, Array_160, Array_161, Array_162, Array_163, Array_164, Array_165, Array_166, Array_167, Array_168, Array_169, Array_170, Array_171, Array_172, Array_173, Array_174, Array_175, Array_176, Array_177, Array_178, Array_179, Array_180, Array_181, Array_182, Array_183, Array_184, Array_185, Array_186, Array_187, Array_188, Array_189, Array_190, Array_191, Array_192, Array_193, Array_194, Array_195, Array_196, Array_197, Array_198, Array_199, Array_200, Array_201, Array_202, Array_203, Array_204, Array_205, Array_206, Array_207, Array_208, Array_209, Array_210, Array_211, Array_212, Array_213, Array_214, Array_215, Array_216, Array_217, Array_218, Array_219, Array_220, Array_221, Array_222, Array_223, Array_224, Array_225, Array_226, Array_227, Array_228, Array_229, Array_230, Array_231, Array_232, Array_233, Array_234, Array_235, Array_236, Array_237, Array_238, Array_239, Array_240, Array_241, Array_242, Array_243, Array_244, Array_245, Array_246, Array_247, Array_248, Array_249, Array_250, Array_251, Array_252, Array_253, Array_254, Array_255, Array_256, Array_257, Array_258, Array_259, Array_260, Array_261, Array_262, Array_263, Array_264, Array_265, Array_266, Array_267, Array_268, Array_269, Array_270, Array_271, Array_272, Array_273, Array_274, Array_275, Array_276, Array_277, Array_278, Array_279, Array_280, Array_281, Array_282, Array_283, Array_284, Array_285, Array_286, Array_287, Array_288, Array_289, Array_290, Array_291, Array_292, Array_293, Array_294, Array_295, Array_296, Array_297, Array_298, Array_299, Array_300, Array_301, Array_302, Array_303, Array_304, Array_305, Array_306, Array_307, Array_308, Array_309, Array_310, Array_311, Array_312, Array_313, Array_314, Array_315, Array_316, Array_317, Array_318, Array_319, Array_320, Array_321, Array_322, Array_323, Array_324, Array_325, Array_326, Array_327, Array_328, Array_329, Array_330, Array_331, Array_332, Array_333, Array_334, Array_335, Array_336, Array_337, Array_338, Array_339, Array_340, Array_341, Array_342, Array_343, Array_344, Array_345, Array_346, Array_347, Array_348, Array_349, Array_350, Array_351, Array_352, Array_353, Array_354, Array_355, Array_356, Array_357, Array_358, Array_359, Array_360, Array_361, Array_362, Array_363, Array_364, Array_365, Array_366, Array_367, Array_368, Array_369, Array_370, Array_371, Array_372, Array_373, Array_374, Array_375, Array_376, Array_377, Array_378, Array_379, Array_380, Array_381, Array_382, Array_383, Array_384, Array_385, Array_386, Array_387, Array_388, Array_389, Array_390, Array_391, Array_392, Array_393, Array_394, Array_395, Array_396, Array_397, Array_398, Array_399, Array_400, Array_401, Array_402, Array_403, Array_404, Array_405, Array_406, Array_407, Array_408, Array_409, Array_410, Array_411, Array_412, Array_413, Array_414, Array_415, Array_416, Array_417, Array_418, Array_419, Array_420, Array_421, Array_422, Array_423, Array_424, Array_425, Array_426, Array_427, Array_428, Array_429, Array_430, Array_431, Array_432, Array_433, Array_434, Array_435, Array_436, Array_437, Array_438, Array_439, Array_440, Array_441, Array_442, Array_443, Array_444, Array_445, Array_446, Array_447, Array_448, Array_449, Array_450, Array_451, Array_452, Array_453, Array_454, Array_455, Array_456, Array_457, Array_458, Array_459, Array_460, Array_461, Array_462, Array_463, Array_464, Array_465, Array_466, Array_467, Array_468, Array_469, Array_470, Array_471, Array_472, Array_473, Array_474, Array_475, Array_476, Array_477, Array_478, Array_479, Array_480, Array_481, Array_482, Array_483, Array_484, Array_485, Array_486, Array_487, Array_488, Array_489, Array_490, Array_491, Array_492, Array_493, Array_494, Array_495, Array_496, Array_497, Array_498, Array_499, Array_500, Array_501, Array_502, Array_503, Array_504, Array_505, Array_506, Array_507, Array_508, Array_509, Array_510, Array_511, Array_512, Array_513, Array_514, Array_515, Array_516, Array_517, Array_518, Array_519, Array_520, Array_521, Array_522, Array_523, Array_524, Array_525, Array_526, Array_527, Array_528, Array_529, Array_530, Array_531, Array_532, Array_533, Array_534, Array_535, Array_536, Array_537, Array_538, Array_539, Array_540, Array_541, Array_542, Array_543, Array_544, Array_545, Array_546, Array_547, Array_548, Array_549, Array_550, Array_551;
local fenv = getfenv();
pcall(function(p1, p2, p3, p4, a, b, c)
    getgenv()["Fuck U"] = function(p1_3, p2_3, p3_3, p4_3, a_3, b_3, c_3) end;
end);
pcall(function(p1_2, p2_2, p3_2, p4_2, a_2, b_2, c_2)
    getgenv()["Fuck U"] = function(p1_3, p2_3, p3_3, p4_3, a_3, b_3, c_3) end;
end);
pcall(function(p1_3, p2_3, p3_3, p4_3, a_3, b_3, c_3)
    getgenv()["Fuck U"] = function(p1_3, p2_3, p3_3, p4_3, a_3, b_3, c_3) end;
end);
local _ = debug and debug.getinfo;
for for_key_0, for_val_0 in pairs(_G) do
end;
if (
    (
        (
            (
                (
                    (
                        (
                            (
                                (
                                    (
                                        (error ~= error) or (pairs ~= pairs) or (setmetatable ~= setmetatable) or (getmetatable ~= getmetatable) or false
                                    ) or (load ~= load)
                                ) or (loadstring ~= loadstring)
                            ) or (pcall ~= pcall)
                        ) or (xpcall ~= xpcall)
                    ) or (debug ~= debug)
                ) or (package ~= package)
            ) or (coroutine ~= coroutine)
        ) or false
    ) or false
) or false then

end;
if (pcall ~= pcall) or (math.random ~= math.random) then

end;
if (_G.os ~= nil) then

end;
if (_G.io ~= nil) then

end;
if (_G.file ~= nil) then

end;
if (_G.debug ~= nil) then

end;
getmetatable(_G.os);
getmetatable(_G.io);
getmetatable(_G.file);
getmetatable(_G.debug);
local _ = debug and debug.getinfo;
if debug.getinfo then
    if pairs then
        Debug_Info_Result = debug.getinfo(pairs, "S");
    end;
end;
if Debug_Info_Result and (Debug_Info_Result.what ~= "C") then

end;
if os.clock then
    Debug_Info_Result_2 = debug.getinfo(os.clock, "S");
end;
if Debug_Info_Result_2 and (Debug_Info_Result_2.what ~= "C") then

end;
debug.getinfo(table.insert, "S");
if not debug.getinfo(1, "S") then

end;
debug.getinfo(function(a_3, b_3, c_3) end, "S");
if (debug.getinfo(pairs, "S").what == "Lua") then

end;
local LocalPlayer = game:GetService("Players").LocalPlayer;
local RunService = game:GetService("RunService");
local TweenService = game:GetService("TweenService");
local UserInputService = game:GetService("UserInputService");
local Character = LocalPlayer.Character;
if Character then
    Character:WaitForChild("Humanoid");
end;
Character:WaitForChild("HumanoidRootPart");
LocalPlayer.CharacterAdded:Connect(function(a_3, b_3, c_3, ...)
    a_3:WaitForChild("Humanoid");
    HumanoidRootPart = a_3:WaitForChild("HumanoidRootPart");
end);
local ReplicatedStorage = game:GetService("ReplicatedStorage");
local ReplicatedStorage_2 = ReplicatedStorage:WaitForChild("RemoteFunctions", 5);
ReplicatedStorage:WaitForChild("RemoteEvents", 5);
ReplicatedStorage:WaitForChild("Packages", 5):WaitForChild("Net", 5);
local ReplicatedStorage_4 = game.ReplicatedStorage;
require(ReplicatedStorage_4.SharedModules.GearModule);
require(ReplicatedStorage_4.SharedModules.CommonModule);
_G.CollectBrainrotMode = "Single";
_G.CollectBrainrotType = "Whitelist";
_G.CollectLuckyBlockMode = "Single";
_G.CollectLuckyBlockType = "Whitelist";
_G.SellBrainrotType = "Whitelist";
_G.FlyEnabled = false;
_G.FlySpeed = 50;
fenv.getMyBase = function(a_5, b_5, c_5, ...)
    if workspace:FindFirstChild("Bases") then -- ran, if id: 21
        Workspace_Bases = workspace.Bases;
    end;
    for for_key_2, for_val_2 in pairs(Workspace_Bases:GetChildren()) do
        if (for_key_2 ~= nil) then -- ran, if id: 22

        end;
        UserId = LocalPlayer.UserId;
        if for_val_2:GetAttribute("Holder") ~= UserId then -- ran, if id: 23
        end;
    end;
end;
fenv.getAllActiveBrainrots = function(a_6, b_6, c_6, ...)
    if workspace:FindFirstChild("ActiveBrainrots") then -- ran, if id: 24

    end;
    for for_key_3, for_val_3 in pairs(workspace.ActiveBrainrots:GetChildren()) do
        if (for_key_3 ~= nil) then -- ran, if id: 25

        end;
    end;
    for for_key_4, for_val_4 in pairs(for_val_3:GetChildren()) do
        if (for_key_4 ~= nil) then -- ran, if id: 26
            Array = {};
            table.insert(Array, for_val_4);
        end;
    end;
end;
return Array;
end;
fenv.takeBrainrot = function(a_8, b_8, c_8, ...)
    if a_8 then -- ran, if id: 353
        pcall(function(a_9, b_9, c_9, ...)
            if not fenv.fireproximityprompt then -- didnt run, if id: 354
            end;
            fenv.fireproximityprompt(a_8.Root.TakePrompt);
        end);
    end;
end;
fenv.getBrainrotName = function(a_10, b_10, c_10, ...)
    local _ = a_10.GetAttribute;
    return a_10:GetAttribute("BrainrotName");
end;
fenv.getBrainrotClass = function(a_11, b_11, c_11, ...)
    return require(ReplicatedStorage_4.SharedModules.BrainrotModule).GetBrainrotClass(a_11);
end;
fenv.getBrainrotMutation = function(a_12, b_12, c_12, ...)
    local _ = a_12.GetAttribute;
    return a_12:GetAttribute("Mutation");
end;
fenv.getAllActiveLuckyBlocks = function(a_13, b_13, c_13, ...)
    if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 355
        ActiveLuckyBlocks = workspace.ActiveLuckyBlocks;
    end;
    for for_key_5, for_val_5 in pairs(ActiveLuckyBlocks:GetChildren()) do
        if (for_key_5 ~= nil) then -- ran, if id: 356
            Array_2 = {};
            table.insert(Array_2, for_val_5);
        end;
    end;
    return Array_2;
end;
fenv.takeLuckyBlock = function(a_14, b_14, c_14, ...)
    if a_14 then -- ran, if id: 357
        pcall(function(a_15, b_15, c_15, ...)
            if not fenv.fireproximityprompt then -- didnt run, if id: 358
            end;
            fenv.fireproximityprompt(a_14.RootPart.ProximityPrompt);
        end);
    end;
end;
fenv.getAllUFOEventCoins = function(a_16, b_16, c_16, ...)
    if workspace:FindFirstChild("UFOEventParts") then -- ran, if id: 359
        UFOEventParts = workspace.UFOEventParts;
    end;
    for for_key_6, for_val_6 in pairs(UFOEventParts:GetChildren()) do
        if (for_key_6 ~= nil) then -- ran, if id: 360

        end;
        if not string.find(for_val_6.Name, "Coin") then -- didnt run, if id: 361
        end;
        local Array_3 = {};
        table.insert(Array_3, for_val_6);
    end;
    return Array_3;
end;
fenv.collectAllUFOEventCoins = function(a_18, b_18, c_18, ...)
    if workspace:FindFirstChild("UFOEventParts") then -- ran, if id: 688

    end;
    for for_key_7, for_val_7 in pairs(UFOEventParts:GetChildren()) do
        if (for_key_7 ~= nil) then -- ran, if id: 689

        end;
        if not string.find(for_val_7.Name, "Coin") then -- didnt run, if id: 690
        end;
        local Array_4 = {};
        table.insert(Array_4, for_val_7);
    end;
    for for_key_8, for_val_8 in pairs(Array_4) do
    end;
end;
fenv.getAllRadioactiveEventCoins = function(a_19, b_19, c_19, ...)
    if workspace:FindFirstChild("EventParts") then -- ran, if id: 691
        EventParts = workspace.EventParts;
    end;
    for for_key_9, for_val_9 in pairs(EventParts:GetChildren()) do
        if (for_key_9 ~= nil) then -- ran, if id: 692

        end;
        if not string.find(for_val_9.Name, "Coin") then -- didnt run, if id: 693
        end;
        local Array_5 = {};
        table.insert(Array_5, for_val_9);
    end;
    return Array_5;
end;
fenv.collectAllRadioactiveEventCoins = function(a_20, b_20, c_20, ...)
    if workspace:FindFirstChild("EventParts") then -- ran, if id: 694

    end;
    for for_key_10, for_val_10 in pairs(EventParts:GetChildren()) do
        if (for_key_10 ~= nil) then -- ran, if id: 695

        end;
        if not string.find(for_val_10.Name, "Coin") then -- didnt run, if id: 696
        end;
        local Array_6 = {};
        table.insert(Array_6, for_val_10);
    end;
    for for_key_11, for_val_11 in pairs(Array_6) do
    end;
end;
fenv.getClosestGap = function(a_21, b_21, c_21, ...)
    if not workspace:FindFirstChild("Misc") then -- didnt run, if id: 697
    end;
    Workspace_Misc = workspace.Misc;
    if Workspace_Misc:FindFirstChild("Gaps") then -- ran, if id: 698

    end;
    for for_key_12, for_val_12 in pairs(Workspace_Misc.Gaps:GetChildren()) do
        if (for_key_12 ~= nil) then -- ran, if id: 699
            CFrame_New = CFrame.new;
        end;
        if (CFrame_New(for_val_12.WorldPivot.Position.X, -9.025, for_val_12.WorldPivot.Position.Z).Position - HumanoidRootPart.Position).Magnitude < 1 / 0 then -- ran, if id: 700
        end;
    end;
end;
fenv.getNextGap = function(a_22, b_22, c_22, ...)
    if not workspace:FindFirstChild("Misc") then -- didnt run, if id: 701
    end;
    if Workspace_Misc:FindFirstChild("Gaps") then -- ran, if id: 702

    end;
    for for_key_13, for_val_13 in pairs(Workspace_Misc.Gaps:GetChildren()) do
        if (for_key_13 ~= nil) then -- ran, if id: 703

        end;
        if (CFrame_New(for_val_13.WorldPivot.Position.X, -9.025, for_val_13.WorldPivot.Position.Z).Position - HumanoidRootPart.Position).Magnitude < 1 / 0 then -- ran, if id: 704
        end;
    end;
end;
fenv.TweenPlayer = function(a_23, b_23, c_23, ...)
    if HumanoidRootPart then -- ran, if id: 705
        pcall(function(a_24, b_24, c_24, ...)
            if LocalPlayer:GetAttribute("CurrentSpeed") then -- didnt run, if id: 706
            end;
            local Magnitude = (HumanoidRootPart.Position - a_23.Position).Magnitude;
            TweenService:Create(HumanoidRootPart, TweenInfo_New(
                Magnitude / 16,
                Enum.EasingStyle.Linear,
                Enum.EasingDirection.Out
            ), {
                ["CFrame"] = a_23
            }):Play();
            if Magnitude < 8 then -- ran, if id: 707
            end;
        end);
    end;
end;
fenv.StopTween = function(a_25, b_25, c_25, ...)
    if a_25 then -- ran, if id: 708
    end;
end;
fenv.EnableFly = function(a_26, b_26, c_26, ...)
    if HumanoidRootPart then -- ran, if id: 709
        BodyVelocity = Instance.new"BodyVelocity";
    end;
    Vector3_New = Vector3.new;
    BodyVelocity.Velocity = Vector3_New(0, 0, 0);
    BodyVelocity.MaxForce = Vector3_New(9000000000, 9000000000, 9000000000);
    BodyVelocity.Parent = HumanoidRootPart;
    BodyGyro = Instance.new"BodyGyro";
    BodyGyro.MaxTorque = Vector3_New(9000000000, 9000000000, 9000000000);
    BodyGyro.P = 90000;
    BodyGyro.Parent = HumanoidRootPart;
    Heartbeat = RunService.Heartbeat;
    Heartbeat_2 = Heartbeat:Connect(function(a_1485, b_1485, c_1485, ...)
        if not UserInputService:IsKeyDown(Enum.KeyCode.W) then -- didnt run, if id: 4775
        end;
        local CFrame = workspace.CurrentCamera.CFrame;
        if not UserInputService:IsKeyDown(Enum.KeyCode.S) then -- didnt run, if id: 4776
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.A) then -- didnt run, if id: 4777
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.D) then -- didnt run, if id: 4778
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.Space) then -- didnt run, if id: 4779
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then -- didnt run, if id: 4780
        end;
        if not BodyVelocity_2 then -- didnt run, if id: 4781
        end;
        BodyVelocity_2.Velocity = (
            (
                (
                    (
                        (
                            (
                                Vector3_New(0, 0, 0) + CFrame.LookVector
                            ) - CFrame.LookVector
                        ) - CFrame.RightVector
                    ) + CFrame.RightVector
                ) + Vector3_New(0, 1, 0)
            ) - Vector3_New(0, 1, 0)
        ) * 50;
        if not BodyGyro_2 then -- didnt run, if id: 4782
        end;
        BodyGyro_2.CFrame = CFrame;
    end);
end;
fenv.DisableFly = function(a_27, b_27, c_27, ...)
    if not BodyVelocity then -- didnt run, if id: 710
    end;
    BodyVelocity:Destroy();
    if not BodyGyro then -- didnt run, if id: 711
    end;
    BodyGyro:Destroy();
    if not Heartbeat_2 then -- didnt run, if id: 712
    end;
end;
local SnapixGUI = Instance.new"ScreenGui";
SnapixGUI.Name = "SnapixGUI";
SnapixGUI.ResetOnSpawn = false;
SnapixGUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling;
SnapixGUI.IgnoreGuiInset = true;
if not gethui then

end;
SnapixGUI.Parent = gethui();
local MainFrame = Instance.new"Frame";
MainFrame.Name = "MainFrame";
local FromRGB = Color3.fromRGB;
MainFrame.BackgroundColor3 = FromRGB(15, 15, 15);
MainFrame.BorderSizePixel = 0;
local UDim2_New = UDim2.new;
MainFrame.Position = UDim2_New(0.5, -275, 0.5, -225);
MainFrame.Size = UDim2_New(0, 550, 0, 450);
MainFrame.Active = true;
MainFrame.ClipsDescendants = true;
MainFrame.Parent = SnapixGUI;
local Shadow = Instance.new"ImageLabel";
Shadow.Name = "Shadow";
Shadow.BackgroundTransparency = 1;
Shadow.Position = UDim2_New(0, -20, 0, -20);
Shadow.Size = UDim2_New(1, 40, 1, 40);
Shadow.ZIndex = 0;
Shadow.Image = "rbxassetid://6015897843";
Shadow.ImageColor3 = FromRGB(0, 0, 0);
Shadow.ImageTransparency = 0.3;
Shadow.ScaleType = Enum.ScaleType.Slice;
Shadow.SliceCenter = Rect.new(49, 49, 450, 450);
Shadow.Parent = MainFrame;
local UICorner = Instance.new"UICorner";
local UDim_New = UDim.new;
UICorner.CornerRadius = UDim_New(0, 15);
UICorner.Parent = MainFrame;
local UIGradient = Instance.new"UIGradient";
local ColorSequence_New = ColorSequence.new;
local ColorSequenceKeypoint_New = ColorSequenceKeypoint.new;
UIGradient.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(25, 25, 25)
    ),
    ColorSequenceKeypoint_New(
        0.5,
        FromRGB(15, 15, 15)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(10, 10, 10)
    )
};
UIGradient.Rotation = 135;
UIGradient.Parent = MainFrame;
local UIStroke = Instance.new"UIStroke";
UIStroke.Color = FromRGB(255, 50, 50);
UIStroke.Thickness = 2;
UIStroke.Transparency = 0;
UIStroke.Parent = MainFrame;
task.spawn(function()
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    TweenService:Create(UIStroke, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0.3
    }):Play();
    task.wait();
    local _ = Enum.EasingStyle.Sine;
end);
local TitleBar = Instance.new"Frame";
TitleBar.Name = "TitleBar";
TitleBar.BackgroundColor3 = FromRGB(25, 25, 25);
TitleBar.BorderSizePixel = 0;
TitleBar.Size = UDim2_New(1, 0, 0, 45);
TitleBar.Parent = MainFrame;
local UIGradient_2 = Instance.new"UIGradient";
UIGradient_2.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(30, 30, 30)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(20, 20, 20)
    )
};
UIGradient_2.Rotation = 90;
UIGradient_2.Parent = TitleBar;
local UICorner_2 = Instance.new"UICorner";
UICorner_2.CornerRadius = UDim_New(0, 12);
UICorner_2.Parent = TitleBar;
local S = Instance.new"TextLabel";
S.BackgroundTransparency = 1;
S.Position = UDim2_New(0, 15, 0, 0);
S.Size = UDim2_New(0, 40, 1, 0);
S.Font = Enum.Font.GothamBold;
S.Text = "S";
S.TextColor3 = FromRGB(255, 50, 50);
S.TextSize = 26;
S.Parent = TitleBar;
local UIStroke_2 = Instance.new"UIStroke";
UIStroke_2.Color = FromRGB(255, 50, 50);
UIStroke_2.Thickness = 1;
UIStroke_2.Transparency = 0.3;
UIStroke_2.Parent = S;
task.spawn(function()
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }):Play();
    task.wait();
    TweenService:Create(S, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["TextSize"] = 28
    }):Play();
    local _ = TweenService:Create(UIStroke_2, TweenInfo_New(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        ["Transparency"] = 0
    }).Play;
end);
local Title = Instance.new"TextLabel";
Title.Name = "Title";
Title.BackgroundTransparency = 1;
Title.Position = UDim2_New(0, 55, 0, 0);
Title.Size = UDim2_New(0, 300, 1, 0);
Title.Font = Enum.Font.GothamBold;
Title.Text = "Snapix - Escape Tsunami";
Title.TextColor3 = FromRGB(255, 255, 255);
Title.TextSize = 18;
Title.TextXAlignment = Enum.TextXAlignment.Left;
Title.Parent = TitleBar;
local UIStroke_3 = Instance.new"UIStroke";
UIStroke_3.Color = FromRGB(255, 50, 50);
UIStroke_3.Thickness = 0.8;
UIStroke_3.Transparency = 0.7;
UIStroke_3.Parent = Title;
local MinimizeButton = Instance.new"TextButton";
MinimizeButton.Name = "MinimizeButton";
MinimizeButton.BackgroundColor3 = FromRGB(40, 40, 40);
MinimizeButton.BorderSizePixel = 0;
MinimizeButton.Position = UDim2_New(1, -80, 0, 7.5);
MinimizeButton.Size = UDim2_New(0, 30, 0, 30);
MinimizeButton.Font = Enum.Font.GothamBold;
MinimizeButton.Text = "-";
MinimizeButton.TextColor3 = FromRGB(255, 255, 255);
MinimizeButton.TextSize = 20;
MinimizeButton.Parent = TitleBar;
local UICorner_3 = Instance.new"UICorner";
UICorner_3.CornerRadius = UDim_New(0, 8);
UICorner_3.Parent = MinimizeButton;
MinimizeButton.MouseButton1Click:Connect(function(a_28, b_28, c_28, ...)
    UDim2_New_2 = UDim2_New(0, 550, 0, 45);
    if UDim2_New_2 then -- ran, if id: 713

    end;
    TweenService:Create(MainFrame, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New_2
    }):Play();
end);
MinimizeButton.MouseEnter:Connect(function(a_29, b_29, c_29, ...)
    TweenService:Create(MinimizeButton, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(0, 32, 0, 32),
        ["BackgroundColor3"] = FromRGB(70, 70, 70)
    }):Play();
end);
MinimizeButton.MouseLeave:Connect(function(a_30, b_30, c_30, ...)
    TweenService:Create(MinimizeButton, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(0, 30, 0, 30),
        ["BackgroundColor3"] = FromRGB(40, 40, 40)
    }):Play();
end);
local CloseButton = Instance.new"TextButton";
CloseButton.Name = "CloseButton";
CloseButton.BackgroundColor3 = FromRGB(255, 50, 50);
CloseButton.BorderSizePixel = 0;
CloseButton.Position = UDim2_New(1, -40, 0, 7.5);
CloseButton.Size = UDim2_New(0, 30, 0, 30);
CloseButton.Font = Enum.Font.GothamBold;
CloseButton.Text = "\195\151";
CloseButton.TextColor3 = FromRGB(255, 255, 255);
CloseButton.TextSize = 20;
CloseButton.Parent = TitleBar;
local UICorner_4 = Instance.new"UICorner";
UICorner_4.CornerRadius = UDim_New(0, 8);
UICorner_4.Parent = CloseButton;
CloseButton.MouseButton1Click:Connect(function(a_31, b_31, c_31, ...)
    TweenService:Create(MainFrame, TweenInfo_New(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        ["Position"] = UDim2_New(0.5, 0, 0.5, 0),
        ["Size"] = UDim2_New(0, 0, 0, 0)
    }):Play();
    task.wait(0.3);
    SnapixGUI:Destroy();
end);
CloseButton.MouseEnter:Connect(function(a_32, b_32, c_32, ...)
    TweenService:Create(CloseButton, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Rotation"] = 90,
        ["Size"] = UDim2_New(0, 32, 0, 32),
        ["BackgroundColor3"] = FromRGB(220, 40, 40)
    }):Play();
end);
CloseButton.MouseLeave:Connect(function(a_34, b_34, c_34, ...)
    TweenService:Create(CloseButton, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Rotation"] = 0,
        ["Size"] = UDim2_New(0, 30, 0, 30),
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
end);
TitleBar.InputBegan:Connect(function(a_35, b_35, c_35, ...)
    if a_35.UserInputType ~= Enum.UserInputType.MouseButton1 then -- ran, if id: 1040
    end;
end);
TitleBar.InputChanged:Connect(function(a_37, b_37, c_37, ...)
    if a_37.UserInputType ~= Enum.UserInputType.MouseMovement then -- ran, if id: 1367
    end;
end);
UserInputService.InputChanged:Connect(function(a_38, b_38, c_38, ...)
    local A_38_Is_Nil = (a_38 == nil);
    local _ = not A_38_Is_Nil;
    if not A_38_Is_Nil then -- ran, if id: 1369
    end;
end);
local TabContainer = Instance.new"Frame";
TabContainer.Name = "TabContainer";
TabContainer.BackgroundColor3 = FromRGB(18, 18, 18);
TabContainer.BorderSizePixel = 0;
TabContainer.Position = UDim2_New(0, 0, 0, 45);
TabContainer.Size = UDim2_New(0, 140, 1, -45);
TabContainer.Parent = MainFrame;
local UIGradient_3 = Instance.new"UIGradient";
UIGradient_3.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(22, 22, 22)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(15, 15, 15)
    )
};
UIGradient_3.Rotation = 90;
UIGradient_3.Parent = TabContainer;
local ContentContainer = Instance.new"Frame";
ContentContainer.Name = "ContentContainer";
ContentContainer.BackgroundTransparency = 1;
ContentContainer.Position = UDim2_New(0, 140, 0, 45);
ContentContainer.Size = UDim2_New(1, -140, 1, -45);
ContentContainer.Parent = MainFrame;
local ScrollFrame = Instance.new"ScrollingFrame";
ScrollFrame.Name = "ScrollFrame";
ScrollFrame.BackgroundTransparency = 1;
ScrollFrame.BorderSizePixel = 0;
ScrollFrame.Size = UDim2_New(1, -15, 1, -15);
ScrollFrame.Position = UDim2_New(0, 10, 0, 10);
ScrollFrame.CanvasSize = UDim2_New(0, 0, 0, 0);
ScrollFrame.ScrollBarThickness = 6;
ScrollFrame.ScrollBarImageColor3 = FromRGB(255, 50, 50);
ScrollFrame.ScrollBarImageTransparency = 0.3;
ScrollFrame.Parent = ContentContainer;
local UIListLayout = Instance.new"UIListLayout";
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder;
UIListLayout.Padding = UDim_New(0, 8);
UIListLayout.Parent = ScrollFrame;
UIListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function(a_39, b_39, c_39, ...)
    ScrollFrame.CanvasSize = UDim2_New(
        0,
        0,
        0,
        UIListLayout.AbsoluteContentSize.Y + 15
    );
end);
local UIListLayout_2 = Instance.new"UIListLayout";
UIListLayout_2.SortOrder = Enum.SortOrder.LayoutOrder;
UIListLayout_2.Padding = UDim_New(0, 8);
UIListLayout_2.Parent = TabContainer;
local UIPadding = Instance.new"UIPadding";
UIPadding.PaddingTop = UDim_New(0, 12);
UIPadding.PaddingLeft = UDim_New(0, 8);
UIPadding.PaddingRight = UDim_New(0, 8);
UIPadding.Parent = TabContainer;
local MainTab = Instance.new"TextButton";
MainTab.Name = "MainTab";
MainTab.BackgroundColor3 = FromRGB(25, 25, 25);
MainTab.BorderSizePixel = 0;
MainTab.Size = UDim2_New(1, -16, 0, 40);
MainTab.Font = Enum.Font.GothamBold;
MainTab.Text = "  \240\159\143\160  Main";
MainTab.TextColor3 = FromRGB(180, 180, 180);
MainTab.TextSize = 14;
MainTab.TextXAlignment = Enum.TextXAlignment.Left;
MainTab.Parent = TabContainer;
local UICorner_5 = Instance.new"UICorner";
UICorner_5.CornerRadius = UDim_New(0, 8);
UICorner_5.Parent = MainTab;
local UIStroke_4 = Instance.new"UIStroke";
UIStroke_4.Color = FromRGB(40, 40, 40);
UIStroke_4.Thickness = 1;
UIStroke_4.Transparency = 0.5;
UIStroke_4.Parent = MainTab;
MainTab.MouseEnter:Connect(function(a_40, b_40, c_40, ...)
    BackgroundColor3 = MainTab.BackgroundColor3;
    if BackgroundColor3 == FromRGB(255, 50, 50) then -- didnt run, if id: 1370
    end;
    TweenService:Create(MainTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(35, 35, 35)
    }):Play();
end);
MainTab.MouseLeave:Connect(function(a_41, b_41, c_41, ...)
    if BackgroundColor3 == FromRGB(255, 50, 50) then -- didnt run, if id: 1371
    end;
    TweenService:Create(MainTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
end);
local EventTab = Instance.new"TextButton";
EventTab.Name = "EventTab";
EventTab.BackgroundColor3 = FromRGB(25, 25, 25);
EventTab.BorderSizePixel = 0;
EventTab.Size = UDim2_New(1, -16, 0, 40);
EventTab.Font = Enum.Font.GothamBold;
EventTab.Text = "  \226\173\144  Event";
EventTab.TextColor3 = FromRGB(180, 180, 180);
EventTab.TextSize = 14;
EventTab.TextXAlignment = Enum.TextXAlignment.Left;
EventTab.Parent = TabContainer;
local UICorner_6 = Instance.new"UICorner";
UICorner_6.CornerRadius = UDim_New(0, 8);
UICorner_6.Parent = EventTab;
local UIStroke_5 = Instance.new"UIStroke";
UIStroke_5.Color = FromRGB(40, 40, 40);
UIStroke_5.Thickness = 1;
UIStroke_5.Transparency = 0.5;
UIStroke_5.Parent = EventTab;
EventTab.MouseEnter:Connect(function(a_42, b_42, c_42, ...)
    BackgroundColor3_2 = EventTab.BackgroundColor3;
    if BackgroundColor3_2 == FromRGB(255, 50, 50) then -- didnt run, if id: 1372
    end;
    TweenService:Create(EventTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(35, 35, 35)
    }):Play();
end);
EventTab.MouseLeave:Connect(function(a_43, b_43, c_43, ...)
    if BackgroundColor3_2 == FromRGB(255, 50, 50) then -- didnt run, if id: 1373
    end;
    TweenService:Create(EventTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
end);
local TeleportTab = Instance.new"TextButton";
TeleportTab.Name = "TeleportTab";
TeleportTab.BackgroundColor3 = FromRGB(25, 25, 25);
TeleportTab.BorderSizePixel = 0;
TeleportTab.Size = UDim2_New(1, -16, 0, 40);
TeleportTab.Font = Enum.Font.GothamBold;
TeleportTab.Text = "  \240\159\147\141  Teleport";
TeleportTab.TextColor3 = FromRGB(180, 180, 180);
TeleportTab.TextSize = 14;
TeleportTab.TextXAlignment = Enum.TextXAlignment.Left;
TeleportTab.Parent = TabContainer;
local UICorner_7 = Instance.new"UICorner";
UICorner_7.CornerRadius = UDim_New(0, 8);
UICorner_7.Parent = TeleportTab;
local UIStroke_6 = Instance.new"UIStroke";
UIStroke_6.Color = FromRGB(40, 40, 40);
UIStroke_6.Thickness = 1;
UIStroke_6.Transparency = 0.5;
UIStroke_6.Parent = TeleportTab;
TeleportTab.MouseEnter:Connect(function(a_44, b_44, c_44, ...)
    BackgroundColor3_3 = TeleportTab.BackgroundColor3;
    if BackgroundColor3_3 == FromRGB(255, 50, 50) then -- didnt run, if id: 1374
    end;
    TweenService:Create(TeleportTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(35, 35, 35)
    }):Play();
end);
TeleportTab.MouseLeave:Connect(function(a_45, b_45, c_45, ...)
    if BackgroundColor3_3 == FromRGB(255, 50, 50) then -- didnt run, if id: 1375
    end;
    TweenService:Create(TeleportTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
end);
local MiscTab = Instance.new"TextButton";
MiscTab.Name = "MiscTab";
MiscTab.BackgroundColor3 = FromRGB(25, 25, 25);
MiscTab.BorderSizePixel = 0;
MiscTab.Size = UDim2_New(1, -16, 0, 40);
MiscTab.Font = Enum.Font.GothamBold;
MiscTab.Text = "  \226\154\153\239\184\143  Misc";
MiscTab.TextColor3 = FromRGB(180, 180, 180);
MiscTab.TextSize = 14;
MiscTab.TextXAlignment = Enum.TextXAlignment.Left;
MiscTab.Parent = TabContainer;
local UICorner_8 = Instance.new"UICorner";
UICorner_8.CornerRadius = UDim_New(0, 8);
UICorner_8.Parent = MiscTab;
local UIStroke_7 = Instance.new"UIStroke";
UIStroke_7.Color = FromRGB(40, 40, 40);
UIStroke_7.Thickness = 1;
UIStroke_7.Transparency = 0.5;
UIStroke_7.Parent = MiscTab;
MiscTab.MouseEnter:Connect(function(a_46, b_46, c_46, ...)
    BackgroundColor3_4 = MiscTab.BackgroundColor3;
    if BackgroundColor3_4 == FromRGB(255, 50, 50) then -- didnt run, if id: 1376
    end;
    TweenService:Create(MiscTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(35, 35, 35)
    }):Play();
end);
MiscTab.MouseLeave:Connect(function(a_48, b_48, c_48, ...)
    if BackgroundColor3_4 == FromRGB(255, 50, 50) then -- didnt run, if id: 1703
    end;
    TweenService:Create(MiscTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
end);
local ServerTab = Instance.new"TextButton";
ServerTab.Name = "ServerTab";
ServerTab.BackgroundColor3 = FromRGB(25, 25, 25);
ServerTab.BorderSizePixel = 0;
ServerTab.Size = UDim2_New(1, -16, 0, 40);
ServerTab.Font = Enum.Font.GothamBold;
ServerTab.Text = "  \240\159\140\144  Server";
ServerTab.TextColor3 = FromRGB(180, 180, 180);
ServerTab.TextSize = 14;
ServerTab.TextXAlignment = Enum.TextXAlignment.Left;
ServerTab.Parent = TabContainer;
local UICorner_9 = Instance.new"UICorner";
UICorner_9.CornerRadius = UDim_New(0, 8);
UICorner_9.Parent = ServerTab;
local UIStroke_8 = Instance.new"UIStroke";
UIStroke_8.Color = FromRGB(40, 40, 40);
UIStroke_8.Thickness = 1;
UIStroke_8.Transparency = 0.5;
UIStroke_8.Parent = ServerTab;
ServerTab.MouseEnter:Connect(function(a_49, b_49, c_49, ...)
    BackgroundColor3_5 = ServerTab.BackgroundColor3;
    if BackgroundColor3_5 == FromRGB(255, 50, 50) then -- didnt run, if id: 1704
    end;
    TweenService:Create(ServerTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(35, 35, 35)
    }):Play();
end);
ServerTab.MouseLeave:Connect(function(a_50, b_50, c_50, ...)
    if BackgroundColor3_5 == FromRGB(255, 50, 50) then -- didnt run, if id: 1705
    end;
    TweenService:Create(ServerTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
end);
local MainContent = Instance.new"Frame";
MainContent.Name = "MainContent";
MainContent.BackgroundTransparency = 1;
MainContent.Size = UDim2_New(1, 0, 1, 0);
MainContent.Position = UDim2_New(0, 50, 0, 0);
MainContent.Visible = false;
MainContent.Parent = ScrollFrame;
local UIListLayout_3 = Instance.new"UIListLayout";
UIListLayout_3.SortOrder = Enum.SortOrder.LayoutOrder;
UIListLayout_3.Padding = UDim_New(0, 8);
UIListLayout_3.Parent = MainContent;
local UIPadding_2 = Instance.new"UIPadding";
UIPadding_2.PaddingTop = UDim_New(0, 5);
UIPadding_2.PaddingLeft = UDim_New(0, 5);
UIPadding_2.PaddingRight = UDim_New(0, 5);
UIPadding_2.Parent = MainContent;
local EventContent = Instance.new"Frame";
EventContent.Name = "EventContent";
EventContent.BackgroundTransparency = 1;
EventContent.Size = UDim2_New(1, 0, 1, 0);
EventContent.Position = UDim2_New(0, 50, 0, 0);
EventContent.Visible = false;
EventContent.Parent = ScrollFrame;
local UIListLayout_4 = Instance.new"UIListLayout";
UIListLayout_4.SortOrder = Enum.SortOrder.LayoutOrder;
UIListLayout_4.Padding = UDim_New(0, 8);
UIListLayout_4.Parent = EventContent;
local UIPadding_3 = Instance.new"UIPadding";
UIPadding_3.PaddingTop = UDim_New(0, 5);
UIPadding_3.PaddingLeft = UDim_New(0, 5);
UIPadding_3.PaddingRight = UDim_New(0, 5);
UIPadding_3.Parent = EventContent;
local TeleportContent = Instance.new"Frame";
TeleportContent.Name = "TeleportContent";
TeleportContent.BackgroundTransparency = 1;
TeleportContent.Size = UDim2_New(1, 0, 1, 0);
TeleportContent.Position = UDim2_New(0, 50, 0, 0);
TeleportContent.Visible = false;
TeleportContent.Parent = ScrollFrame;
local UIListLayout_5 = Instance.new"UIListLayout";
UIListLayout_5.SortOrder = Enum.SortOrder.LayoutOrder;
UIListLayout_5.Padding = UDim_New(0, 8);
UIListLayout_5.Parent = TeleportContent;
local UIPadding_4 = Instance.new"UIPadding";
UIPadding_4.PaddingTop = UDim_New(0, 5);
UIPadding_4.PaddingLeft = UDim_New(0, 5);
UIPadding_4.PaddingRight = UDim_New(0, 5);
UIPadding_4.Parent = TeleportContent;
local MiscContent = Instance.new"Frame";
MiscContent.Name = "MiscContent";
MiscContent.BackgroundTransparency = 1;
MiscContent.Size = UDim2_New(1, 0, 1, 0);
MiscContent.Position = UDim2_New(0, 50, 0, 0);
MiscContent.Visible = false;
MiscContent.Parent = ScrollFrame;
local UIListLayout_6 = Instance.new"UIListLayout";
UIListLayout_6.SortOrder = Enum.SortOrder.LayoutOrder;
UIListLayout_6.Padding = UDim_New(0, 8);
UIListLayout_6.Parent = MiscContent;
local UIPadding_5 = Instance.new"UIPadding";
UIPadding_5.PaddingTop = UDim_New(0, 5);
UIPadding_5.PaddingLeft = UDim_New(0, 5);
UIPadding_5.PaddingRight = UDim_New(0, 5);
UIPadding_5.Parent = MiscContent;
local ServerContent = Instance.new"Frame";
ServerContent.Name = "ServerContent";
ServerContent.BackgroundTransparency = 1;
ServerContent.Size = UDim2_New(1, 0, 1, 0);
ServerContent.Position = UDim2_New(0, 50, 0, 0);
ServerContent.Visible = false;
ServerContent.Parent = ScrollFrame;
local UIListLayout_7 = Instance.new"UIListLayout";
UIListLayout_7.SortOrder = Enum.SortOrder.LayoutOrder;
UIListLayout_7.Padding = UDim_New(0, 8);
UIListLayout_7.Parent = ServerContent;
local UIPadding_6 = Instance.new"UIPadding";
UIPadding_6.PaddingTop = UDim_New(0, 5);
UIPadding_6.PaddingLeft = UDim_New(0, 5);
UIPadding_6.PaddingRight = UDim_New(0, 5);
UIPadding_6.Parent = ServerContent;
MainTab.MouseButton1Click:Connect(function(a_51, b_51, c_51, ...)
    MiscContent.Visible = false;
    MainContent.Visible = true;
    TweenService:Create(MainContent, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Position"] = UDim2_New(0, 0, 0, 0)
    }):Play();
    TeleportContent.Visible = false;
    EventContent.Visible = false;
    ServerContent.Visible = false;
    if not MainTab then -- didnt run, if id: 1706
    end;
    TweenService:Create(MainTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
    TweenService:Create(MainTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
end);
EventTab.MouseButton1Click:Connect(function(a_53, b_53, c_53, ...)
    MiscContent.Visible = false;
    MainContent.Visible = false;
    TeleportContent.Visible = false;
    EventContent.Visible = true;
    TweenService:Create(EventContent, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Position"] = UDim2_New(0, 0, 0, 0)
    }):Play();
    ServerContent.Visible = false;
    if not MainTab then -- didnt run, if id: 1707
    end;
    TweenService:Create(MainTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
    TweenService:Create(EventTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
end);
TeleportTab.MouseButton1Click:Connect(function(a_54, b_54, c_54, ...)
    MiscContent.Visible = false;
    MainContent.Visible = false;
    TeleportContent.Visible = true;
    TweenService:Create(TeleportContent, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Position"] = UDim2_New(0, 0, 0, 0)
    }):Play();
    EventContent.Visible = false;
    ServerContent.Visible = false;
    if not EventTab then -- didnt run, if id: 1708
    end;
    TweenService:Create(EventTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
    TweenService:Create(TeleportTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
end);
MiscTab.MouseButton1Click:Connect(function(a_55, b_55, c_55, ...)
    MiscContent.Visible = true;
    TweenService:Create(MiscContent, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Position"] = UDim2_New(0, 0, 0, 0)
    }):Play();
    MainContent.Visible = false;
    TeleportContent.Visible = false;
    EventContent.Visible = false;
    ServerContent.Visible = false;
    if not TeleportTab then -- didnt run, if id: 1709
    end;
    TweenService:Create(TeleportTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
    TweenService:Create(MiscTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
end);
ServerTab.MouseButton1Click:Connect(function(a_56, b_56, c_56, ...)
    MiscContent.Visible = false;
    MainContent.Visible = false;
    TeleportContent.Visible = false;
    EventContent.Visible = false;
    ServerContent.Visible = true;
    TweenService:Create(ServerContent, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["Position"] = UDim2_New(0, 0, 0, 0)
    }):Play();
    if not MiscTab then -- didnt run, if id: 1710
    end;
    TweenService:Create(MiscTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(180, 180, 180),
        ["BackgroundColor3"] = FromRGB(25, 25, 25)
    }):Play();
    TweenService:Create(ServerTab, TweenInfo_New(0.2), {
        ["TextColor3"] = FromRGB(255, 255, 255),
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
end);
local Brainrot_CollectionSection = Instance.new"Frame";
Brainrot_CollectionSection.Name = "Brainrot CollectionSection";
Brainrot_CollectionSection.BackgroundTransparency = 1;
Brainrot_CollectionSection.Size = UDim2_New(1, 0, 0, 30);
Brainrot_CollectionSection.Parent = MainContent;
local Frame = Instance.new"Frame";
Frame.BackgroundColor3 = FromRGB(255, 50, 50);
Frame.BorderSizePixel = 0;
Frame.Position = UDim2_New(0, 0, 0.5, -1);
Frame.Size = UDim2_New(0.3, -5, 0, 2);
Frame.Parent = Brainrot_CollectionSection;
local UIGradient_4 = Instance.new"UIGradient";
UIGradient_4.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_4.Parent = Frame;
local Brainrot_Collection = Instance.new"TextLabel";
Brainrot_Collection.BackgroundTransparency = 1;
Brainrot_Collection.Position = UDim2_New(0.3, 0, 0, 0);
Brainrot_Collection.Size = UDim2_New(0.4, 0, 1, 0);
Brainrot_Collection.Font = Enum.Font.GothamBold;
Brainrot_Collection.Text = "Brainrot Collection";
Brainrot_Collection.TextColor3 = FromRGB(255, 50, 50);
Brainrot_Collection.TextSize = 13;
Brainrot_Collection.Parent = Brainrot_CollectionSection;
local Frame_2 = Instance.new"Frame";
Frame_2.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_2.BorderSizePixel = 0;
Frame_2.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_2.Size = UDim2_New(0.3, -5, 0, 2);
Frame_2.Parent = Brainrot_CollectionSection;
local UIGradient_5 = Instance.new"UIGradient";
UIGradient_5.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_5.Parent = Frame_2;
local Max_Carry_ModeToggle = Instance.new"Frame";
Max_Carry_ModeToggle.Name = "Max Carry ModeToggle";
Max_Carry_ModeToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Max_Carry_ModeToggle.BorderSizePixel = 0;
Max_Carry_ModeToggle.Size = UDim2_New(1, -10, 0, 40);
Max_Carry_ModeToggle.Parent = MainContent;
local UICorner_10 = Instance.new"UICorner";
UICorner_10.CornerRadius = UDim_New(0, 8);
UICorner_10.Parent = Max_Carry_ModeToggle;
local UIStroke_9 = Instance.new"UIStroke";
UIStroke_9.Color = FromRGB(40, 40, 40);
UIStroke_9.Thickness = 1;
UIStroke_9.Transparency = 0.5;
UIStroke_9.Parent = Max_Carry_ModeToggle;
local Max_Carry_Mode = Instance.new"TextLabel";
Max_Carry_Mode.BackgroundTransparency = 1;
Max_Carry_Mode.Position = UDim2_New(0, 12, 0, 0);
Max_Carry_Mode.Size = UDim2_New(1, -60, 1, 0);
Max_Carry_Mode.Font = Enum.Font.Gotham;
Max_Carry_Mode.Text = "Max Carry Mode";
Max_Carry_Mode.TextColor3 = FromRGB(220, 220, 220);
Max_Carry_Mode.TextSize = 13;
Max_Carry_Mode.TextXAlignment = Enum.TextXAlignment.Left;
Max_Carry_Mode.Parent = Max_Carry_ModeToggle;
local TextButton = Instance.new"TextButton";
TextButton.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton.BorderSizePixel = 0;
TextButton.Position = UDim2_New(1, -48, 0.5, -14);
TextButton.Size = UDim2_New(0, 42, 0, 28);
TextButton.Text = "";
TextButton.Parent = Max_Carry_ModeToggle;
local UICorner_11 = Instance.new"UICorner";
UICorner_11.CornerRadius = UDim_New(1, 0);
UICorner_11.Parent = TextButton;
local Frame_3 = Instance.new"Frame";
Frame_3.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_3.BorderSizePixel = 0;
Frame_3.Position = UDim2_New(0, 3, 0.5, -11);
Frame_3.Size = UDim2_New(0, 22, 0, 22);
Frame_3.Parent = TextButton;
local UICorner_12 = Instance.new"UICorner";
UICorner_12.CornerRadius = UDim_New(1, 0);
UICorner_12.Parent = Frame_3;
local UIStroke_10 = Instance.new"UIStroke";
UIStroke_10.Color = FromRGB(0, 0, 0);
UIStroke_10.Thickness = 2;
UIStroke_10.Transparency = 0.7;
UIStroke_10.Parent = Frame_3;
TextButton.MouseButton1Click:Connect(function(a_57, b_57, c_57, ...)
    TweenService:Create(Frame_3, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_9, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
end);
Max_Carry_ModeToggle.MouseEnter:Connect(function(a_58, b_58, c_58, ...)
    TweenService:Create(Max_Carry_ModeToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Max_Carry_ModeToggle.MouseLeave:Connect(function(a_59, b_59, c_59, ...)
    TweenService:Create(Max_Carry_ModeToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local Blacklist_ModeToggle = Instance.new"Frame";
Blacklist_ModeToggle.Name = "Blacklist ModeToggle";
Blacklist_ModeToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Blacklist_ModeToggle.BorderSizePixel = 0;
Blacklist_ModeToggle.Size = UDim2_New(1, -10, 0, 40);
Blacklist_ModeToggle.Parent = MainContent;
local UICorner_13 = Instance.new"UICorner";
UICorner_13.CornerRadius = UDim_New(0, 8);
UICorner_13.Parent = Blacklist_ModeToggle;
local UIStroke_11 = Instance.new"UIStroke";
UIStroke_11.Color = FromRGB(40, 40, 40);
UIStroke_11.Thickness = 1;
UIStroke_11.Transparency = 0.5;
UIStroke_11.Parent = Blacklist_ModeToggle;
local Blacklist_Mode = Instance.new"TextLabel";
Blacklist_Mode.BackgroundTransparency = 1;
Blacklist_Mode.Position = UDim2_New(0, 12, 0, 0);
Blacklist_Mode.Size = UDim2_New(1, -60, 1, 0);
Blacklist_Mode.Font = Enum.Font.Gotham;
Blacklist_Mode.Text = "Blacklist Mode";
Blacklist_Mode.TextColor3 = FromRGB(220, 220, 220);
Blacklist_Mode.TextSize = 13;
Blacklist_Mode.TextXAlignment = Enum.TextXAlignment.Left;
Blacklist_Mode.Parent = Blacklist_ModeToggle;
local TextButton_2 = Instance.new"TextButton";
TextButton_2.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_2.BorderSizePixel = 0;
TextButton_2.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_2.Size = UDim2_New(0, 42, 0, 28);
TextButton_2.Text = "";
TextButton_2.Parent = Blacklist_ModeToggle;
local UICorner_14 = Instance.new"UICorner";
UICorner_14.CornerRadius = UDim_New(1, 0);
UICorner_14.Parent = TextButton_2;
local Frame_4 = Instance.new"Frame";
Frame_4.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_4.BorderSizePixel = 0;
Frame_4.Position = UDim2_New(0, 3, 0.5, -11);
Frame_4.Size = UDim2_New(0, 22, 0, 22);
Frame_4.Parent = TextButton_2;
local UICorner_15 = Instance.new"UICorner";
UICorner_15.CornerRadius = UDim_New(1, 0);
UICorner_15.Parent = Frame_4;
local UIStroke_12 = Instance.new"UIStroke";
UIStroke_12.Color = FromRGB(0, 0, 0);
UIStroke_12.Thickness = 2;
UIStroke_12.Transparency = 0.7;
UIStroke_12.Parent = Frame_4;
TextButton_2.MouseButton1Click:Connect(function(a_60, b_60, c_60, ...)
    TweenService:Create(Frame_4, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_2, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_11, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
end);
Blacklist_ModeToggle.MouseEnter:Connect(function(a_61, b_61, c_61, ...)
    TweenService:Create(Blacklist_ModeToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Blacklist_ModeToggle.MouseLeave:Connect(function(a_62, b_62, c_62, ...)
    TweenService:Create(Blacklist_ModeToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local Auto_Collect_BrainrotToggle = Instance.new"Frame";
Auto_Collect_BrainrotToggle.Name = "Auto Collect BrainrotToggle";
Auto_Collect_BrainrotToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Auto_Collect_BrainrotToggle.BorderSizePixel = 0;
Auto_Collect_BrainrotToggle.Size = UDim2_New(1, -10, 0, 40);
Auto_Collect_BrainrotToggle.Parent = MainContent;
local UICorner_16 = Instance.new"UICorner";
UICorner_16.CornerRadius = UDim_New(0, 8);
UICorner_16.Parent = Auto_Collect_BrainrotToggle;
local UIStroke_13 = Instance.new"UIStroke";
UIStroke_13.Color = FromRGB(40, 40, 40);
UIStroke_13.Thickness = 1;
UIStroke_13.Transparency = 0.5;
UIStroke_13.Parent = Auto_Collect_BrainrotToggle;
local Auto_Collect_Brainrot = Instance.new"TextLabel";
Auto_Collect_Brainrot.BackgroundTransparency = 1;
Auto_Collect_Brainrot.Position = UDim2_New(0, 12, 0, 0);
Auto_Collect_Brainrot.Size = UDim2_New(1, -60, 1, 0);
Auto_Collect_Brainrot.Font = Enum.Font.Gotham;
Auto_Collect_Brainrot.Text = "Auto Collect Brainrot";
Auto_Collect_Brainrot.TextColor3 = FromRGB(220, 220, 220);
Auto_Collect_Brainrot.TextSize = 13;
Auto_Collect_Brainrot.TextXAlignment = Enum.TextXAlignment.Left;
Auto_Collect_Brainrot.Parent = Auto_Collect_BrainrotToggle;
local TextButton_3 = Instance.new"TextButton";
TextButton_3.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_3.BorderSizePixel = 0;
TextButton_3.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_3.Size = UDim2_New(0, 42, 0, 28);
TextButton_3.Text = "";
TextButton_3.Parent = Auto_Collect_BrainrotToggle;
local UICorner_17 = Instance.new"UICorner";
UICorner_17.CornerRadius = UDim_New(1, 0);
UICorner_17.Parent = TextButton_3;
local Frame_5 = Instance.new"Frame";
Frame_5.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_5.BorderSizePixel = 0;
Frame_5.Position = UDim2_New(0, 3, 0.5, -11);
Frame_5.Size = UDim2_New(0, 22, 0, 22);
Frame_5.Parent = TextButton_3;
local UICorner_18 = Instance.new"UICorner";
UICorner_18.CornerRadius = UDim_New(1, 0);
UICorner_18.Parent = Frame_5;
local UIStroke_14 = Instance.new"UIStroke";
UIStroke_14.Color = FromRGB(0, 0, 0);
UIStroke_14.Thickness = 2;
UIStroke_14.Transparency = 0.7;
UIStroke_14.Parent = Frame_5;
TextButton_3.MouseButton1Click:Connect(function(a_63, b_63, c_63, ...)
    TweenService:Create(Frame_5, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_3, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_13, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.AutoCollectBrainrot = true;
end);
Auto_Collect_BrainrotToggle.MouseEnter:Connect(function(a_880, b_880, c_880, ...)
    TweenService:Create(Auto_Collect_BrainrotToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Auto_Collect_BrainrotToggle.MouseLeave:Connect(function(a_886, b_886, c_886, ...)
    TweenService:Create(Auto_Collect_BrainrotToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local UpgradesSection = Instance.new"Frame";
UpgradesSection.Name = "UpgradesSection";
UpgradesSection.BackgroundTransparency = 1;
UpgradesSection.Size = UDim2_New(1, 0, 0, 30);
UpgradesSection.Parent = MainContent;
local Frame_6 = Instance.new"Frame";
Frame_6.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_6.BorderSizePixel = 0;
Frame_6.Position = UDim2_New(0, 0, 0.5, -1);
Frame_6.Size = UDim2_New(0.3, -5, 0, 2);
Frame_6.Parent = UpgradesSection;
local UIGradient_6 = Instance.new"UIGradient";
UIGradient_6.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_6.Parent = Frame_6;
local Upgrades = Instance.new"TextLabel";
Upgrades.BackgroundTransparency = 1;
Upgrades.Position = UDim2_New(0.3, 0, 0, 0);
Upgrades.Size = UDim2_New(0.4, 0, 1, 0);
Upgrades.Font = Enum.Font.GothamBold;
Upgrades.Text = "Upgrades";
Upgrades.TextColor3 = FromRGB(255, 50, 50);
Upgrades.TextSize = 13;
Upgrades.Parent = UpgradesSection;
local Frame_7 = Instance.new"Frame";
Frame_7.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_7.BorderSizePixel = 0;
Frame_7.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_7.Size = UDim2_New(0.3, -5, 0, 2);
Frame_7.Parent = UpgradesSection;
local UIGradient_7 = Instance.new"UIGradient";
UIGradient_7.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_7.Parent = Frame_7;
local Auto_Upgrade_BaseToggle = Instance.new"Frame";
Auto_Upgrade_BaseToggle.Name = "Auto Upgrade BaseToggle";
Auto_Upgrade_BaseToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Auto_Upgrade_BaseToggle.BorderSizePixel = 0;
Auto_Upgrade_BaseToggle.Size = UDim2_New(1, -10, 0, 40);
Auto_Upgrade_BaseToggle.Parent = MainContent;
local UICorner_19 = Instance.new"UICorner";
UICorner_19.CornerRadius = UDim_New(0, 8);
UICorner_19.Parent = Auto_Upgrade_BaseToggle;
local UIStroke_15 = Instance.new"UIStroke";
UIStroke_15.Color = FromRGB(40, 40, 40);
UIStroke_15.Thickness = 1;
UIStroke_15.Transparency = 0.5;
UIStroke_15.Parent = Auto_Upgrade_BaseToggle;
local Auto_Upgrade_Base = Instance.new"TextLabel";
Auto_Upgrade_Base.BackgroundTransparency = 1;
Auto_Upgrade_Base.Position = UDim2_New(0, 12, 0, 0);
Auto_Upgrade_Base.Size = UDim2_New(1, -60, 1, 0);
Auto_Upgrade_Base.Font = Enum.Font.Gotham;
Auto_Upgrade_Base.Text = "Auto Upgrade Base";
Auto_Upgrade_Base.TextColor3 = FromRGB(220, 220, 220);
Auto_Upgrade_Base.TextSize = 13;
Auto_Upgrade_Base.TextXAlignment = Enum.TextXAlignment.Left;
Auto_Upgrade_Base.Parent = Auto_Upgrade_BaseToggle;
local TextButton_4 = Instance.new"TextButton";
TextButton_4.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_4.BorderSizePixel = 0;
TextButton_4.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_4.Size = UDim2_New(0, 42, 0, 28);
TextButton_4.Text = "";
TextButton_4.Parent = Auto_Upgrade_BaseToggle;
local UICorner_20 = Instance.new"UICorner";
UICorner_20.CornerRadius = UDim_New(1, 0);
UICorner_20.Parent = TextButton_4;
local Frame_8 = Instance.new"Frame";
Frame_8.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_8.BorderSizePixel = 0;
Frame_8.Position = UDim2_New(0, 3, 0.5, -11);
Frame_8.Size = UDim2_New(0, 22, 0, 22);
Frame_8.Parent = TextButton_4;
local UICorner_21 = Instance.new"UICorner";
UICorner_21.CornerRadius = UDim_New(1, 0);
UICorner_21.Parent = Frame_8;
local UIStroke_16 = Instance.new"UIStroke";
UIStroke_16.Color = FromRGB(0, 0, 0);
UIStroke_16.Thickness = 2;
UIStroke_16.Transparency = 0.7;
UIStroke_16.Parent = Frame_8;
TextButton_4.MouseButton1Click:Connect(function(a_887, b_887, c_887, ...)
    TweenService:Create(Frame_8, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_4, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_15, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.AutoUpgradeBase = true;
end);
Auto_Upgrade_BaseToggle.MouseEnter:Connect(function(a_881, b_881, c_881, ...)
    TweenService:Create(Auto_Upgrade_BaseToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Auto_Upgrade_BaseToggle.MouseLeave:Connect(function(a_888, b_888, c_888, ...)
    TweenService:Create(Auto_Upgrade_BaseToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local Auto_Upgrade_SpeedToggle = Instance.new"Frame";
Auto_Upgrade_SpeedToggle.Name = "Auto Upgrade SpeedToggle";
Auto_Upgrade_SpeedToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Auto_Upgrade_SpeedToggle.BorderSizePixel = 0;
Auto_Upgrade_SpeedToggle.Size = UDim2_New(1, -10, 0, 40);
Auto_Upgrade_SpeedToggle.Parent = MainContent;
local UICorner_22 = Instance.new"UICorner";
UICorner_22.CornerRadius = UDim_New(0, 8);
UICorner_22.Parent = Auto_Upgrade_SpeedToggle;
local UIStroke_17 = Instance.new"UIStroke";
UIStroke_17.Color = FromRGB(40, 40, 40);
UIStroke_17.Thickness = 1;
UIStroke_17.Transparency = 0.5;
UIStroke_17.Parent = Auto_Upgrade_SpeedToggle;
local Auto_Upgrade_Speed = Instance.new"TextLabel";
Auto_Upgrade_Speed.BackgroundTransparency = 1;
Auto_Upgrade_Speed.Position = UDim2_New(0, 12, 0, 0);
Auto_Upgrade_Speed.Size = UDim2_New(1, -60, 1, 0);
Auto_Upgrade_Speed.Font = Enum.Font.Gotham;
Auto_Upgrade_Speed.Text = "Auto Upgrade Speed";
Auto_Upgrade_Speed.TextColor3 = FromRGB(220, 220, 220);
Auto_Upgrade_Speed.TextSize = 13;
Auto_Upgrade_Speed.TextXAlignment = Enum.TextXAlignment.Left;
Auto_Upgrade_Speed.Parent = Auto_Upgrade_SpeedToggle;
local TextButton_5 = Instance.new"TextButton";
TextButton_5.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_5.BorderSizePixel = 0;
TextButton_5.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_5.Size = UDim2_New(0, 42, 0, 28);
TextButton_5.Text = "";
TextButton_5.Parent = Auto_Upgrade_SpeedToggle;
local UICorner_23 = Instance.new"UICorner";
UICorner_23.CornerRadius = UDim_New(1, 0);
UICorner_23.Parent = TextButton_5;
local Frame_9 = Instance.new"Frame";
Frame_9.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_9.BorderSizePixel = 0;
Frame_9.Position = UDim2_New(0, 3, 0.5, -11);
Frame_9.Size = UDim2_New(0, 22, 0, 22);
Frame_9.Parent = TextButton_5;
local UICorner_24 = Instance.new"UICorner";
UICorner_24.CornerRadius = UDim_New(1, 0);
UICorner_24.Parent = Frame_9;
local UIStroke_18 = Instance.new"UIStroke";
UIStroke_18.Color = FromRGB(0, 0, 0);
UIStroke_18.Thickness = 2;
UIStroke_18.Transparency = 0.7;
UIStroke_18.Parent = Frame_9;
TextButton_5.MouseButton1Click:Connect(function(a_884, b_884, c_884, ...)
    TweenService:Create(Frame_9, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_5, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_17, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.AutoUpgradeSpeed = true;
end);
Auto_Upgrade_SpeedToggle.MouseEnter:Connect(function(a_889, b_889, c_889, ...)
    TweenService:Create(Auto_Upgrade_SpeedToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Auto_Upgrade_SpeedToggle.MouseLeave:Connect(function(a_890, b_890, c_890, ...)
    TweenService:Create(Auto_Upgrade_SpeedToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local Auto_Upgrade_CarryToggle = Instance.new"Frame";
Auto_Upgrade_CarryToggle.Name = "Auto Upgrade CarryToggle";
Auto_Upgrade_CarryToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Auto_Upgrade_CarryToggle.BorderSizePixel = 0;
Auto_Upgrade_CarryToggle.Size = UDim2_New(1, -10, 0, 40);
Auto_Upgrade_CarryToggle.Parent = MainContent;
local UICorner_25 = Instance.new"UICorner";
UICorner_25.CornerRadius = UDim_New(0, 8);
UICorner_25.Parent = Auto_Upgrade_CarryToggle;
local UIStroke_19 = Instance.new"UIStroke";
UIStroke_19.Color = FromRGB(40, 40, 40);
UIStroke_19.Thickness = 1;
UIStroke_19.Transparency = 0.5;
UIStroke_19.Parent = Auto_Upgrade_CarryToggle;
local Auto_Upgrade_Carry = Instance.new"TextLabel";
Auto_Upgrade_Carry.BackgroundTransparency = 1;
Auto_Upgrade_Carry.Position = UDim2_New(0, 12, 0, 0);
Auto_Upgrade_Carry.Size = UDim2_New(1, -60, 1, 0);
Auto_Upgrade_Carry.Font = Enum.Font.Gotham;
Auto_Upgrade_Carry.Text = "Auto Upgrade Carry";
Auto_Upgrade_Carry.TextColor3 = FromRGB(220, 220, 220);
Auto_Upgrade_Carry.TextSize = 13;
Auto_Upgrade_Carry.TextXAlignment = Enum.TextXAlignment.Left;
Auto_Upgrade_Carry.Parent = Auto_Upgrade_CarryToggle;
local TextButton_6 = Instance.new"TextButton";
TextButton_6.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_6.BorderSizePixel = 0;
TextButton_6.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_6.Size = UDim2_New(0, 42, 0, 28);
TextButton_6.Text = "";
TextButton_6.Parent = Auto_Upgrade_CarryToggle;
local UICorner_26 = Instance.new"UICorner";
UICorner_26.CornerRadius = UDim_New(1, 0);
UICorner_26.Parent = TextButton_6;
local Frame_10 = Instance.new"Frame";
Frame_10.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_10.BorderSizePixel = 0;
Frame_10.Position = UDim2_New(0, 3, 0.5, -11);
Frame_10.Size = UDim2_New(0, 22, 0, 22);
Frame_10.Parent = TextButton_6;
local UICorner_27 = Instance.new"UICorner";
UICorner_27.CornerRadius = UDim_New(1, 0);
UICorner_27.Parent = Frame_10;
local UIStroke_20 = Instance.new"UIStroke";
UIStroke_20.Color = FromRGB(0, 0, 0);
UIStroke_20.Thickness = 2;
UIStroke_20.Transparency = 0.7;
UIStroke_20.Parent = Frame_10;
TextButton_6.MouseButton1Click:Connect(function(a_891, b_891, c_891, ...)
    TweenService:Create(Frame_10, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_6, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_19, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.AutoUpgradeCarry = true;
end);
Auto_Upgrade_CarryToggle.MouseEnter:Connect(function(a_892, b_892, c_892, ...)
    TweenService:Create(Auto_Upgrade_CarryToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Auto_Upgrade_CarryToggle.MouseLeave:Connect(function(a_893, b_893, c_893, ...)
    TweenService:Create(Auto_Upgrade_CarryToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local Sell_AllButton = Instance.new"TextButton";
Sell_AllButton.Name = "Sell AllButton";
Sell_AllButton.BackgroundColor3 = FromRGB(255, 50, 50);
Sell_AllButton.BorderSizePixel = 0;
Sell_AllButton.Size = UDim2_New(1, -10, 0, 40);
Sell_AllButton.Font = Enum.Font.GothamBold;
Sell_AllButton.Text = "Sell All";
Sell_AllButton.TextColor3 = FromRGB(255, 255, 255);
Sell_AllButton.TextSize = 14;
Sell_AllButton.Parent = MainContent;
local UICorner_28 = Instance.new"UICorner";
UICorner_28.CornerRadius = UDim_New(0, 8);
UICorner_28.Parent = Sell_AllButton;
local UIGradient_8 = Instance.new"UIGradient";
UIGradient_8.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_8.Rotation = 90;
UIGradient_8.Parent = Sell_AllButton;
local UIStroke_21 = Instance.new"UIStroke";
UIStroke_21.Color = FromRGB(255, 100, 100);
UIStroke_21.Thickness = 1;
UIStroke_21.Transparency = 0.5;
UIStroke_21.Parent = Sell_AllButton;
Sell_AllButton.MouseButton1Click:Connect(function(a_894, b_894, c_894, ...)
    TweenService:Create(Sell_AllButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(Sell_AllButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    pcall(function(a_895, b_895, c_895, ...)
        if not ReplicatedStorage_2 then -- didnt run, if id: 3667
        end;
        ReplicatedStorage_2:WaitForChild("SellAll"):InvokeServer();
        StarterGui:SetCore("SendNotification", {
            ["Text"] = "Sold all brainrots!",
            ["Title"] = "\226\156\133 Sell",
            ["Duration"] = 2
        });
    end);
end);
Sell_AllButton.MouseEnter:Connect(function(a_896, b_896, c_896, ...)
    TweenService:Create(Sell_AllButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_21, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
Sell_AllButton.MouseLeave:Connect(function(a_897, b_897, c_897, ...)
    TweenService:Create(Sell_AllButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_21, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
local RebirthButton = Instance.new"TextButton";
RebirthButton.Name = "RebirthButton";
RebirthButton.BackgroundColor3 = FromRGB(255, 50, 50);
RebirthButton.BorderSizePixel = 0;
RebirthButton.Size = UDim2_New(1, -10, 0, 40);
RebirthButton.Font = Enum.Font.GothamBold;
RebirthButton.Text = "Rebirth";
RebirthButton.TextColor3 = FromRGB(255, 255, 255);
RebirthButton.TextSize = 14;
RebirthButton.Parent = MainContent;
local UICorner_29 = Instance.new"UICorner";
UICorner_29.CornerRadius = UDim_New(0, 8);
UICorner_29.Parent = RebirthButton;
local UIGradient_9 = Instance.new"UIGradient";
UIGradient_9.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_9.Rotation = 90;
UIGradient_9.Parent = RebirthButton;
local UIStroke_22 = Instance.new"UIStroke";
UIStroke_22.Color = FromRGB(255, 100, 100);
UIStroke_22.Thickness = 1;
UIStroke_22.Transparency = 0.5;
UIStroke_22.Parent = RebirthButton;
RebirthButton.MouseButton1Click:Connect(function(a_898, b_898, c_898, ...)
    TweenService:Create(RebirthButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(RebirthButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    pcall(function(a_899, b_899, c_899, ...)
        if not ReplicatedStorage_2 then -- didnt run, if id: 3668
        end;
        ReplicatedStorage_2:WaitForChild("Rebirth"):InvokeServer();
        StarterGui:SetCore("SendNotification", {
            ["Text"] = "Rebirth executed!",
            ["Title"] = "\226\156\133 Rebirth",
            ["Duration"] = 2
        });
    end);
end);
RebirthButton.MouseEnter:Connect(function(a_900, b_900, c_900, ...)
    TweenService:Create(RebirthButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_22, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
RebirthButton.MouseLeave:Connect(function(a_901, b_901, c_901, ...)
    TweenService:Create(RebirthButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_22, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
local Event_FeaturesSection = Instance.new"Frame";
Event_FeaturesSection.Name = "Event FeaturesSection";
Event_FeaturesSection.BackgroundTransparency = 1;
Event_FeaturesSection.Size = UDim2_New(1, 0, 0, 30);
Event_FeaturesSection.Parent = EventContent;
local Frame_11 = Instance.new"Frame";
Frame_11.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_11.BorderSizePixel = 0;
Frame_11.Position = UDim2_New(0, 0, 0.5, -1);
Frame_11.Size = UDim2_New(0.3, -5, 0, 2);
Frame_11.Parent = Event_FeaturesSection;
local UIGradient_10 = Instance.new"UIGradient";
UIGradient_10.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_10.Parent = Frame_11;
local Event_Features = Instance.new"TextLabel";
Event_Features.BackgroundTransparency = 1;
Event_Features.Position = UDim2_New(0.3, 0, 0, 0);
Event_Features.Size = UDim2_New(0.4, 0, 1, 0);
Event_Features.Font = Enum.Font.GothamBold;
Event_Features.Text = "Event Features";
Event_Features.TextColor3 = FromRGB(255, 50, 50);
Event_Features.TextSize = 13;
Event_Features.Parent = Event_FeaturesSection;
local Frame_12 = Instance.new"Frame";
Frame_12.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_12.BorderSizePixel = 0;
Frame_12.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_12.Size = UDim2_New(0.3, -5, 0, 2);
Frame_12.Parent = Event_FeaturesSection;
local UIGradient_11 = Instance.new"UIGradient";
UIGradient_11.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_11.Parent = Frame_12;
local Auto_Collect_UFO_CoinsToggle = Instance.new"Frame";
Auto_Collect_UFO_CoinsToggle.Name = "Auto Collect UFO CoinsToggle";
Auto_Collect_UFO_CoinsToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Auto_Collect_UFO_CoinsToggle.BorderSizePixel = 0;
Auto_Collect_UFO_CoinsToggle.Size = UDim2_New(1, -10, 0, 40);
Auto_Collect_UFO_CoinsToggle.Parent = EventContent;
local UICorner_30 = Instance.new"UICorner";
UICorner_30.CornerRadius = UDim_New(0, 8);
UICorner_30.Parent = Auto_Collect_UFO_CoinsToggle;
local UIStroke_23 = Instance.new"UIStroke";
UIStroke_23.Color = FromRGB(40, 40, 40);
UIStroke_23.Thickness = 1;
UIStroke_23.Transparency = 0.5;
UIStroke_23.Parent = Auto_Collect_UFO_CoinsToggle;
local Auto_Collect_UFO_Coins = Instance.new"TextLabel";
Auto_Collect_UFO_Coins.BackgroundTransparency = 1;
Auto_Collect_UFO_Coins.Position = UDim2_New(0, 12, 0, 0);
Auto_Collect_UFO_Coins.Size = UDim2_New(1, -60, 1, 0);
Auto_Collect_UFO_Coins.Font = Enum.Font.Gotham;
Auto_Collect_UFO_Coins.Text = "Auto Collect UFO Coins";
Auto_Collect_UFO_Coins.TextColor3 = FromRGB(220, 220, 220);
Auto_Collect_UFO_Coins.TextSize = 13;
Auto_Collect_UFO_Coins.TextXAlignment = Enum.TextXAlignment.Left;
Auto_Collect_UFO_Coins.Parent = Auto_Collect_UFO_CoinsToggle;
local TextButton_7 = Instance.new"TextButton";
TextButton_7.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_7.BorderSizePixel = 0;
TextButton_7.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_7.Size = UDim2_New(0, 42, 0, 28);
TextButton_7.Text = "";
TextButton_7.Parent = Auto_Collect_UFO_CoinsToggle;
local UICorner_31 = Instance.new"UICorner";
UICorner_31.CornerRadius = UDim_New(1, 0);
UICorner_31.Parent = TextButton_7;
local Frame_13 = Instance.new"Frame";
Frame_13.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_13.BorderSizePixel = 0;
Frame_13.Position = UDim2_New(0, 3, 0.5, -11);
Frame_13.Size = UDim2_New(0, 22, 0, 22);
Frame_13.Parent = TextButton_7;
local UICorner_32 = Instance.new"UICorner";
UICorner_32.CornerRadius = UDim_New(1, 0);
UICorner_32.Parent = Frame_13;
local UIStroke_24 = Instance.new"UIStroke";
UIStroke_24.Color = FromRGB(0, 0, 0);
UIStroke_24.Thickness = 2;
UIStroke_24.Transparency = 0.7;
UIStroke_24.Parent = Frame_13;
TextButton_7.MouseButton1Click:Connect(function(a_902, b_902, c_902, ...)
    TweenService:Create(Frame_13, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_7, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_23, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.AutoCollectUFOCoins = true;
end);
Auto_Collect_UFO_CoinsToggle.MouseEnter:Connect(function(a_903, b_903, c_903, ...)
    TweenService:Create(Auto_Collect_UFO_CoinsToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Auto_Collect_UFO_CoinsToggle.MouseLeave:Connect(function(a_904, b_904, c_904, ...)
    TweenService:Create(Auto_Collect_UFO_CoinsToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local v1121 = Instance.new"Frame";
v1121.Name = "Auto Collect RadioactiveToggle";
v1121.BackgroundColor3 = FromRGB(22, 22, 22);
v1121.BorderSizePixel = 0;
v1121.Size = UDim2_New(1, -10, 0, 40);
v1121.Parent = EventContent;
local UICorner_33 = Instance.new"UICorner";
UICorner_33.CornerRadius = UDim_New(0, 8);
UICorner_33.Parent = v1121;
local UIStroke_25 = Instance.new"UIStroke";
UIStroke_25.Color = FromRGB(40, 40, 40);
UIStroke_25.Thickness = 1;
UIStroke_25.Transparency = 0.5;
UIStroke_25.Parent = v1121;
local Auto_Collect_Radioactive = Instance.new"TextLabel";
Auto_Collect_Radioactive.BackgroundTransparency = 1;
Auto_Collect_Radioactive.Position = UDim2_New(0, 12, 0, 0);
Auto_Collect_Radioactive.Size = UDim2_New(1, -60, 1, 0);
Auto_Collect_Radioactive.Font = Enum.Font.Gotham;
Auto_Collect_Radioactive.Text = "Auto Collect Radioactive";
Auto_Collect_Radioactive.TextColor3 = FromRGB(220, 220, 220);
Auto_Collect_Radioactive.TextSize = 13;
Auto_Collect_Radioactive.TextXAlignment = Enum.TextXAlignment.Left;
Auto_Collect_Radioactive.Parent = v1121;
local TextButton_8 = Instance.new"TextButton";
TextButton_8.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_8.BorderSizePixel = 0;
TextButton_8.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_8.Size = UDim2_New(0, 42, 0, 28);
TextButton_8.Text = "";
TextButton_8.Parent = v1121;
local UICorner_34 = Instance.new"UICorner";
UICorner_34.CornerRadius = UDim_New(1, 0);
UICorner_34.Parent = TextButton_8;
local Frame_14 = Instance.new"Frame";
Frame_14.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_14.BorderSizePixel = 0;
Frame_14.Position = UDim2_New(0, 3, 0.5, -11);
Frame_14.Size = UDim2_New(0, 22, 0, 22);
Frame_14.Parent = TextButton_8;
local UICorner_35 = Instance.new"UICorner";
UICorner_35.CornerRadius = UDim_New(1, 0);
UICorner_35.Parent = Frame_14;
local UIStroke_26 = Instance.new"UIStroke";
UIStroke_26.Color = FromRGB(0, 0, 0);
UIStroke_26.Thickness = 2;
UIStroke_26.Transparency = 0.7;
UIStroke_26.Parent = Frame_14;
TextButton_8.MouseButton1Click:Connect(function(a_905, b_905, c_905, ...)
    TweenService:Create(Frame_14, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_8, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_25, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.AutoCollectRadioactiveCoins = true;
end);
v1121.MouseEnter:Connect(function(a_906, b_906, c_906, ...)
    TweenService:Create(v1121, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
v1121.MouseLeave:Connect(function(a_907, b_907, c_907, ...)
    TweenService:Create(v1121, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local v1175 = Instance.new"Frame";
v1175.Name = "Auto Collect Lucky BlockToggle";
v1175.BackgroundColor3 = FromRGB(22, 22, 22);
v1175.BorderSizePixel = 0;
v1175.Size = UDim2_New(1, -10, 0, 40);
v1175.Parent = EventContent;
local UICorner_36 = Instance.new"UICorner";
UICorner_36.CornerRadius = UDim_New(0, 8);
UICorner_36.Parent = v1175;
local UIStroke_27 = Instance.new"UIStroke";
UIStroke_27.Color = FromRGB(40, 40, 40);
UIStroke_27.Thickness = 1;
UIStroke_27.Transparency = 0.5;
UIStroke_27.Parent = v1175;
local Auto_Collect_Lucky_Block = Instance.new"TextLabel";
Auto_Collect_Lucky_Block.BackgroundTransparency = 1;
Auto_Collect_Lucky_Block.Position = UDim2_New(0, 12, 0, 0);
Auto_Collect_Lucky_Block.Size = UDim2_New(1, -60, 1, 0);
Auto_Collect_Lucky_Block.Font = Enum.Font.Gotham;
Auto_Collect_Lucky_Block.Text = "Auto Collect Lucky Block";
Auto_Collect_Lucky_Block.TextColor3 = FromRGB(220, 220, 220);
Auto_Collect_Lucky_Block.TextSize = 13;
Auto_Collect_Lucky_Block.TextXAlignment = Enum.TextXAlignment.Left;
Auto_Collect_Lucky_Block.Parent = v1175;
local TextButton_9 = Instance.new"TextButton";
TextButton_9.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_9.BorderSizePixel = 0;
TextButton_9.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_9.Size = UDim2_New(0, 42, 0, 28);
TextButton_9.Text = "";
TextButton_9.Parent = v1175;
local UICorner_37 = Instance.new"UICorner";
UICorner_37.CornerRadius = UDim_New(1, 0);
UICorner_37.Parent = TextButton_9;
local Frame_15 = Instance.new"Frame";
Frame_15.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_15.BorderSizePixel = 0;
Frame_15.Position = UDim2_New(0, 3, 0.5, -11);
Frame_15.Size = UDim2_New(0, 22, 0, 22);
Frame_15.Parent = TextButton_9;
local UICorner_38 = Instance.new"UICorner";
UICorner_38.CornerRadius = UDim_New(1, 0);
UICorner_38.Parent = Frame_15;
local UIStroke_28 = Instance.new"UIStroke";
UIStroke_28.Color = FromRGB(0, 0, 0);
UIStroke_28.Thickness = 2;
UIStroke_28.Transparency = 0.7;
UIStroke_28.Parent = Frame_15;
TextButton_9.MouseButton1Click:Connect(function(a_908, b_908, c_908, ...)
    TweenService:Create(Frame_15, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_9, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_27, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.AutoCollectLuckyBlock = true;
end);
v1175.MouseEnter:Connect(function(a_909, b_909, c_909, ...)
    TweenService:Create(v1175, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
v1175.MouseLeave:Connect(function(a_910, b_910, c_910, ...)
    TweenService:Create(v1175, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local Teleport_OptionsSection = Instance.new"Frame";
Teleport_OptionsSection.Name = "Teleport OptionsSection";
Teleport_OptionsSection.BackgroundTransparency = 1;
Teleport_OptionsSection.Size = UDim2_New(1, 0, 0, 30);
Teleport_OptionsSection.Parent = TeleportContent;
local Frame_16 = Instance.new"Frame";
Frame_16.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_16.BorderSizePixel = 0;
Frame_16.Position = UDim2_New(0, 0, 0.5, -1);
Frame_16.Size = UDim2_New(0.3, -5, 0, 2);
Frame_16.Parent = Teleport_OptionsSection;
local UIGradient_12 = Instance.new"UIGradient";
UIGradient_12.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_12.Parent = Frame_16;
local Teleport_Options = Instance.new"TextLabel";
Teleport_Options.BackgroundTransparency = 1;
Teleport_Options.Position = UDim2_New(0.3, 0, 0, 0);
Teleport_Options.Size = UDim2_New(0.4, 0, 1, 0);
Teleport_Options.Font = Enum.Font.GothamBold;
Teleport_Options.Text = "Teleport Options";
Teleport_Options.TextColor3 = FromRGB(255, 50, 50);
Teleport_Options.TextSize = 13;
Teleport_Options.Parent = Teleport_OptionsSection;
local Frame_17 = Instance.new"Frame";
Frame_17.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_17.BorderSizePixel = 0;
Frame_17.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_17.Size = UDim2_New(0.3, -5, 0, 2);
Frame_17.Parent = Teleport_OptionsSection;
local UIGradient_13 = Instance.new"UIGradient";
UIGradient_13.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_13.Parent = Frame_17;
local Teleport_To_BaseButton = Instance.new"TextButton";
Teleport_To_BaseButton.Name = "Teleport to BaseButton";
Teleport_To_BaseButton.BackgroundColor3 = FromRGB(255, 50, 50);
Teleport_To_BaseButton.BorderSizePixel = 0;
Teleport_To_BaseButton.Size = UDim2_New(1, -10, 0, 40);
Teleport_To_BaseButton.Font = Enum.Font.GothamBold;
Teleport_To_BaseButton.Text = "Teleport to Base";
Teleport_To_BaseButton.TextColor3 = FromRGB(255, 255, 255);
Teleport_To_BaseButton.TextSize = 14;
Teleport_To_BaseButton.Parent = TeleportContent;
local UICorner_39 = Instance.new"UICorner";
UICorner_39.CornerRadius = UDim_New(0, 8);
UICorner_39.Parent = Teleport_To_BaseButton;
local UIGradient_14 = Instance.new"UIGradient";
UIGradient_14.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_14.Rotation = 90;
UIGradient_14.Parent = Teleport_To_BaseButton;
local UIStroke_29 = Instance.new"UIStroke";
UIStroke_29.Color = FromRGB(255, 100, 100);
UIStroke_29.Thickness = 1;
UIStroke_29.Transparency = 0.5;
UIStroke_29.Parent = Teleport_To_BaseButton;
Teleport_To_BaseButton.MouseButton1Click:Connect(function(a_911, b_911, c_911, ...)
    TweenService:Create(Teleport_To_BaseButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(Teleport_To_BaseButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    pcall(function(a_912, b_912, c_912, ...)
        if workspace:FindFirstChild("Bases") then -- ran, if id: 3669

        end;
        for for_key_14, for_val_14 in pairs(Workspace_Bases:GetChildren()) do
            if (for_key_14 ~= nil) then -- ran, if id: 3670

            end;
            if for_val_14:GetAttribute("Holder") ~= UserId then -- ran, if id: 3671
            end;
        end;
    end);
end);
Teleport_To_BaseButton.MouseEnter:Connect(function(a_913, b_913, c_913, ...)
    TweenService:Create(Teleport_To_BaseButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_29, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
Teleport_To_BaseButton.MouseLeave:Connect(function(a_914, b_914, c_914, ...)
    TweenService:Create(Teleport_To_BaseButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_29, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
local Teleport_To_Highest_GapButton = Instance.new"TextButton";
Teleport_To_Highest_GapButton.Name = "Teleport to Highest GapButton";
Teleport_To_Highest_GapButton.BackgroundColor3 = FromRGB(255, 50, 50);
Teleport_To_Highest_GapButton.BorderSizePixel = 0;
Teleport_To_Highest_GapButton.Size = UDim2_New(1, -10, 0, 40);
Teleport_To_Highest_GapButton.Font = Enum.Font.GothamBold;
Teleport_To_Highest_GapButton.Text = "Teleport to Highest Gap";
Teleport_To_Highest_GapButton.TextColor3 = FromRGB(255, 255, 255);
Teleport_To_Highest_GapButton.TextSize = 14;
Teleport_To_Highest_GapButton.Parent = TeleportContent;
local UICorner_40 = Instance.new"UICorner";
UICorner_40.CornerRadius = UDim_New(0, 8);
UICorner_40.Parent = Teleport_To_Highest_GapButton;
local UIGradient_15 = Instance.new"UIGradient";
UIGradient_15.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_15.Rotation = 90;
UIGradient_15.Parent = Teleport_To_Highest_GapButton;
local UIStroke_30 = Instance.new"UIStroke";
UIStroke_30.Color = FromRGB(255, 100, 100);
UIStroke_30.Thickness = 1;
UIStroke_30.Transparency = 0.5;
UIStroke_30.Parent = Teleport_To_Highest_GapButton;
Teleport_To_Highest_GapButton.MouseButton1Click:Connect(function(a_915, b_915, c_915, ...)
    TweenService:Create(Teleport_To_Highest_GapButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(Teleport_To_Highest_GapButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    pcall(function(a_916, b_916, c_916, ...)
        if not workspace:FindFirstChild("Misc") then -- didnt run, if id: 3672
        end;
        if Workspace_Misc:FindFirstChild("Gaps") then -- ran, if id: 3673

        end;
        for for_key_15, for_val_15 in pairs(Workspace_Misc.Gaps:GetChildren()) do
            if (for_key_15 ~= nil) then -- ran, if id: 3674

            end;
            if (CFrame_New(for_val_15.WorldPivot.Position.X, -9.025, for_val_15.WorldPivot.Position.Z).Position - HumanoidRootPart.Position).Magnitude < 1 / 0 then -- ran, if id: 3675
            end;
        end;
    end);
end);
Teleport_To_Highest_GapButton.MouseEnter:Connect(function(a_917, b_917, c_917, ...)
    TweenService:Create(Teleport_To_Highest_GapButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_30, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
Teleport_To_Highest_GapButton.MouseLeave:Connect(function(a_918, b_918, c_918, ...)
    TweenService:Create(Teleport_To_Highest_GapButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_30, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
local MovementSection = Instance.new"Frame";
MovementSection.Name = "MovementSection";
MovementSection.BackgroundTransparency = 1;
MovementSection.Size = UDim2_New(1, 0, 0, 30);
MovementSection.Parent = MiscContent;
local Frame_18 = Instance.new"Frame";
Frame_18.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_18.BorderSizePixel = 0;
Frame_18.Position = UDim2_New(0, 0, 0.5, -1);
Frame_18.Size = UDim2_New(0.3, -5, 0, 2);
Frame_18.Parent = MovementSection;
local UIGradient_16 = Instance.new"UIGradient";
UIGradient_16.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_16.Parent = Frame_18;
local Movement = Instance.new"TextLabel";
Movement.BackgroundTransparency = 1;
Movement.Position = UDim2_New(0.3, 0, 0, 0);
Movement.Size = UDim2_New(0.4, 0, 1, 0);
Movement.Font = Enum.Font.GothamBold;
Movement.Text = "Movement";
Movement.TextColor3 = FromRGB(255, 50, 50);
Movement.TextSize = 13;
Movement.Parent = MovementSection;
local Frame_19 = Instance.new"Frame";
Frame_19.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_19.BorderSizePixel = 0;
Frame_19.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_19.Size = UDim2_New(0.3, -5, 0, 2);
Frame_19.Parent = MovementSection;
local UIGradient_17 = Instance.new"UIGradient";
UIGradient_17.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_17.Parent = Frame_19;
local Fly_WASD_SpaceShiftToggle = Instance.new"Frame";
Fly_WASD_SpaceShiftToggle.Name = "Fly (WASD + Space/Shift)Toggle";
Fly_WASD_SpaceShiftToggle.BackgroundColor3 = FromRGB(22, 22, 22);
Fly_WASD_SpaceShiftToggle.BorderSizePixel = 0;
Fly_WASD_SpaceShiftToggle.Size = UDim2_New(1, -10, 0, 40);
Fly_WASD_SpaceShiftToggle.Parent = MiscContent;
local UICorner_41 = Instance.new"UICorner";
UICorner_41.CornerRadius = UDim_New(0, 8);
UICorner_41.Parent = Fly_WASD_SpaceShiftToggle;
local UIStroke_31 = Instance.new"UIStroke";
UIStroke_31.Color = FromRGB(40, 40, 40);
UIStroke_31.Thickness = 1;
UIStroke_31.Transparency = 0.5;
UIStroke_31.Parent = Fly_WASD_SpaceShiftToggle;
local Fly_WASD_SpaceShift = Instance.new"TextLabel";
Fly_WASD_SpaceShift.BackgroundTransparency = 1;
Fly_WASD_SpaceShift.Position = UDim2_New(0, 12, 0, 0);
Fly_WASD_SpaceShift.Size = UDim2_New(1, -60, 1, 0);
Fly_WASD_SpaceShift.Font = Enum.Font.Gotham;
Fly_WASD_SpaceShift.Text = "Fly (WASD + Space/Shift)";
Fly_WASD_SpaceShift.TextColor3 = FromRGB(220, 220, 220);
Fly_WASD_SpaceShift.TextSize = 13;
Fly_WASD_SpaceShift.TextXAlignment = Enum.TextXAlignment.Left;
Fly_WASD_SpaceShift.Parent = Fly_WASD_SpaceShiftToggle;
local TextButton_10 = Instance.new"TextButton";
TextButton_10.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_10.BorderSizePixel = 0;
TextButton_10.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_10.Size = UDim2_New(0, 42, 0, 28);
TextButton_10.Text = "";
TextButton_10.Parent = Fly_WASD_SpaceShiftToggle;
local UICorner_42 = Instance.new"UICorner";
UICorner_42.CornerRadius = UDim_New(1, 0);
UICorner_42.Parent = TextButton_10;
local Frame_20 = Instance.new"Frame";
Frame_20.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_20.BorderSizePixel = 0;
Frame_20.Position = UDim2_New(0, 3, 0.5, -11);
Frame_20.Size = UDim2_New(0, 22, 0, 22);
Frame_20.Parent = TextButton_10;
local UICorner_43 = Instance.new"UICorner";
UICorner_43.CornerRadius = UDim_New(1, 0);
UICorner_43.Parent = Frame_20;
local UIStroke_32 = Instance.new"UIStroke";
UIStroke_32.Color = FromRGB(0, 0, 0);
UIStroke_32.Thickness = 2;
UIStroke_32.Transparency = 0.7;
UIStroke_32.Parent = Frame_20;
TextButton_10.MouseButton1Click:Connect(function(a_919, b_919, c_919, ...)
    TweenService:Create(Frame_20, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_10, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_31, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    if HumanoidRootPart then -- ran, if id: 3676
        BodyVelocity_2 = Instance.new"BodyVelocity";
    end;
    BodyVelocity_2.Velocity = Vector3_New(0, 0, 0);
    BodyVelocity_2.MaxForce = Vector3_New(9000000000, 9000000000, 9000000000);
    BodyVelocity_2.Parent = HumanoidRootPart;
    BodyGyro_2 = Instance.new"BodyGyro";
    BodyGyro_2.MaxTorque = Vector3_New(9000000000, 9000000000, 9000000000);
    BodyGyro_2.P = 90000;
    BodyGyro_2.Parent = HumanoidRootPart;
    Heartbeat:Connect(function(a_1488, b_1488, c_1488, ...)
        if not UserInputService:IsKeyDown(Enum.KeyCode.W) then -- didnt run, if id: 4786
        end;
        local CFrame_2 = workspace.CurrentCamera.CFrame;
        if not UserInputService:IsKeyDown(Enum.KeyCode.S) then -- didnt run, if id: 4787
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.A) then -- didnt run, if id: 4788
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.D) then -- didnt run, if id: 4789
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.Space) then -- didnt run, if id: 4790
        end;
        if not UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then -- didnt run, if id: 4791
        end;
        if not BodyVelocity_2 then -- didnt run, if id: 4792
        end;
        BodyVelocity_2.Velocity = (
            (
                (
                    (
                        (
                            (
                                Vector3_New(0, 0, 0) + CFrame_2.LookVector
                            ) - CFrame_2.LookVector
                        ) - CFrame_2.RightVector
                    ) + CFrame_2.RightVector
                ) + Vector3_New(0, 1, 0)
            ) - Vector3_New(0, 1, 0)
        ) * 50;
        if not BodyGyro_2 then -- didnt run, if id: 4793
        end;
        BodyGyro_2.CFrame = CFrame_2;
    end);
    StarterGui:SetCore("SendNotification", {
        ["Text"] = "Fly enabled! Use WASD + Space/Shift",
        ["Title"] = "\226\156\136\239\184\143 Fly",
        ["Duration"] = 3
    });
end);
Fly_WASD_SpaceShiftToggle.MouseEnter:Connect(function(a_920, b_920, c_920, ...)
    TweenService:Create(Fly_WASD_SpaceShiftToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
Fly_WASD_SpaceShiftToggle.MouseLeave:Connect(function(a_921, b_921, c_921, ...)
    TweenService:Create(Fly_WASD_SpaceShiftToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local God_ModeSection = Instance.new"Frame";
God_ModeSection.Name = "God ModeSection";
God_ModeSection.BackgroundTransparency = 1;
God_ModeSection.Size = UDim2_New(1, 0, 0, 30);
God_ModeSection.Parent = MiscContent;
local Frame_21 = Instance.new"Frame";
Frame_21.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_21.BorderSizePixel = 0;
Frame_21.Position = UDim2_New(0, 0, 0.5, -1);
Frame_21.Size = UDim2_New(0.3, -5, 0, 2);
Frame_21.Parent = God_ModeSection;
local UIGradient_18 = Instance.new"UIGradient";
UIGradient_18.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_18.Parent = Frame_21;
local God_Mode = Instance.new"TextLabel";
God_Mode.BackgroundTransparency = 1;
God_Mode.Position = UDim2_New(0.3, 0, 0, 0);
God_Mode.Size = UDim2_New(0.4, 0, 1, 0);
God_Mode.Font = Enum.Font.GothamBold;
God_Mode.Text = "God Mode";
God_Mode.TextColor3 = FromRGB(255, 50, 50);
God_Mode.TextSize = 13;
God_Mode.Parent = God_ModeSection;
local Frame_22 = Instance.new"Frame";
Frame_22.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_22.BorderSizePixel = 0;
Frame_22.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_22.Size = UDim2_New(0.3, -5, 0, 2);
Frame_22.Parent = God_ModeSection;
local UIGradient_19 = Instance.new"UIGradient";
UIGradient_19.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_19.Parent = Frame_22;
local God_Mode_Clone_MethodToggle = Instance.new"Frame";
God_Mode_Clone_MethodToggle.Name = "God Mode (Clone Method)Toggle";
God_Mode_Clone_MethodToggle.BackgroundColor3 = FromRGB(22, 22, 22);
God_Mode_Clone_MethodToggle.BorderSizePixel = 0;
God_Mode_Clone_MethodToggle.Size = UDim2_New(1, -10, 0, 40);
God_Mode_Clone_MethodToggle.Parent = MiscContent;
local UICorner_44 = Instance.new"UICorner";
UICorner_44.CornerRadius = UDim_New(0, 8);
UICorner_44.Parent = God_Mode_Clone_MethodToggle;
local UIStroke_33 = Instance.new"UIStroke";
UIStroke_33.Color = FromRGB(40, 40, 40);
UIStroke_33.Thickness = 1;
UIStroke_33.Transparency = 0.5;
UIStroke_33.Parent = God_Mode_Clone_MethodToggle;
local God_Mode_Clone_Method = Instance.new"TextLabel";
God_Mode_Clone_Method.BackgroundTransparency = 1;
God_Mode_Clone_Method.Position = UDim2_New(0, 12, 0, 0);
God_Mode_Clone_Method.Size = UDim2_New(1, -60, 1, 0);
God_Mode_Clone_Method.Font = Enum.Font.Gotham;
God_Mode_Clone_Method.Text = "God Mode (Clone Method)";
God_Mode_Clone_Method.TextColor3 = FromRGB(220, 220, 220);
God_Mode_Clone_Method.TextSize = 13;
God_Mode_Clone_Method.TextXAlignment = Enum.TextXAlignment.Left;
God_Mode_Clone_Method.Parent = God_Mode_Clone_MethodToggle;
local TextButton_11 = Instance.new"TextButton";
TextButton_11.BackgroundColor3 = FromRGB(40, 40, 40);
TextButton_11.BorderSizePixel = 0;
TextButton_11.Position = UDim2_New(1, -48, 0.5, -14);
TextButton_11.Size = UDim2_New(0, 42, 0, 28);
TextButton_11.Text = "";
TextButton_11.Parent = God_Mode_Clone_MethodToggle;
local UICorner_45 = Instance.new"UICorner";
UICorner_45.CornerRadius = UDim_New(1, 0);
UICorner_45.Parent = TextButton_11;
local Frame_23 = Instance.new"Frame";
Frame_23.BackgroundColor3 = FromRGB(120, 120, 120);
Frame_23.BorderSizePixel = 0;
Frame_23.Position = UDim2_New(0, 3, 0.5, -11);
Frame_23.Size = UDim2_New(0, 22, 0, 22);
Frame_23.Parent = TextButton_11;
local UICorner_46 = Instance.new"UICorner";
UICorner_46.CornerRadius = UDim_New(1, 0);
UICorner_46.Parent = Frame_23;
local UIStroke_34 = Instance.new"UIStroke";
UIStroke_34.Color = FromRGB(0, 0, 0);
UIStroke_34.Thickness = 2;
UIStroke_34.Transparency = 0.7;
UIStroke_34.Parent = Frame_23;
TextButton_11.MouseButton1Click:Connect(function(a_922, b_922, c_922, ...)
    TweenService:Create(Frame_23, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50),
        ["Position"] = UDim2_New(1, -25, 0.5, -11)
    }):Play();
    TweenService:Create(TextButton_11, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        ["BackgroundColor3"] = FromRGB(60, 20, 20)
    }):Play();
    TweenService:Create(UIStroke_33, TweenInfo_New(0.3), {
        ["Color"] = FromRGB(255, 50, 50),
        ["Transparency"] = 0
    }):Play();
    _G.GodMode = true;
    if Character then -- ran, if id: 3677
        Character_2 = Character:FindFirstChild("HumanoidRootPart");
    end;
    Character_3 = Character:FindFirstChild("Humanoid");
    if not Character_2 then -- didnt run, if id: 3678
    end;
    if Character_3 then -- ran, if id: 3679
        Character.Archivable = true;
    end;
    Character_4 = Character:Clone();
    Character_4.Parent = workspace;
    Character.Archivable = false;
    for for_key_16, for_val_16 in pairs(Character_4:GetDescendants()) do
        if (for_key_16 ~= nil) then -- ran, if id: 3680

        end;
        if not for_val_16:IsA("BasePart") then -- didnt run, if id: 3681
        end;
        for_val_16.Transparency = 0.5;
        for_val_16.CanCollide = true;
    end;
    Character_3.PlatformStand = true;
    Humanoid = Character_4.Humanoid;
    workspace.CurrentCamera.CameraSubject = Humanoid;
    Stepped:Connect(function(a_1489, b_1489, c_1489, ...)
        if not Character then -- didnt run, if id: 4794
        end;
        for for_key_1108, for_val_1108 in pairs(Character:GetDescendants()) do
            if (for_key_1108 ~= nil) then -- ran, if id: 4795

            end;
            if not for_val_1108:IsA("BasePart") then -- didnt run, if id: 4796
            end;
            for_val_1108.CanCollide = false;
        end;
    end);
    Heartbeat:Connect(function(a_1490, b_1490, c_1490, ...)
        if not Character_4 then -- didnt run, if id: 4797
        end;
        if not Character:FindFirstChild("HumanoidRootPart") then -- didnt run, if id: 4798
        end;
        Humanoid:Move(Character_3.MoveDirection);
        Humanoid.Jump = Character_3.Jump;
        Character_2.CFrame = Character_4.HumanoidRootPart.CFrame * CFrame_New(0, -10, 0);
        Character_2.AssemblyLinearVelocity = Vector3.zero;
    end);
    StarterGui:SetCore("SendNotification", {
        ["Text"] = "God Mode enabled! You are invincible!",
        ["Title"] = "\240\159\155\161\239\184\143 God Mode",
        ["Duration"] = 3
    });
end);
God_Mode_Clone_MethodToggle.MouseEnter:Connect(function(a_923, b_923, c_923, ...)
    TweenService:Create(God_Mode_Clone_MethodToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(28, 28, 28)
    }):Play();
end);
God_Mode_Clone_MethodToggle.MouseLeave:Connect(function(a_924, b_924, c_924, ...)
    TweenService:Create(God_Mode_Clone_MethodToggle, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(22, 22, 22)
    }):Play();
end);
local WorldSection = Instance.new"Frame";
WorldSection.Name = "WorldSection";
WorldSection.BackgroundTransparency = 1;
WorldSection.Size = UDim2_New(1, 0, 0, 30);
WorldSection.Parent = MiscContent;
local Frame_24 = Instance.new"Frame";
Frame_24.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_24.BorderSizePixel = 0;
Frame_24.Position = UDim2_New(0, 0, 0.5, -1);
Frame_24.Size = UDim2_New(0.3, -5, 0, 2);
Frame_24.Parent = WorldSection;
local UIGradient_20 = Instance.new"UIGradient";
UIGradient_20.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_20.Parent = Frame_24;
local World = Instance.new"TextLabel";
World.BackgroundTransparency = 1;
World.Position = UDim2_New(0.3, 0, 0, 0);
World.Size = UDim2_New(0.4, 0, 1, 0);
World.Font = Enum.Font.GothamBold;
World.Text = "World";
World.TextColor3 = FromRGB(255, 50, 50);
World.TextSize = 13;
World.Parent = WorldSection;
local Frame_25 = Instance.new"Frame";
Frame_25.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_25.BorderSizePixel = 0;
Frame_25.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_25.Size = UDim2_New(0.3, -5, 0, 2);
Frame_25.Parent = WorldSection;
local UIGradient_21 = Instance.new"UIGradient";
UIGradient_21.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_21.Parent = Frame_25;
local Destroy_TsunamiButton = Instance.new"TextButton";
Destroy_TsunamiButton.Name = "Destroy TsunamiButton";
Destroy_TsunamiButton.BackgroundColor3 = FromRGB(255, 50, 50);
Destroy_TsunamiButton.BorderSizePixel = 0;
Destroy_TsunamiButton.Size = UDim2_New(1, -10, 0, 40);
Destroy_TsunamiButton.Font = Enum.Font.GothamBold;
Destroy_TsunamiButton.Text = "Destroy Tsunami";
Destroy_TsunamiButton.TextColor3 = FromRGB(255, 255, 255);
Destroy_TsunamiButton.TextSize = 14;
Destroy_TsunamiButton.Parent = MiscContent;
local UICorner_47 = Instance.new"UICorner";
UICorner_47.CornerRadius = UDim_New(0, 8);
UICorner_47.Parent = Destroy_TsunamiButton;
local UIGradient_22 = Instance.new"UIGradient";
UIGradient_22.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_22.Rotation = 90;
UIGradient_22.Parent = Destroy_TsunamiButton;
local UIStroke_35 = Instance.new"UIStroke";
UIStroke_35.Color = FromRGB(255, 100, 100);
UIStroke_35.Thickness = 1;
UIStroke_35.Transparency = 0.5;
UIStroke_35.Parent = Destroy_TsunamiButton;
Destroy_TsunamiButton.MouseButton1Click:Connect(function(a_925, b_925, c_925, ...)
    TweenService:Create(Destroy_TsunamiButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(Destroy_TsunamiButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    pcall(function(a_926, b_926, c_926, ...)
        if not workspace:FindFirstChild("ActiveTsunamis") then -- didnt run, if id: 3682
        end;
        workspace.ActiveTsunamis:Destroy();
        if not workspace:FindFirstChild("AllowedSpaces") then -- didnt run, if id: 3683
        end;
        workspace.AllowedSpaces:Destroy();
        StarterGui:SetCore("SendNotification", {
            ["Text"] = "Tsunami destroyed!",
            ["Title"] = "\226\156\133 Tsunami",
            ["Duration"] = 2
        });
    end);
end);
Destroy_TsunamiButton.MouseEnter:Connect(function(a_927, b_927, c_927, ...)
    TweenService:Create(Destroy_TsunamiButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_35, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
Destroy_TsunamiButton.MouseLeave:Connect(function(a_928, b_928, c_928, ...)
    TweenService:Create(Destroy_TsunamiButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_35, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
local Destroy_VIP_WallsButton = Instance.new"TextButton";
Destroy_VIP_WallsButton.Name = "Destroy VIP WallsButton";
Destroy_VIP_WallsButton.BackgroundColor3 = FromRGB(255, 50, 50);
Destroy_VIP_WallsButton.BorderSizePixel = 0;
Destroy_VIP_WallsButton.Size = UDim2_New(1, -10, 0, 40);
Destroy_VIP_WallsButton.Font = Enum.Font.GothamBold;
Destroy_VIP_WallsButton.Text = "Destroy VIP Walls";
Destroy_VIP_WallsButton.TextColor3 = FromRGB(255, 255, 255);
Destroy_VIP_WallsButton.TextSize = 14;
Destroy_VIP_WallsButton.Parent = MiscContent;
local UICorner_48 = Instance.new"UICorner";
UICorner_48.CornerRadius = UDim_New(0, 8);
UICorner_48.Parent = Destroy_VIP_WallsButton;
local UIGradient_23 = Instance.new"UIGradient";
UIGradient_23.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_23.Rotation = 90;
UIGradient_23.Parent = Destroy_VIP_WallsButton;
local UIStroke_36 = Instance.new"UIStroke";
UIStroke_36.Color = FromRGB(255, 100, 100);
UIStroke_36.Thickness = 1;
UIStroke_36.Transparency = 0.5;
UIStroke_36.Parent = Destroy_VIP_WallsButton;
Destroy_VIP_WallsButton.MouseButton1Click:Connect(function(a_929, b_929, c_929, ...)
    TweenService:Create(Destroy_VIP_WallsButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(Destroy_VIP_WallsButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    pcall(function(a_930, b_930, c_930, ...)
        if not workspace:FindFirstChild("VIPWalls") then -- didnt run, if id: 3684
        end;
        workspace.VIPWalls:Destroy();
        StarterGui:SetCore("SendNotification", {
            ["Text"] = "VIP walls destroyed!",
            ["Title"] = "\226\156\133 VIP Walls",
            ["Duration"] = 2
        });
    end);
end);
Destroy_VIP_WallsButton.MouseEnter:Connect(function(a_931, b_931, c_931, ...)
    TweenService:Create(Destroy_VIP_WallsButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_36, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
Destroy_VIP_WallsButton.MouseLeave:Connect(function(a_932, b_932, c_932, ...)
    TweenService:Create(Destroy_VIP_WallsButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_36, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
local Server_OptionsSection = Instance.new"Frame";
Server_OptionsSection.Name = "Server OptionsSection";
Server_OptionsSection.BackgroundTransparency = 1;
Server_OptionsSection.Size = UDim2_New(1, 0, 0, 30);
Server_OptionsSection.Parent = ServerContent;
local Frame_26 = Instance.new"Frame";
Frame_26.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_26.BorderSizePixel = 0;
Frame_26.Position = UDim2_New(0, 0, 0.5, -1);
Frame_26.Size = UDim2_New(0.3, -5, 0, 2);
Frame_26.Parent = Server_OptionsSection;
local UIGradient_24 = Instance.new"UIGradient";
UIGradient_24.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50, 0)
    )
};
UIGradient_24.Parent = Frame_26;
local Server_Options = Instance.new"TextLabel";
Server_Options.BackgroundTransparency = 1;
Server_Options.Position = UDim2_New(0.3, 0, 0, 0);
Server_Options.Size = UDim2_New(0.4, 0, 1, 0);
Server_Options.Font = Enum.Font.GothamBold;
Server_Options.Text = "Server Options";
Server_Options.TextColor3 = FromRGB(255, 50, 50);
Server_Options.TextSize = 13;
Server_Options.Parent = Server_OptionsSection;
local Frame_27 = Instance.new"Frame";
Frame_27.BackgroundColor3 = FromRGB(255, 50, 50);
Frame_27.BorderSizePixel = 0;
Frame_27.Position = UDim2_New(0.7, 5, 0.5, -1);
Frame_27.Size = UDim2_New(0.3, -5, 0, 2);
Frame_27.Parent = Server_OptionsSection;
local UIGradient_25 = Instance.new"UIGradient";
UIGradient_25.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 50, 50, 0)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 50, 50)
    )
};
UIGradient_25.Parent = Frame_27;
local Rejoin_ServerButton = Instance.new"TextButton";
Rejoin_ServerButton.Name = "Rejoin ServerButton";
Rejoin_ServerButton.BackgroundColor3 = FromRGB(255, 50, 50);
Rejoin_ServerButton.BorderSizePixel = 0;
Rejoin_ServerButton.Size = UDim2_New(1, -10, 0, 40);
Rejoin_ServerButton.Font = Enum.Font.GothamBold;
Rejoin_ServerButton.Text = "Rejoin Server";
Rejoin_ServerButton.TextColor3 = FromRGB(255, 255, 255);
Rejoin_ServerButton.TextSize = 14;
Rejoin_ServerButton.Parent = ServerContent;
local UICorner_49 = Instance.new"UICorner";
UICorner_49.CornerRadius = UDim_New(0, 8);
UICorner_49.Parent = Rejoin_ServerButton;
local UIGradient_26 = Instance.new"UIGradient";
UIGradient_26.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_26.Rotation = 90;
UIGradient_26.Parent = Rejoin_ServerButton;
local UIStroke_37 = Instance.new"UIStroke";
UIStroke_37.Color = FromRGB(255, 100, 100);
UIStroke_37.Thickness = 1;
UIStroke_37.Transparency = 0.5;
UIStroke_37.Parent = Rejoin_ServerButton;
Rejoin_ServerButton.MouseButton1Click:Connect(function(a_933, b_933, c_933, ...)
    TweenService:Create(Rejoin_ServerButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(Rejoin_ServerButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer);
end);
Rejoin_ServerButton.MouseEnter:Connect(function(a_934, b_934, c_934, ...)
    TweenService:Create(Rejoin_ServerButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_37, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
Rejoin_ServerButton.MouseLeave:Connect(function(a_935, b_935, c_935, ...)
    TweenService:Create(Rejoin_ServerButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_37, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
local Server_HopButton = Instance.new"TextButton";
Server_HopButton.Name = "Server HopButton";
Server_HopButton.BackgroundColor3 = FromRGB(255, 50, 50);
Server_HopButton.BorderSizePixel = 0;
Server_HopButton.Size = UDim2_New(1, -10, 0, 40);
Server_HopButton.Font = Enum.Font.GothamBold;
Server_HopButton.Text = "Server Hop";
Server_HopButton.TextColor3 = FromRGB(255, 255, 255);
Server_HopButton.TextSize = 14;
Server_HopButton.Parent = ServerContent;
local UICorner_50 = Instance.new"UICorner";
UICorner_50.CornerRadius = UDim_New(0, 8);
UICorner_50.Parent = Server_HopButton;
local UIGradient_27 = Instance.new"UIGradient";
UIGradient_27.Color = ColorSequence_New{
    ColorSequenceKeypoint_New(
        0,
        FromRGB(255, 60, 60)
    ),
    ColorSequenceKeypoint_New(
        1,
        FromRGB(255, 40, 40)
    )
};
UIGradient_27.Rotation = 90;
UIGradient_27.Parent = Server_HopButton;
local UIStroke_38 = Instance.new"UIStroke";
UIStroke_38.Color = FromRGB(255, 100, 100);
UIStroke_38.Thickness = 1;
UIStroke_38.Transparency = 0.5;
UIStroke_38.Parent = Server_HopButton;
Server_HopButton.MouseButton1Click:Connect(function(a_936, b_936, c_936, ...)
    TweenService:Create(Server_HopButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -15, 0, 38)
    }):Play();
    task.wait(0.1);
    TweenService:Create(Server_HopButton, TweenInfo_New(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ["Size"] = UDim2_New(1, -10, 0, 40)
    }):Play();
    StarterGui:SetCore("SendNotification", {
        ["Text"] = "Finding new server...",
        ["Title"] = "\240\159\148\132 Server Hop",
        ["Duration"] = 2
    });
end);
Server_HopButton.MouseEnter:Connect(function(a_937, b_937, c_937, ...)
    TweenService:Create(Server_HopButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 70, 70)
    }):Play();
    TweenService:Create(UIStroke_38, TweenInfo_New(0.2), {
        ["Transparency"] = 0
    }):Play();
end);
Server_HopButton.MouseLeave:Connect(function(a_938, b_938, c_938, ...)
    TweenService:Create(Server_HopButton, TweenInfo_New(0.2), {
        ["BackgroundColor3"] = FromRGB(255, 50, 50)
    }):Play();
    TweenService:Create(UIStroke_38, TweenInfo_New(0.2), {
        ["Transparency"] = 0.5
    }):Play();
end);
task.wait(0.1);
MiscContent.Visible = false;
MainContent.Visible = true;
TweenInfo_New = TweenInfo.new;
TweenService:Create(MainContent, TweenInfo_New(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    ["Position"] = UDim2_New(0, 0, 0, 0)
}):Play();
TeleportContent.Visible = false;
EventContent.Visible = false;
ServerContent.Visible = false;
TweenService:Create(MainTab, TweenInfo_New(0.3), {
    ["TextColor3"] = FromRGB(255, 255, 255),
    ["BackgroundColor3"] = FromRGB(255, 50, 50)
}):Play();
MainFrame.Size = UDim2_New(0, 0, 0, 0);
MainFrame.Position = UDim2_New(0.5, 0, 0.5, 0);
MainFrame.Rotation = 180;
TweenService:Create(MainFrame, TweenInfo_New(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    ["Rotation"] = 0,
    ["Position"] = UDim2_New(0.5, -275, 0.5, -225),
    ["Size"] = UDim2_New(0, 550, 0, 450)
}):Play();
for for_key_1, for_val_1 in pairs(MainFrame:GetDescendants()) do
    if (for_key_1 ~= nil) then
        TextLabel = for_val_1:IsA("TextLabel");
    end;
    if TextLabel then
        if not TextLabel then

        end;
    end;
    for_val_1.TextTransparency = 1;
    task.wait(0.05);
    TweenService:Create(for_val_1, TweenInfo_New(0.5), {
        ["TextTransparency"] = 0
    }):Play();
end;
task.spawn(function()
    task.wait();
    pcall(function(a_65, b_65, c_65, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1711
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1712
        end;
    end);
    task.wait();
    pcall(function(a_66, b_66, c_66, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1713
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1714
        end;
    end);
    task.wait();
    pcall(function(a_67, b_67, c_67, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1715
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1716
        end;
    end);
    task.wait();
    pcall(function(a_68, b_68, c_68, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1717
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1718
        end;
    end);
    task.wait();
    pcall(function(a_69, b_69, c_69, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1719
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1720
        end;
    end);
    task.wait();
    pcall(function(a_70, b_70, c_70, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1721
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1722
        end;
    end);
    task.wait();
    pcall(function(a_71, b_71, c_71, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1723
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1724
        end;
    end);
    task.wait();
    pcall(function(a_72, b_72, c_72, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1725
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1726
        end;
    end);
    task.wait();
    pcall(function(a_73, b_73, c_73, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1727
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1728
        end;
    end);
    task.wait();
    pcall(function(a_74, b_74, c_74, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1729
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1730
        end;
    end);
    task.wait();
    pcall(function(a_75, b_75, c_75, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1731
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1732
        end;
    end);
    task.wait();
    pcall(function(a_76, b_76, c_76, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1733
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1734
        end;
    end);
    task.wait();
    pcall(function(a_77, b_77, c_77, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1735
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1736
        end;
    end);
    task.wait();
    pcall(function(a_78, b_78, c_78, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1737
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1738
        end;
    end);
    task.wait();
    pcall(function(a_79, b_79, c_79, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1739
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1740
        end;
    end);
    task.wait();
    pcall(function(a_80, b_80, c_80, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1741
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1742
        end;
    end);
    task.wait();
    pcall(function(a_81, b_81, c_81, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1743
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1744
        end;
    end);
    task.wait();
    pcall(function(a_82, b_82, c_82, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1745
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1746
        end;
    end);
    task.wait();
    pcall(function(a_83, b_83, c_83, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1747
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1748
        end;
    end);
    task.wait();
    pcall(function(a_84, b_84, c_84, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1749
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1750
        end;
    end);
    task.wait();
    pcall(function(a_85, b_85, c_85, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1751
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1752
        end;
    end);
    task.wait();
    pcall(function(a_86, b_86, c_86, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1753
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1754
        end;
    end);
    task.wait();
    pcall(function(a_87, b_87, c_87, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1755
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1756
        end;
    end);
    task.wait();
    pcall(function(a_88, b_88, c_88, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1757
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1758
        end;
    end);
    task.wait();
    pcall(function(a_89, b_89, c_89, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1759
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1760
        end;
    end);
    task.wait();
    pcall(function(a_90, b_90, c_90, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1761
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1762
        end;
    end);
    task.wait();
    pcall(function(a_91, b_91, c_91, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1763
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1764
        end;
    end);
    task.wait();
    pcall(function(a_92, b_92, c_92, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1765
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1766
        end;
    end);
    task.wait();
    pcall(function(a_93, b_93, c_93, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1767
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1768
        end;
    end);
    task.wait();
    pcall(function(a_94, b_94, c_94, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1769
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1770
        end;
    end);
    task.wait();
    pcall(function(a_95, b_95, c_95, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1771
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1772
        end;
    end);
    task.wait();
    pcall(function(a_96, b_96, c_96, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1773
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1774
        end;
    end);
    task.wait();
    pcall(function(a_97, b_97, c_97, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1775
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1776
        end;
    end);
    task.wait();
    pcall(function(a_98, b_98, c_98, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1777
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1778
        end;
    end);
    task.wait();
    pcall(function(a_99, b_99, c_99, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1779
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1780
        end;
    end);
    task.wait();
    pcall(function(a_100, b_100, c_100, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1781
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1782
        end;
    end);
    task.wait();
    pcall(function(a_101, b_101, c_101, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1783
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1784
        end;
    end);
    task.wait();
    pcall(function(a_102, b_102, c_102, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1785
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1786
        end;
    end);
    task.wait();
    pcall(function(a_103, b_103, c_103, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1787
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1788
        end;
    end);
    task.wait();
    pcall(function(a_104, b_104, c_104, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1789
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1790
        end;
    end);
    task.wait();
    pcall(function(a_105, b_105, c_105, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1791
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1792
        end;
    end);
    task.wait();
    pcall(function(a_106, b_106, c_106, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1793
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1794
        end;
    end);
    task.wait();
    pcall(function(a_107, b_107, c_107, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1795
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1796
        end;
    end);
    task.wait();
    pcall(function(a_108, b_108, c_108, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1797
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1798
        end;
    end);
    task.wait();
    pcall(function(a_109, b_109, c_109, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1799
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1800
        end;
    end);
    task.wait();
    pcall(function(a_110, b_110, c_110, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1801
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1802
        end;
    end);
    task.wait();
    pcall(function(a_111, b_111, c_111, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1803
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1804
        end;
    end);
    task.wait();
    pcall(function(a_112, b_112, c_112, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1805
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1806
        end;
    end);
    task.wait();
    pcall(function(a_113, b_113, c_113, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1807
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1808
        end;
    end);
    task.wait();
    pcall(function(a_114, b_114, c_114, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1809
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1810
        end;
    end);
    task.wait();
    pcall(function(a_115, b_115, c_115, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1811
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1812
        end;
    end);
    task.wait();
    pcall(function(a_116, b_116, c_116, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1813
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1814
        end;
    end);
    task.wait();
    pcall(function(a_117, b_117, c_117, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1815
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1816
        end;
    end);
    task.wait();
    pcall(function(a_118, b_118, c_118, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1817
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1818
        end;
    end);
    task.wait();
    pcall(function(a_119, b_119, c_119, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1819
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1820
        end;
    end);
    task.wait();
    pcall(function(a_120, b_120, c_120, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1821
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1822
        end;
    end);
    task.wait();
    pcall(function(a_121, b_121, c_121, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1823
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1824
        end;
    end);
    task.wait();
    pcall(function(a_122, b_122, c_122, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1825
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1826
        end;
    end);
    task.wait();
    pcall(function(a_123, b_123, c_123, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1827
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1828
        end;
    end);
    task.wait();
    pcall(function(a_124, b_124, c_124, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1829
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1830
        end;
    end);
    task.wait();
    pcall(function(a_125, b_125, c_125, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1831
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1832
        end;
    end);
    task.wait();
    pcall(function(a_126, b_126, c_126, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1833
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1834
        end;
    end);
    task.wait();
    pcall(function(a_127, b_127, c_127, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1835
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1836
        end;
    end);
    task.wait();
    pcall(function(a_128, b_128, c_128, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1837
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1838
        end;
    end);
    task.wait();
    pcall(function(a_129, b_129, c_129, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1839
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1840
        end;
    end);
    task.wait();
    pcall(function(a_130, b_130, c_130, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1841
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1842
        end;
    end);
    task.wait();
    pcall(function(a_131, b_131, c_131, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1843
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1844
        end;
    end);
    task.wait();
    pcall(function(a_132, b_132, c_132, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1845
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1846
        end;
    end);
    task.wait();
    pcall(function(a_133, b_133, c_133, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1847
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1848
        end;
    end);
    task.wait();
    pcall(function(a_134, b_134, c_134, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1849
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1850
        end;
    end);
    task.wait();
    pcall(function(a_135, b_135, c_135, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1851
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1852
        end;
    end);
    task.wait();
    pcall(function(a_136, b_136, c_136, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1853
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1854
        end;
    end);
    task.wait();
    pcall(function(a_137, b_137, c_137, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1855
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1856
        end;
    end);
    task.wait();
    pcall(function(a_138, b_138, c_138, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1857
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1858
        end;
    end);
    task.wait();
    pcall(function(a_139, b_139, c_139, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1859
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1860
        end;
    end);
    task.wait();
    pcall(function(a_140, b_140, c_140, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1861
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1862
        end;
    end);
    task.wait();
    pcall(function(a_141, b_141, c_141, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1863
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1864
        end;
    end);
    task.wait();
    pcall(function(a_142, b_142, c_142, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1865
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1866
        end;
    end);
    task.wait();
    pcall(function(a_143, b_143, c_143, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1867
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1868
        end;
    end);
    task.wait();
    pcall(function(a_144, b_144, c_144, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1869
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1870
        end;
    end);
    task.wait();
    pcall(function(a_145, b_145, c_145, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1871
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1872
        end;
    end);
    task.wait();
    pcall(function(a_146, b_146, c_146, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1873
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1874
        end;
    end);
    task.wait();
    pcall(function(a_147, b_147, c_147, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1875
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1876
        end;
    end);
    task.wait();
    pcall(function(a_148, b_148, c_148, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1877
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1878
        end;
    end);
    task.wait();
    pcall(function(a_149, b_149, c_149, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1879
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1880
        end;
    end);
    task.wait();
    pcall(function(a_150, b_150, c_150, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1881
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1882
        end;
    end);
    task.wait();
    pcall(function(a_151, b_151, c_151, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1883
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1884
        end;
    end);
    task.wait();
    pcall(function(a_152, b_152, c_152, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1885
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1886
        end;
    end);
    task.wait();
    pcall(function(a_153, b_153, c_153, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1887
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1888
        end;
    end);
    task.wait();
    pcall(function(a_154, b_154, c_154, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1889
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1890
        end;
    end);
    task.wait();
    pcall(function(a_155, b_155, c_155, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1891
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1892
        end;
    end);
    task.wait();
    pcall(function(a_156, b_156, c_156, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1893
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1894
        end;
    end);
    task.wait();
    pcall(function(a_157, b_157, c_157, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1895
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1896
        end;
    end);
    task.wait();
    pcall(function(a_158, b_158, c_158, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1897
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1898
        end;
    end);
    task.wait();
    pcall(function(a_159, b_159, c_159, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1899
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1900
        end;
    end);
    task.wait();
    pcall(function(a_160, b_160, c_160, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1901
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1902
        end;
    end);
    task.wait();
    pcall(function(a_161, b_161, c_161, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1903
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1904
        end;
    end);
    task.wait();
    pcall(function(a_162, b_162, c_162, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1905
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1906
        end;
    end);
    task.wait();
    pcall(function(a_163, b_163, c_163, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1907
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1908
        end;
    end);
    task.wait();
    pcall(function(a_164, b_164, c_164, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1909
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1910
        end;
    end);
    task.wait();
    pcall(function(a_165, b_165, c_165, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1911
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1912
        end;
    end);
    task.wait();
    pcall(function(a_166, b_166, c_166, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1913
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1914
        end;
    end);
    task.wait();
    pcall(function(a_167, b_167, c_167, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1915
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1916
        end;
    end);
    task.wait();
    pcall(function(a_168, b_168, c_168, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1917
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1918
        end;
    end);
    task.wait();
    pcall(function(a_169, b_169, c_169, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1919
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1920
        end;
    end);
    task.wait();
    pcall(function(a_170, b_170, c_170, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1921
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1922
        end;
    end);
    task.wait();
    pcall(function(a_171, b_171, c_171, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1923
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1924
        end;
    end);
    task.wait();
    pcall(function(a_172, b_172, c_172, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1925
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1926
        end;
    end);
    task.wait();
    pcall(function(a_173, b_173, c_173, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1927
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1928
        end;
    end);
    task.wait();
    pcall(function(a_174, b_174, c_174, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1929
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1930
        end;
    end);
    task.wait();
    pcall(function(a_175, b_175, c_175, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1931
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1932
        end;
    end);
    task.wait();
    pcall(function(a_176, b_176, c_176, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1933
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1934
        end;
    end);
    task.wait();
    pcall(function(a_177, b_177, c_177, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1935
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1936
        end;
    end);
    task.wait();
    pcall(function(a_178, b_178, c_178, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1937
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1938
        end;
    end);
    task.wait();
    pcall(function(a_179, b_179, c_179, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1939
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1940
        end;
    end);
    task.wait();
    pcall(function(a_180, b_180, c_180, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1941
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1942
        end;
    end);
    task.wait();
    pcall(function(a_181, b_181, c_181, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1943
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1944
        end;
    end);
    task.wait();
    pcall(function(a_182, b_182, c_182, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1945
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1946
        end;
    end);
    task.wait();
    pcall(function(a_183, b_183, c_183, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1947
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1948
        end;
    end);
    task.wait();
    pcall(function(a_184, b_184, c_184, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1949
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1950
        end;
    end);
    task.wait();
    pcall(function(a_185, b_185, c_185, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1951
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1952
        end;
    end);
    task.wait();
    pcall(function(a_186, b_186, c_186, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1953
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1954
        end;
    end);
    task.wait();
    pcall(function(a_187, b_187, c_187, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1955
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1956
        end;
    end);
    task.wait();
    pcall(function(a_188, b_188, c_188, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1957
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1958
        end;
    end);
    task.wait();
    pcall(function(a_189, b_189, c_189, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1959
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1960
        end;
    end);
    task.wait();
    pcall(function(a_190, b_190, c_190, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1961
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1962
        end;
    end);
    task.wait();
    pcall(function(a_191, b_191, c_191, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1963
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1964
        end;
    end);
    task.wait();
    pcall(function(a_192, b_192, c_192, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1965
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1966
        end;
    end);
    task.wait();
    pcall(function(a_193, b_193, c_193, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1967
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1968
        end;
    end);
    task.wait();
    pcall(function(a_194, b_194, c_194, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1969
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1970
        end;
    end);
    task.wait();
    pcall(function(a_195, b_195, c_195, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1971
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1972
        end;
    end);
    task.wait();
    pcall(function(a_196, b_196, c_196, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1973
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1974
        end;
    end);
    task.wait();
    pcall(function(a_197, b_197, c_197, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1975
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1976
        end;
    end);
    task.wait();
    pcall(function(a_198, b_198, c_198, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1977
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1978
        end;
    end);
    task.wait();
    pcall(function(a_199, b_199, c_199, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1979
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1980
        end;
    end);
    task.wait();
    pcall(function(a_200, b_200, c_200, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1981
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1982
        end;
    end);
    task.wait();
    pcall(function(a_201, b_201, c_201, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1983
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1984
        end;
    end);
    task.wait();
    pcall(function(a_202, b_202, c_202, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1985
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1986
        end;
    end);
    task.wait();
    pcall(function(a_203, b_203, c_203, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1987
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1988
        end;
    end);
    task.wait();
    pcall(function(a_204, b_204, c_204, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1989
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1990
        end;
    end);
    task.wait();
    pcall(function(a_205, b_205, c_205, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1991
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1992
        end;
    end);
    task.wait();
    pcall(function(a_206, b_206, c_206, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1993
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1994
        end;
    end);
    task.wait();
    pcall(function(a_207, b_207, c_207, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1995
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1996
        end;
    end);
    task.wait();
    pcall(function(a_208, b_208, c_208, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1997
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 1998
        end;
    end);
    task.wait();
    pcall(function(a_209, b_209, c_209, ...)
        if not HumanoidRootPart then -- didnt run, if id: 1999
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2000
        end;
    end);
    task.wait();
    pcall(function(a_210, b_210, c_210, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2001
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2002
        end;
    end);
    task.wait();
    pcall(function(a_211, b_211, c_211, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2003
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2004
        end;
    end);
    task.wait();
    pcall(function(a_212, b_212, c_212, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2005
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2006
        end;
    end);
    task.wait();
    pcall(function(a_213, b_213, c_213, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2007
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2008
        end;
    end);
    task.wait();
    pcall(function(a_214, b_214, c_214, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2009
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2010
        end;
    end);
    task.wait();
    pcall(function(a_215, b_215, c_215, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2011
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2012
        end;
    end);
    task.wait();
    pcall(function(a_216, b_216, c_216, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2013
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2014
        end;
    end);
    task.wait();
    pcall(function(a_217, b_217, c_217, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2015
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2016
        end;
    end);
    task.wait();
    pcall(function(a_218, b_218, c_218, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2017
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2018
        end;
    end);
    task.wait();
    pcall(function(a_219, b_219, c_219, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2019
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2020
        end;
    end);
    task.wait();
    pcall(function(a_220, b_220, c_220, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2021
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2022
        end;
    end);
    task.wait();
    pcall(function(a_221, b_221, c_221, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2023
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2024
        end;
    end);
    task.wait();
    pcall(function(a_222, b_222, c_222, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2025
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2026
        end;
    end);
    task.wait();
    pcall(function(a_223, b_223, c_223, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2027
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2028
        end;
    end);
    task.wait();
    pcall(function(a_224, b_224, c_224, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2029
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2030
        end;
    end);
    task.wait();
    pcall(function(a_225, b_225, c_225, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2031
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2032
        end;
    end);
    task.wait();
    pcall(function(a_226, b_226, c_226, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2033
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2034
        end;
    end);
    task.wait();
    pcall(function(a_227, b_227, c_227, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2035
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2036
        end;
    end);
    task.wait();
    pcall(function(a_228, b_228, c_228, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2037
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2038
        end;
    end);
    task.wait();
    pcall(function(a_229, b_229, c_229, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2039
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2040
        end;
    end);
    task.wait();
    pcall(function(a_230, b_230, c_230, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2041
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2042
        end;
    end);
    task.wait();
    pcall(function(a_231, b_231, c_231, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2043
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2044
        end;
    end);
    task.wait();
    pcall(function(a_232, b_232, c_232, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2045
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2046
        end;
    end);
    task.wait();
    pcall(function(a_233, b_233, c_233, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2047
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2048
        end;
    end);
    task.wait();
    pcall(function(a_234, b_234, c_234, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2049
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2050
        end;
    end);
    task.wait();
    pcall(function(a_235, b_235, c_235, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2051
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2052
        end;
    end);
    task.wait();
    pcall(function(a_236, b_236, c_236, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2053
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2054
        end;
    end);
    task.wait();
    pcall(function(a_237, b_237, c_237, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2055
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2056
        end;
    end);
    task.wait();
    pcall(function(a_238, b_238, c_238, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2057
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2058
        end;
    end);
    task.wait();
    pcall(function(a_239, b_239, c_239, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2059
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2060
        end;
    end);
    task.wait();
    pcall(function(a_240, b_240, c_240, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2061
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2062
        end;
    end);
    task.wait();
    pcall(function(a_241, b_241, c_241, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2063
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2064
        end;
    end);
    task.wait();
    pcall(function(a_242, b_242, c_242, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2065
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2066
        end;
    end);
    task.wait();
    pcall(function(a_243, b_243, c_243, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2067
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2068
        end;
    end);
    task.wait();
    pcall(function(a_244, b_244, c_244, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2069
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2070
        end;
    end);
    task.wait();
    pcall(function(a_245, b_245, c_245, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2071
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2072
        end;
    end);
    task.wait();
    pcall(function(a_246, b_246, c_246, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2073
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2074
        end;
    end);
    task.wait();
    pcall(function(a_247, b_247, c_247, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2075
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2076
        end;
    end);
    task.wait();
    pcall(function(a_248, b_248, c_248, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2077
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2078
        end;
    end);
    task.wait();
    pcall(function(a_249, b_249, c_249, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2079
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2080
        end;
    end);
    task.wait();
    pcall(function(a_250, b_250, c_250, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2081
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2082
        end;
    end);
    task.wait();
    pcall(function(a_251, b_251, c_251, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2083
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2084
        end;
    end);
    task.wait();
    pcall(function(a_252, b_252, c_252, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2085
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2086
        end;
    end);
    task.wait();
    pcall(function(a_253, b_253, c_253, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2087
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2088
        end;
    end);
    task.wait();
    pcall(function(a_254, b_254, c_254, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2089
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2090
        end;
    end);
    task.wait();
    pcall(function(a_255, b_255, c_255, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2091
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2092
        end;
    end);
    task.wait();
    pcall(function(a_256, b_256, c_256, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2093
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2094
        end;
    end);
    task.wait();
    pcall(function(a_257, b_257, c_257, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2095
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2096
        end;
    end);
    task.wait();
    pcall(function(a_258, b_258, c_258, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2097
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2098
        end;
    end);
    task.wait();
    pcall(function(a_259, b_259, c_259, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2099
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2100
        end;
    end);
    task.wait();
    pcall(function(a_260, b_260, c_260, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2101
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2102
        end;
    end);
    task.wait();
    pcall(function(a_261, b_261, c_261, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2103
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2104
        end;
    end);
    task.wait();
    pcall(function(a_262, b_262, c_262, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2105
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2106
        end;
    end);
    task.wait();
    pcall(function(a_263, b_263, c_263, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2107
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2108
        end;
    end);
    task.wait();
    pcall(function(a_264, b_264, c_264, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2109
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2110
        end;
    end);
    task.wait();
    pcall(function(a_265, b_265, c_265, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2111
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2112
        end;
    end);
    task.wait();
    pcall(function(a_266, b_266, c_266, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2113
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2114
        end;
    end);
    task.wait();
    pcall(function(a_267, b_267, c_267, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2115
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2116
        end;
    end);
    task.wait();
    pcall(function(a_268, b_268, c_268, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2117
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2118
        end;
    end);
    task.wait();
    pcall(function(a_269, b_269, c_269, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2119
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2120
        end;
    end);
    task.wait();
    pcall(function(a_270, b_270, c_270, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2121
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2122
        end;
    end);
    task.wait();
    pcall(function(a_271, b_271, c_271, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2123
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2124
        end;
    end);
    task.wait();
    pcall(function(a_272, b_272, c_272, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2125
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2126
        end;
    end);
    task.wait();
    pcall(function(a_273, b_273, c_273, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2127
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2128
        end;
    end);
    task.wait();
    pcall(function(a_274, b_274, c_274, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2129
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2130
        end;
    end);
    task.wait();
    pcall(function(a_275, b_275, c_275, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2131
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2132
        end;
    end);
    task.wait();
    pcall(function(a_276, b_276, c_276, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2133
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2134
        end;
    end);
    task.wait();
    pcall(function(a_277, b_277, c_277, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2135
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2136
        end;
    end);
    task.wait();
    pcall(function(a_278, b_278, c_278, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2137
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2138
        end;
    end);
    task.wait();
    pcall(function(a_279, b_279, c_279, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2139
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2140
        end;
    end);
    task.wait();
    pcall(function(a_280, b_280, c_280, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2141
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2142
        end;
    end);
    task.wait();
    pcall(function(a_281, b_281, c_281, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2143
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2144
        end;
    end);
    task.wait();
    pcall(function(a_282, b_282, c_282, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2145
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2146
        end;
    end);
    task.wait();
    pcall(function(a_283, b_283, c_283, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2147
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2148
        end;
    end);
    task.wait();
    pcall(function(a_284, b_284, c_284, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2149
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2150
        end;
    end);
    task.wait();
    pcall(function(a_285, b_285, c_285, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2151
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2152
        end;
    end);
    task.wait();
    pcall(function(a_286, b_286, c_286, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2153
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2154
        end;
    end);
    task.wait();
    pcall(function(a_287, b_287, c_287, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2155
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2156
        end;
    end);
    task.wait();
    pcall(function(a_288, b_288, c_288, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2157
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2158
        end;
    end);
    task.wait();
    pcall(function(a_289, b_289, c_289, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2159
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2160
        end;
    end);
    task.wait();
    pcall(function(a_290, b_290, c_290, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2161
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2162
        end;
    end);
    task.wait();
    pcall(function(a_291, b_291, c_291, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2163
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2164
        end;
    end);
    task.wait();
    pcall(function(a_292, b_292, c_292, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2165
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2166
        end;
    end);
    task.wait();
    pcall(function(a_293, b_293, c_293, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2167
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2168
        end;
    end);
    task.wait();
    pcall(function(a_294, b_294, c_294, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2169
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2170
        end;
    end);
    task.wait();
    pcall(function(a_295, b_295, c_295, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2171
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2172
        end;
    end);
    task.wait();
    pcall(function(a_296, b_296, c_296, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2173
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2174
        end;
    end);
    task.wait();
    pcall(function(a_297, b_297, c_297, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2175
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2176
        end;
    end);
    task.wait();
    pcall(function(a_298, b_298, c_298, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2177
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2178
        end;
    end);
    task.wait();
    pcall(function(a_299, b_299, c_299, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2179
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2180
        end;
    end);
    task.wait();
    pcall(function(a_300, b_300, c_300, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2181
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2182
        end;
    end);
    task.wait();
    pcall(function(a_301, b_301, c_301, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2183
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2184
        end;
    end);
    task.wait();
    pcall(function(a_302, b_302, c_302, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2185
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2186
        end;
    end);
    task.wait();
    pcall(function(a_303, b_303, c_303, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2187
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2188
        end;
    end);
    task.wait();
    pcall(function(a_304, b_304, c_304, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2189
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2190
        end;
    end);
    task.wait();
    pcall(function(a_305, b_305, c_305, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2191
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2192
        end;
    end);
    task.wait();
    pcall(function(a_306, b_306, c_306, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2193
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2194
        end;
    end);
    task.wait();
    pcall(function(a_307, b_307, c_307, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2195
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2196
        end;
    end);
    task.wait();
    pcall(function(a_308, b_308, c_308, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2197
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2198
        end;
    end);
    task.wait();
    pcall(function(a_309, b_309, c_309, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2199
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2200
        end;
    end);
    task.wait();
    pcall(function(a_310, b_310, c_310, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2201
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2202
        end;
    end);
    task.wait();
    pcall(function(a_311, b_311, c_311, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2203
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2204
        end;
    end);
    task.wait();
    pcall(function(a_312, b_312, c_312, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2205
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2206
        end;
    end);
    task.wait();
    pcall(function(a_313, b_313, c_313, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2207
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2208
        end;
    end);
    task.wait();
    pcall(function(a_314, b_314, c_314, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2209
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2210
        end;
    end);
    task.wait();
    pcall(function(a_315, b_315, c_315, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2211
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2212
        end;
    end);
    task.wait();
    pcall(function(a_316, b_316, c_316, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2213
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2214
        end;
    end);
    task.wait();
    pcall(function(a_317, b_317, c_317, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2215
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2216
        end;
    end);
    task.wait();
    pcall(function(a_318, b_318, c_318, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2217
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2218
        end;
    end);
    task.wait();
    pcall(function(a_319, b_319, c_319, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2219
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2220
        end;
    end);
    task.wait();
    pcall(function(a_320, b_320, c_320, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2221
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2222
        end;
    end);
    task.wait();
    pcall(function(a_321, b_321, c_321, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2223
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2224
        end;
    end);
    task.wait();
    pcall(function(a_322, b_322, c_322, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2225
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2226
        end;
    end);
    task.wait();
    pcall(function(a_323, b_323, c_323, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2227
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2228
        end;
    end);
    task.wait();
    pcall(function(a_324, b_324, c_324, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2229
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2230
        end;
    end);
    task.wait();
    pcall(function(a_325, b_325, c_325, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2231
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2232
        end;
    end);
    task.wait();
    pcall(function(a_326, b_326, c_326, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2233
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2234
        end;
    end);
    task.wait();
    pcall(function(a_327, b_327, c_327, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2235
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2236
        end;
    end);
    task.wait();
    pcall(function(a_328, b_328, c_328, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2237
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2238
        end;
    end);
    task.wait();
    pcall(function(a_329, b_329, c_329, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2239
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2240
        end;
    end);
    task.wait();
    pcall(function(a_330, b_330, c_330, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2241
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2242
        end;
    end);
    task.wait();
    pcall(function(a_331, b_331, c_331, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2243
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2244
        end;
    end);
    task.wait();
    pcall(function(a_332, b_332, c_332, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2245
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2246
        end;
    end);
    task.wait();
    pcall(function(a_333, b_333, c_333, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2247
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2248
        end;
    end);
    task.wait();
    pcall(function(a_334, b_334, c_334, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2249
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2250
        end;
    end);
    task.wait();
    pcall(function(a_335, b_335, c_335, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2251
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2252
        end;
    end);
    task.wait();
    pcall(function(a_336, b_336, c_336, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2253
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2254
        end;
    end);
    task.wait();
    pcall(function(a_337, b_337, c_337, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2255
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2256
        end;
    end);
    task.wait();
    pcall(function(a_338, b_338, c_338, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2257
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2258
        end;
    end);
    task.wait();
    pcall(function(a_339, b_339, c_339, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2259
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2260
        end;
    end);
    task.wait();
    pcall(function(a_340, b_340, c_340, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2261
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2262
        end;
    end);
    task.wait();
    pcall(function(a_341, b_341, c_341, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2263
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2264
        end;
    end);
    task.wait();
    pcall(function(a_342, b_342, c_342, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2265
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2266
        end;
    end);
    task.wait();
    pcall(function(a_343, b_343, c_343, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2267
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2268
        end;
    end);
    task.wait();
    pcall(function(a_344, b_344, c_344, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2269
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2270
        end;
    end);
    task.wait();
    pcall(function(a_345, b_345, c_345, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2271
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2272
        end;
    end);
    task.wait();
    pcall(function(a_346, b_346, c_346, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2273
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2274
        end;
    end);
    task.wait();
    pcall(function(a_347, b_347, c_347, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2275
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2276
        end;
    end);
    task.wait();
    pcall(function(a_348, b_348, c_348, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2277
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2278
        end;
    end);
    task.wait();
    pcall(function(a_349, b_349, c_349, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2279
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2280
        end;
    end);
    task.wait();
    pcall(function(a_350, b_350, c_350, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2281
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2282
        end;
    end);
    task.wait();
    pcall(function(a_351, b_351, c_351, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2283
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2284
        end;
    end);
    task.wait();
    pcall(function(a_352, b_352, c_352, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2285
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2286
        end;
    end);
    task.wait();
    pcall(function(a_353, b_353, c_353, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2287
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2288
        end;
    end);
    task.wait();
    pcall(function(a_354, b_354, c_354, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2289
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2290
        end;
    end);
    task.wait();
    pcall(function(a_355, b_355, c_355, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2291
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2292
        end;
    end);
    task.wait();
    pcall(function(a_356, b_356, c_356, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2293
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2294
        end;
    end);
    task.wait();
    pcall(function(a_357, b_357, c_357, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2295
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2296
        end;
    end);
    task.wait();
    pcall(function(a_358, b_358, c_358, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2297
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2298
        end;
    end);
    task.wait();
    pcall(function(a_359, b_359, c_359, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2299
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2300
        end;
    end);
    task.wait();
    pcall(function(a_360, b_360, c_360, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2301
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2302
        end;
    end);
    task.wait();
    pcall(function(a_361, b_361, c_361, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2303
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2304
        end;
    end);
    task.wait();
    pcall(function(a_362, b_362, c_362, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2305
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2306
        end;
    end);
    task.wait();
    pcall(function(a_363, b_363, c_363, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2307
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2308
        end;
    end);
    task.wait();
    pcall(function(a_364, b_364, c_364, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2309
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2310
        end;
    end);
    task.wait();
    pcall(function(a_365, b_365, c_365, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2311
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2312
        end;
    end);
    task.wait();
    pcall(function(a_366, b_366, c_366, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2313
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2314
        end;
    end);
    task.wait();
    pcall(function(a_367, b_367, c_367, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2315
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2316
        end;
    end);
    task.wait();
    pcall(function(a_368, b_368, c_368, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2317
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2318
        end;
    end);
    task.wait();
    pcall(function(a_369, b_369, c_369, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2319
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2320
        end;
    end);
    task.wait();
    pcall(function(a_370, b_370, c_370, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2321
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2322
        end;
    end);
    task.wait();
    pcall(function(a_371, b_371, c_371, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2323
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2324
        end;
    end);
    task.wait();
    pcall(function(a_372, b_372, c_372, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2325
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2326
        end;
    end);
    task.wait();
    pcall(function(a_373, b_373, c_373, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2327
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2328
        end;
    end);
    task.wait();
    pcall(function(a_374, b_374, c_374, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2329
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2330
        end;
    end);
    task.wait();
    pcall(function(a_375, b_375, c_375, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2331
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2332
        end;
    end);
    task.wait();
    pcall(function(a_376, b_376, c_376, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2333
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2334
        end;
    end);
    task.wait();
    pcall(function(a_377, b_377, c_377, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2335
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2336
        end;
    end);
    task.wait();
    pcall(function(a_378, b_378, c_378, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2337
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2338
        end;
    end);
    task.wait();
    pcall(function(a_379, b_379, c_379, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2339
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2340
        end;
    end);
    task.wait();
    pcall(function(a_380, b_380, c_380, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2341
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2342
        end;
    end);
    task.wait();
    pcall(function(a_381, b_381, c_381, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2343
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2344
        end;
    end);
    task.wait();
    pcall(function(a_382, b_382, c_382, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2345
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2346
        end;
    end);
    task.wait();
    pcall(function(a_383, b_383, c_383, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2347
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2348
        end;
    end);
    task.wait();
    pcall(function(a_384, b_384, c_384, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2349
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2350
        end;
    end);
    task.wait();
    pcall(function(a_385, b_385, c_385, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2351
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2352
        end;
    end);
    task.wait();
    pcall(function(a_386, b_386, c_386, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2353
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2354
        end;
    end);
    task.wait();
    pcall(function(a_387, b_387, c_387, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2355
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2356
        end;
    end);
    task.wait();
    pcall(function(a_388, b_388, c_388, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2357
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2358
        end;
    end);
    task.wait();
    pcall(function(a_389, b_389, c_389, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2359
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2360
        end;
    end);
    task.wait();
    pcall(function(a_390, b_390, c_390, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2361
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2362
        end;
    end);
    task.wait();
    pcall(function(a_391, b_391, c_391, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2363
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2364
        end;
    end);
    task.wait();
    pcall(function(a_392, b_392, c_392, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2365
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2366
        end;
    end);
    task.wait();
    pcall(function(a_393, b_393, c_393, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2367
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2368
        end;
    end);
    task.wait();
    pcall(function(a_394, b_394, c_394, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2369
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2370
        end;
    end);
    task.wait();
    pcall(function(a_395, b_395, c_395, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2371
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2372
        end;
    end);
    task.wait();
    pcall(function(a_396, b_396, c_396, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2373
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2374
        end;
    end);
    task.wait();
    pcall(function(a_397, b_397, c_397, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2375
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2376
        end;
    end);
    task.wait();
    pcall(function(a_398, b_398, c_398, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2377
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2378
        end;
    end);
    task.wait();
    pcall(function(a_399, b_399, c_399, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2379
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2380
        end;
    end);
    task.wait();
    pcall(function(a_400, b_400, c_400, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2381
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2382
        end;
    end);
    task.wait();
    pcall(function(a_401, b_401, c_401, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2383
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2384
        end;
    end);
    task.wait();
    pcall(function(a_402, b_402, c_402, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2385
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2386
        end;
    end);
    task.wait();
    pcall(function(a_403, b_403, c_403, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2387
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2388
        end;
    end);
    task.wait();
    pcall(function(a_404, b_404, c_404, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2389
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2390
        end;
    end);
    task.wait();
    pcall(function(a_405, b_405, c_405, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2391
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2392
        end;
    end);
    task.wait();
    pcall(function(a_406, b_406, c_406, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2393
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2394
        end;
    end);
    task.wait();
    pcall(function(a_407, b_407, c_407, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2395
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2396
        end;
    end);
    task.wait();
    pcall(function(a_408, b_408, c_408, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2397
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2398
        end;
    end);
    task.wait();
    pcall(function(a_409, b_409, c_409, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2399
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2400
        end;
    end);
    task.wait();
    pcall(function(a_410, b_410, c_410, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2401
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2402
        end;
    end);
    task.wait();
    pcall(function(a_411, b_411, c_411, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2403
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2404
        end;
    end);
    task.wait();
    pcall(function(a_412, b_412, c_412, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2405
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2406
        end;
    end);
    task.wait();
    pcall(function(a_413, b_413, c_413, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2407
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2408
        end;
    end);
    task.wait();
    pcall(function(a_414, b_414, c_414, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2409
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2410
        end;
    end);
    task.wait();
    pcall(function(a_415, b_415, c_415, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2411
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2412
        end;
    end);
    task.wait();
    pcall(function(a_416, b_416, c_416, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2413
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2414
        end;
    end);
    task.wait();
    pcall(function(a_417, b_417, c_417, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2415
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2416
        end;
    end);
    task.wait();
    pcall(function(a_418, b_418, c_418, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2417
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2418
        end;
    end);
    task.wait();
    pcall(function(a_419, b_419, c_419, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2419
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2420
        end;
    end);
    task.wait();
    pcall(function(a_420, b_420, c_420, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2421
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2422
        end;
    end);
    task.wait();
    pcall(function(a_421, b_421, c_421, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2423
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2424
        end;
    end);
    task.wait();
    pcall(function(a_422, b_422, c_422, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2425
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2426
        end;
    end);
    task.wait();
    pcall(function(a_423, b_423, c_423, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2427
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2428
        end;
    end);
    task.wait();
    pcall(function(a_424, b_424, c_424, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2429
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2430
        end;
    end);
    task.wait();
    pcall(function(a_425, b_425, c_425, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2431
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2432
        end;
    end);
    task.wait();
    pcall(function(a_426, b_426, c_426, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2433
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2434
        end;
    end);
    task.wait();
    pcall(function(a_427, b_427, c_427, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2435
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2436
        end;
    end);
    task.wait();
    pcall(function(a_428, b_428, c_428, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2437
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2438
        end;
    end);
    task.wait();
    pcall(function(a_429, b_429, c_429, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2439
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2440
        end;
    end);
    task.wait();
    pcall(function(a_430, b_430, c_430, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2441
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2442
        end;
    end);
    task.wait();
    pcall(function(a_431, b_431, c_431, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2443
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2444
        end;
    end);
    task.wait();
    pcall(function(a_432, b_432, c_432, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2445
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2446
        end;
    end);
    task.wait();
    pcall(function(a_433, b_433, c_433, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2447
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2448
        end;
    end);
    task.wait();
    pcall(function(a_434, b_434, c_434, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2449
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2450
        end;
    end);
    task.wait();
    pcall(function(a_435, b_435, c_435, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2451
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2452
        end;
    end);
    task.wait();
    pcall(function(a_436, b_436, c_436, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2453
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2454
        end;
    end);
    task.wait();
    pcall(function(a_437, b_437, c_437, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2455
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2456
        end;
    end);
    task.wait();
    pcall(function(a_438, b_438, c_438, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2457
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2458
        end;
    end);
    task.wait();
    pcall(function(a_439, b_439, c_439, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2459
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2460
        end;
    end);
    task.wait();
    pcall(function(a_440, b_440, c_440, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2461
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2462
        end;
    end);
    task.wait();
    pcall(function(a_441, b_441, c_441, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2463
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2464
        end;
    end);
    task.wait();
    pcall(function(a_442, b_442, c_442, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2465
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2466
        end;
    end);
    task.wait();
    pcall(function(a_443, b_443, c_443, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2467
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2468
        end;
    end);
    task.wait();
    pcall(function(a_444, b_444, c_444, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2469
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2470
        end;
    end);
    task.wait();
    pcall(function(a_445, b_445, c_445, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2471
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2472
        end;
    end);
    task.wait();
    pcall(function(a_446, b_446, c_446, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2473
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2474
        end;
    end);
    task.wait();
    pcall(function(a_447, b_447, c_447, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2475
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2476
        end;
    end);
    task.wait();
    pcall(function(a_448, b_448, c_448, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2477
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2478
        end;
    end);
    task.wait();
    pcall(function(a_449, b_449, c_449, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2479
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2480
        end;
    end);
    task.wait();
    pcall(function(a_450, b_450, c_450, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2481
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2482
        end;
    end);
    task.wait();
    pcall(function(a_451, b_451, c_451, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2483
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2484
        end;
    end);
    task.wait();
    pcall(function(a_452, b_452, c_452, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2485
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2486
        end;
    end);
    task.wait();
    pcall(function(a_453, b_453, c_453, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2487
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2488
        end;
    end);
    task.wait();
    pcall(function(a_454, b_454, c_454, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2489
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2490
        end;
    end);
    task.wait();
    pcall(function(a_455, b_455, c_455, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2491
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2492
        end;
    end);
    task.wait();
    pcall(function(a_456, b_456, c_456, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2493
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2494
        end;
    end);
    task.wait();
    pcall(function(a_457, b_457, c_457, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2495
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2496
        end;
    end);
    task.wait();
    pcall(function(a_458, b_458, c_458, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2497
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2498
        end;
    end);
    task.wait();
    pcall(function(a_459, b_459, c_459, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2499
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2500
        end;
    end);
    task.wait();
    pcall(function(a_460, b_460, c_460, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2501
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2502
        end;
    end);
    task.wait();
    pcall(function(a_461, b_461, c_461, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2503
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2504
        end;
    end);
    task.wait();
    pcall(function(a_462, b_462, c_462, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2505
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2506
        end;
    end);
    task.wait();
    pcall(function(a_463, b_463, c_463, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2507
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2508
        end;
    end);
    task.wait();
    pcall(function(a_464, b_464, c_464, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2509
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2510
        end;
    end);
    task.wait();
    pcall(function(a_465, b_465, c_465, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2511
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2512
        end;
    end);
    task.wait();
    pcall(function(a_466, b_466, c_466, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2513
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2514
        end;
    end);
    task.wait();
    pcall(function(a_467, b_467, c_467, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2515
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2516
        end;
    end);
    task.wait();
    pcall(function(a_468, b_468, c_468, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2517
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2518
        end;
    end);
    task.wait();
    pcall(function(a_469, b_469, c_469, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2519
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2520
        end;
    end);
    task.wait();
    pcall(function(a_470, b_470, c_470, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2521
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2522
        end;
    end);
    task.wait();
    pcall(function(a_471, b_471, c_471, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2523
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2524
        end;
    end);
    task.wait();
    pcall(function(a_472, b_472, c_472, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2525
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2526
        end;
    end);
    task.wait();
    pcall(function(a_473, b_473, c_473, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2527
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2528
        end;
    end);
    task.wait();
    pcall(function(a_474, b_474, c_474, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2529
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2530
        end;
    end);
    task.wait();
    pcall(function(a_475, b_475, c_475, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2531
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2532
        end;
    end);
    task.wait();
    pcall(function(a_476, b_476, c_476, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2533
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2534
        end;
    end);
    task.wait();
    pcall(function(a_477, b_477, c_477, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2535
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2536
        end;
    end);
    task.wait();
    pcall(function(a_478, b_478, c_478, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2537
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2538
        end;
    end);
    task.wait();
    pcall(function(a_479, b_479, c_479, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2539
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2540
        end;
    end);
    task.wait();
    pcall(function(a_480, b_480, c_480, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2541
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2542
        end;
    end);
    task.wait();
    pcall(function(a_481, b_481, c_481, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2543
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2544
        end;
    end);
    task.wait();
    pcall(function(a_482, b_482, c_482, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2545
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2546
        end;
    end);
    task.wait();
    pcall(function(a_483, b_483, c_483, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2547
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2548
        end;
    end);
    task.wait();
    pcall(function(a_484, b_484, c_484, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2549
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2550
        end;
    end);
    task.wait();
    pcall(function(a_485, b_485, c_485, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2551
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2552
        end;
    end);
    task.wait();
    pcall(function(a_486, b_486, c_486, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2553
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2554
        end;
    end);
    task.wait();
    pcall(function(a_487, b_487, c_487, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2555
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2556
        end;
    end);
    task.wait();
    pcall(function(a_488, b_488, c_488, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2557
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2558
        end;
    end);
    task.wait();
    pcall(function(a_489, b_489, c_489, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2559
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2560
        end;
    end);
    task.wait();
    pcall(function(a_490, b_490, c_490, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2561
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2562
        end;
    end);
    task.wait();
    pcall(function(a_491, b_491, c_491, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2563
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2564
        end;
    end);
    task.wait();
    pcall(function(a_492, b_492, c_492, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2565
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2566
        end;
    end);
    task.wait();
    pcall(function(a_493, b_493, c_493, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2567
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2568
        end;
    end);
    task.wait();
    pcall(function(a_494, b_494, c_494, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2569
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2570
        end;
    end);
    task.wait();
    pcall(function(a_495, b_495, c_495, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2571
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2572
        end;
    end);
    task.wait();
    pcall(function(a_496, b_496, c_496, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2573
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2574
        end;
    end);
    task.wait();
    pcall(function(a_497, b_497, c_497, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2575
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2576
        end;
    end);
    task.wait();
    pcall(function(a_498, b_498, c_498, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2577
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2578
        end;
    end);
    task.wait();
    pcall(function(a_499, b_499, c_499, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2579
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2580
        end;
    end);
    task.wait();
    pcall(function(a_500, b_500, c_500, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2581
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2582
        end;
    end);
    task.wait();
    pcall(function(a_501, b_501, c_501, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2583
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2584
        end;
    end);
    task.wait();
    pcall(function(a_502, b_502, c_502, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2585
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2586
        end;
    end);
    task.wait();
    pcall(function(a_503, b_503, c_503, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2587
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2588
        end;
    end);
    task.wait();
    pcall(function(a_504, b_504, c_504, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2589
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2590
        end;
    end);
    task.wait();
    pcall(function(a_505, b_505, c_505, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2591
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2592
        end;
    end);
    task.wait();
    pcall(function(a_506, b_506, c_506, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2593
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2594
        end;
    end);
    task.wait();
    pcall(function(a_507, b_507, c_507, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2595
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2596
        end;
    end);
    task.wait();
    pcall(function(a_508, b_508, c_508, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2597
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2598
        end;
    end);
    task.wait();
    pcall(function(a_509, b_509, c_509, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2599
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2600
        end;
    end);
    task.wait();
    pcall(function(a_510, b_510, c_510, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2601
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2602
        end;
    end);
    task.wait();
    pcall(function(a_511, b_511, c_511, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2603
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2604
        end;
    end);
    task.wait();
    pcall(function(a_512, b_512, c_512, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2605
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2606
        end;
    end);
    task.wait();
    pcall(function(a_513, b_513, c_513, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2607
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2608
        end;
    end);
    task.wait();
    pcall(function(a_514, b_514, c_514, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2609
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2610
        end;
    end);
    task.wait();
    pcall(function(a_515, b_515, c_515, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2611
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2612
        end;
    end);
    task.wait();
    pcall(function(a_516, b_516, c_516, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2613
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2614
        end;
    end);
    task.wait();
    pcall(function(a_517, b_517, c_517, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2615
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2616
        end;
    end);
    task.wait();
    pcall(function(a_518, b_518, c_518, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2617
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2618
        end;
    end);
    task.wait();
    pcall(function(a_519, b_519, c_519, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2619
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2620
        end;
    end);
    task.wait();
    pcall(function(a_520, b_520, c_520, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2621
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2622
        end;
    end);
    task.wait();
    pcall(function(a_521, b_521, c_521, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2623
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2624
        end;
    end);
    task.wait();
    pcall(function(a_522, b_522, c_522, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2625
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2626
        end;
    end);
    task.wait();
    pcall(function(a_523, b_523, c_523, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2627
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2628
        end;
    end);
    task.wait();
    pcall(function(a_524, b_524, c_524, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2629
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2630
        end;
    end);
    task.wait();
    pcall(function(a_525, b_525, c_525, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2631
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2632
        end;
    end);
    task.wait();
    pcall(function(a_526, b_526, c_526, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2633
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2634
        end;
    end);
    task.wait();
    pcall(function(a_527, b_527, c_527, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2635
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2636
        end;
    end);
    task.wait();
    pcall(function(a_528, b_528, c_528, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2637
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2638
        end;
    end);
    task.wait();
    pcall(function(a_529, b_529, c_529, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2639
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2640
        end;
    end);
    task.wait();
    pcall(function(a_530, b_530, c_530, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2641
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2642
        end;
    end);
    task.wait();
    pcall(function(a_531, b_531, c_531, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2643
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2644
        end;
    end);
    task.wait();
    pcall(function(a_532, b_532, c_532, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2645
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2646
        end;
    end);
    task.wait();
    pcall(function(a_533, b_533, c_533, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2647
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2648
        end;
    end);
    task.wait();
    pcall(function(a_534, b_534, c_534, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2649
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2650
        end;
    end);
    task.wait();
    pcall(function(a_535, b_535, c_535, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2651
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2652
        end;
    end);
    task.wait();
    pcall(function(a_536, b_536, c_536, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2653
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2654
        end;
    end);
    task.wait();
    pcall(function(a_537, b_537, c_537, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2655
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2656
        end;
    end);
    task.wait();
    pcall(function(a_538, b_538, c_538, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2657
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2658
        end;
    end);
    task.wait();
    pcall(function(a_539, b_539, c_539, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2659
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2660
        end;
    end);
    task.wait();
    pcall(function(a_540, b_540, c_540, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2661
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2662
        end;
    end);
    task.wait();
    pcall(function(a_541, b_541, c_541, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2663
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2664
        end;
    end);
    task.wait();
    pcall(function(a_542, b_542, c_542, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2665
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2666
        end;
    end);
    task.wait();
    pcall(function(a_543, b_543, c_543, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2667
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2668
        end;
    end);
    task.wait();
    pcall(function(a_544, b_544, c_544, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2669
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2670
        end;
    end);
    task.wait();
    pcall(function(a_545, b_545, c_545, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2671
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2672
        end;
    end);
    task.wait();
    pcall(function(a_546, b_546, c_546, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2673
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2674
        end;
    end);
    task.wait();
    pcall(function(a_547, b_547, c_547, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2675
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2676
        end;
    end);
    task.wait();
    pcall(function(a_548, b_548, c_548, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2677
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2678
        end;
    end);
    task.wait();
    pcall(function(a_549, b_549, c_549, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2679
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2680
        end;
    end);
    task.wait();
    pcall(function(a_550, b_550, c_550, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2681
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2682
        end;
    end);
    task.wait();
    pcall(function(a_551, b_551, c_551, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2683
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2684
        end;
    end);
    task.wait();
    pcall(function(a_552, b_552, c_552, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2685
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2686
        end;
    end);
    task.wait();
    pcall(function(a_553, b_553, c_553, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2687
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2688
        end;
    end);
    task.wait();
    pcall(function(a_554, b_554, c_554, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2689
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2690
        end;
    end);
    task.wait();
    pcall(function(a_555, b_555, c_555, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2691
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2692
        end;
    end);
    task.wait();
    pcall(function(a_556, b_556, c_556, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2693
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2694
        end;
    end);
    task.wait();
    pcall(function(a_557, b_557, c_557, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2695
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2696
        end;
    end);
    task.wait();
    pcall(function(a_558, b_558, c_558, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2697
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2698
        end;
    end);
    task.wait();
    pcall(function(a_559, b_559, c_559, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2699
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2700
        end;
    end);
    task.wait();
    pcall(function(a_560, b_560, c_560, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2701
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2702
        end;
    end);
    task.wait();
    pcall(function(a_561, b_561, c_561, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2703
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2704
        end;
    end);
    task.wait();
    pcall(function(a_562, b_562, c_562, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2705
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2706
        end;
    end);
    task.wait();
    pcall(function(a_563, b_563, c_563, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2707
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2708
        end;
    end);
    task.wait();
    pcall(function(a_564, b_564, c_564, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2709
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2710
        end;
    end);
    task.wait();
    pcall(function(a_565, b_565, c_565, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2711
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2712
        end;
    end);
    task.wait();
    pcall(function(a_566, b_566, c_566, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2713
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2714
        end;
    end);
    task.wait();
    pcall(function(a_567, b_567, c_567, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2715
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2716
        end;
    end);
    task.wait();
    pcall(function(a_568, b_568, c_568, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2717
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2718
        end;
    end);
    task.wait();
    pcall(function(a_569, b_569, c_569, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2719
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2720
        end;
    end);
    task.wait();
    pcall(function(a_570, b_570, c_570, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2721
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2722
        end;
    end);
    task.wait();
    pcall(function(a_571, b_571, c_571, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2723
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2724
        end;
    end);
    task.wait();
    pcall(function(a_572, b_572, c_572, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2725
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2726
        end;
    end);
    task.wait();
    pcall(function(a_573, b_573, c_573, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2727
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2728
        end;
    end);
    task.wait();
    pcall(function(a_574, b_574, c_574, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2729
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2730
        end;
    end);
    task.wait();
    pcall(function(a_575, b_575, c_575, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2731
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2732
        end;
    end);
    task.wait();
    pcall(function(a_576, b_576, c_576, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2733
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2734
        end;
    end);
    task.wait();
    pcall(function(a_577, b_577, c_577, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2735
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2736
        end;
    end);
    task.wait();
    pcall(function(a_578, b_578, c_578, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2737
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2738
        end;
    end);
    task.wait();
    pcall(function(a_579, b_579, c_579, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2739
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2740
        end;
    end);
    task.wait();
    pcall(function(a_580, b_580, c_580, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2741
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2742
        end;
    end);
    task.wait();
    pcall(function(a_581, b_581, c_581, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2743
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2744
        end;
    end);
    task.wait();
    pcall(function(a_582, b_582, c_582, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2745
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2746
        end;
    end);
    task.wait();
    pcall(function(a_583, b_583, c_583, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2747
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2748
        end;
    end);
    task.wait();
    pcall(function(a_584, b_584, c_584, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2749
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2750
        end;
    end);
    task.wait();
    pcall(function(a_585, b_585, c_585, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2751
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2752
        end;
    end);
    task.wait();
    pcall(function(a_586, b_586, c_586, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2753
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2754
        end;
    end);
    task.wait();
    pcall(function(a_587, b_587, c_587, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2755
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2756
        end;
    end);
    task.wait();
    pcall(function(a_588, b_588, c_588, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2757
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2758
        end;
    end);
    task.wait();
    pcall(function(a_589, b_589, c_589, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2759
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2760
        end;
    end);
    task.wait();
    pcall(function(a_590, b_590, c_590, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2761
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2762
        end;
    end);
    task.wait();
    pcall(function(a_591, b_591, c_591, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2763
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2764
        end;
    end);
    task.wait();
    pcall(function(a_592, b_592, c_592, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2765
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2766
        end;
    end);
    task.wait();
    pcall(function(a_593, b_593, c_593, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2767
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2768
        end;
    end);
    task.wait();
    pcall(function(a_594, b_594, c_594, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2769
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2770
        end;
    end);
    task.wait();
    pcall(function(a_595, b_595, c_595, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2771
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2772
        end;
    end);
    task.wait();
    pcall(function(a_596, b_596, c_596, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2773
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2774
        end;
    end);
    task.wait();
    pcall(function(a_597, b_597, c_597, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2775
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2776
        end;
    end);
    task.wait();
    pcall(function(a_598, b_598, c_598, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2777
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2778
        end;
    end);
    task.wait();
    pcall(function(a_599, b_599, c_599, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2779
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2780
        end;
    end);
    task.wait();
    pcall(function(a_600, b_600, c_600, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2781
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2782
        end;
    end);
    task.wait();
    pcall(function(a_601, b_601, c_601, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2783
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2784
        end;
    end);
    task.wait();
    pcall(function(a_602, b_602, c_602, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2785
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2786
        end;
    end);
    task.wait();
    pcall(function(a_603, b_603, c_603, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2787
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2788
        end;
    end);
    task.wait();
    pcall(function(a_604, b_604, c_604, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2789
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2790
        end;
    end);
    task.wait();
    pcall(function(a_605, b_605, c_605, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2791
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2792
        end;
    end);
    task.wait();
    pcall(function(a_606, b_606, c_606, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2793
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2794
        end;
    end);
    task.wait();
    pcall(function(a_607, b_607, c_607, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2795
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2796
        end;
    end);
    task.wait();
    pcall(function(a_608, b_608, c_608, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2797
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2798
        end;
    end);
    task.wait();
    pcall(function(a_609, b_609, c_609, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2799
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2800
        end;
    end);
    task.wait();
    pcall(function(a_610, b_610, c_610, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2801
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2802
        end;
    end);
    task.wait();
    pcall(function(a_611, b_611, c_611, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2803
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2804
        end;
    end);
    task.wait();
    pcall(function(a_612, b_612, c_612, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2805
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2806
        end;
    end);
    task.wait();
    pcall(function(a_613, b_613, c_613, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2807
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2808
        end;
    end);
    task.wait();
    pcall(function(a_614, b_614, c_614, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2809
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2810
        end;
    end);
    task.wait();
    pcall(function(a_615, b_615, c_615, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2811
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2812
        end;
    end);
    task.wait();
    pcall(function(a_616, b_616, c_616, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2813
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2814
        end;
    end);
    task.wait();
    pcall(function(a_617, b_617, c_617, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2815
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2816
        end;
    end);
    task.wait();
    pcall(function(a_618, b_618, c_618, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2817
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2818
        end;
    end);
    task.wait();
    pcall(function(a_619, b_619, c_619, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2819
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2820
        end;
    end);
    task.wait();
    pcall(function(a_620, b_620, c_620, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2821
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2822
        end;
    end);
    task.wait();
    pcall(function(a_621, b_621, c_621, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2823
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2824
        end;
    end);
    task.wait();
    pcall(function(a_622, b_622, c_622, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2825
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2826
        end;
    end);
    task.wait();
    pcall(function(a_623, b_623, c_623, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2827
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2828
        end;
    end);
    task.wait();
    pcall(function(a_624, b_624, c_624, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2829
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2830
        end;
    end);
    task.wait();
    pcall(function(a_625, b_625, c_625, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2831
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2832
        end;
    end);
    task.wait();
    pcall(function(a_626, b_626, c_626, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2833
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2834
        end;
    end);
    task.wait();
    pcall(function(a_627, b_627, c_627, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2835
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2836
        end;
    end);
    task.wait();
    pcall(function(a_628, b_628, c_628, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2837
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2838
        end;
    end);
    task.wait();
    pcall(function(a_629, b_629, c_629, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2839
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2840
        end;
    end);
    task.wait();
    pcall(function(a_630, b_630, c_630, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2841
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2842
        end;
    end);
    task.wait();
    pcall(function(a_631, b_631, c_631, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2843
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2844
        end;
    end);
    task.wait();
    pcall(function(a_632, b_632, c_632, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2845
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2846
        end;
    end);
    task.wait();
    pcall(function(a_633, b_633, c_633, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2847
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2848
        end;
    end);
    task.wait();
    pcall(function(a_634, b_634, c_634, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2849
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2850
        end;
    end);
    task.wait();
    pcall(function(a_635, b_635, c_635, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2851
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2852
        end;
    end);
    task.wait();
    pcall(function(a_636, b_636, c_636, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2853
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2854
        end;
    end);
    task.wait();
    pcall(function(a_637, b_637, c_637, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2855
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2856
        end;
    end);
    task.wait();
    pcall(function(a_638, b_638, c_638, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2857
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2858
        end;
    end);
    task.wait();
    pcall(function(a_639, b_639, c_639, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2859
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2860
        end;
    end);
    task.wait();
    pcall(function(a_640, b_640, c_640, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2861
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2862
        end;
    end);
    task.wait();
    pcall(function(a_641, b_641, c_641, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2863
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2864
        end;
    end);
    task.wait();
    pcall(function(a_642, b_642, c_642, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2865
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2866
        end;
    end);
    task.wait();
    pcall(function(a_643, b_643, c_643, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2867
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2868
        end;
    end);
    task.wait();
    pcall(function(a_644, b_644, c_644, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2869
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2870
        end;
    end);
    task.wait();
    pcall(function(a_645, b_645, c_645, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2871
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2872
        end;
    end);
    task.wait();
    pcall(function(a_646, b_646, c_646, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2873
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2874
        end;
    end);
    task.wait();
    pcall(function(a_647, b_647, c_647, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2875
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2876
        end;
    end);
    task.wait();
    pcall(function(a_648, b_648, c_648, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2877
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2878
        end;
    end);
    task.wait();
    pcall(function(a_649, b_649, c_649, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2879
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2880
        end;
    end);
    task.wait();
    pcall(function(a_650, b_650, c_650, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2881
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2882
        end;
    end);
    task.wait();
    pcall(function(a_651, b_651, c_651, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2883
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2884
        end;
    end);
    task.wait();
    pcall(function(a_652, b_652, c_652, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2885
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2886
        end;
    end);
    task.wait();
    pcall(function(a_653, b_653, c_653, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2887
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2888
        end;
    end);
    task.wait();
    pcall(function(a_654, b_654, c_654, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2889
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2890
        end;
    end);
    task.wait();
    pcall(function(a_655, b_655, c_655, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2891
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2892
        end;
    end);
    task.wait();
    pcall(function(a_656, b_656, c_656, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2893
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2894
        end;
    end);
    task.wait();
    pcall(function(a_657, b_657, c_657, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2895
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2896
        end;
    end);
    task.wait();
    pcall(function(a_658, b_658, c_658, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2897
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2898
        end;
    end);
    task.wait();
    pcall(function(a_659, b_659, c_659, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2899
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2900
        end;
    end);
    task.wait();
    pcall(function(a_660, b_660, c_660, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2901
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2902
        end;
    end);
    task.wait();
    pcall(function(a_661, b_661, c_661, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2903
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2904
        end;
    end);
    task.wait();
    pcall(function(a_662, b_662, c_662, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2905
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2906
        end;
    end);
    task.wait();
    pcall(function(a_663, b_663, c_663, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2907
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2908
        end;
    end);
    task.wait();
    pcall(function(a_664, b_664, c_664, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2909
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2910
        end;
    end);
    task.wait();
    pcall(function(a_665, b_665, c_665, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2911
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2912
        end;
    end);
    task.wait();
    pcall(function(a_666, b_666, c_666, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2913
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2914
        end;
    end);
    task.wait();
    pcall(function(a_667, b_667, c_667, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2915
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2916
        end;
    end);
    task.wait();
    pcall(function(a_668, b_668, c_668, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2917
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2918
        end;
    end);
    task.wait();
    pcall(function(a_669, b_669, c_669, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2919
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2920
        end;
    end);
    task.wait();
    pcall(function(a_670, b_670, c_670, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2921
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2922
        end;
    end);
    task.wait();
    pcall(function(a_671, b_671, c_671, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2923
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2924
        end;
    end);
    task.wait();
    pcall(function(a_672, b_672, c_672, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2925
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2926
        end;
    end);
    task.wait();
    pcall(function(a_673, b_673, c_673, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2927
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2928
        end;
    end);
    task.wait();
    pcall(function(a_674, b_674, c_674, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2929
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2930
        end;
    end);
    task.wait();
    pcall(function(a_675, b_675, c_675, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2931
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2932
        end;
    end);
    task.wait();
    pcall(function(a_676, b_676, c_676, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2933
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2934
        end;
    end);
    task.wait();
    pcall(function(a_677, b_677, c_677, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2935
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2936
        end;
    end);
    task.wait();
    pcall(function(a_678, b_678, c_678, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2937
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2938
        end;
    end);
    task.wait();
    pcall(function(a_679, b_679, c_679, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2939
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2940
        end;
    end);
    task.wait();
    pcall(function(a_680, b_680, c_680, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2941
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2942
        end;
    end);
    task.wait();
    pcall(function(a_681, b_681, c_681, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2943
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2944
        end;
    end);
    task.wait();
    pcall(function(a_682, b_682, c_682, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2945
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2946
        end;
    end);
    task.wait();
    pcall(function(a_683, b_683, c_683, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2947
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2948
        end;
    end);
    task.wait();
    pcall(function(a_684, b_684, c_684, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2949
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2950
        end;
    end);
    task.wait();
    pcall(function(a_685, b_685, c_685, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2951
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2952
        end;
    end);
    task.wait();
    pcall(function(a_686, b_686, c_686, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2953
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2954
        end;
    end);
    task.wait();
    pcall(function(a_687, b_687, c_687, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2955
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2956
        end;
    end);
    task.wait();
    pcall(function(a_688, b_688, c_688, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2957
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2958
        end;
    end);
    task.wait();
    pcall(function(a_689, b_689, c_689, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2959
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2960
        end;
    end);
    task.wait();
    pcall(function(a_690, b_690, c_690, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2961
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2962
        end;
    end);
    task.wait();
    pcall(function(a_691, b_691, c_691, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2963
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2964
        end;
    end);
    task.wait();
    pcall(function(a_692, b_692, c_692, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2965
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2966
        end;
    end);
    task.wait();
    pcall(function(a_693, b_693, c_693, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2967
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2968
        end;
    end);
    task.wait();
    pcall(function(a_694, b_694, c_694, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2969
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2970
        end;
    end);
    task.wait();
    pcall(function(a_695, b_695, c_695, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2971
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2972
        end;
    end);
    task.wait();
    pcall(function(a_696, b_696, c_696, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2973
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2974
        end;
    end);
    task.wait();
    pcall(function(a_697, b_697, c_697, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2975
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2976
        end;
    end);
    task.wait();
    pcall(function(a_698, b_698, c_698, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2977
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2978
        end;
    end);
    task.wait();
    pcall(function(a_699, b_699, c_699, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2979
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2980
        end;
    end);
    task.wait();
    pcall(function(a_700, b_700, c_700, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2981
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2982
        end;
    end);
    task.wait();
    pcall(function(a_701, b_701, c_701, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2983
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2984
        end;
    end);
    task.wait();
    pcall(function(a_702, b_702, c_702, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2985
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2986
        end;
    end);
    task.wait();
    pcall(function(a_703, b_703, c_703, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2987
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2988
        end;
    end);
    task.wait();
    pcall(function(a_704, b_704, c_704, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2989
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2990
        end;
    end);
    task.wait();
    pcall(function(a_705, b_705, c_705, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2991
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2992
        end;
    end);
    task.wait();
    pcall(function(a_706, b_706, c_706, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2993
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2994
        end;
    end);
    task.wait();
    pcall(function(a_707, b_707, c_707, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2995
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2996
        end;
    end);
    task.wait();
    pcall(function(a_708, b_708, c_708, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2997
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 2998
        end;
    end);
    task.wait();
    pcall(function(a_709, b_709, c_709, ...)
        if not HumanoidRootPart then -- didnt run, if id: 2999
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3000
        end;
    end);
    task.wait();
    pcall(function(a_710, b_710, c_710, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3001
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3002
        end;
    end);
    task.wait();
    pcall(function(a_711, b_711, c_711, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3003
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3004
        end;
    end);
    task.wait();
    pcall(function(a_712, b_712, c_712, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3005
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3006
        end;
    end);
    task.wait();
    pcall(function(a_713, b_713, c_713, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3007
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3008
        end;
    end);
    task.wait();
    pcall(function(a_714, b_714, c_714, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3009
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3010
        end;
    end);
    task.wait();
    pcall(function(a_715, b_715, c_715, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3011
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3012
        end;
    end);
    task.wait();
    pcall(function(a_716, b_716, c_716, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3013
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3014
        end;
    end);
    task.wait();
    pcall(function(a_717, b_717, c_717, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3015
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3016
        end;
    end);
    task.wait();
    pcall(function(a_718, b_718, c_718, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3017
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3018
        end;
    end);
    task.wait();
    pcall(function(a_719, b_719, c_719, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3019
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3020
        end;
    end);
    task.wait();
    pcall(function(a_720, b_720, c_720, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3021
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3022
        end;
    end);
    task.wait();
    pcall(function(a_721, b_721, c_721, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3023
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3024
        end;
    end);
    task.wait();
    pcall(function(a_722, b_722, c_722, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3025
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3026
        end;
    end);
    task.wait();
    pcall(function(a_723, b_723, c_723, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3027
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3028
        end;
    end);
    task.wait();
    pcall(function(a_724, b_724, c_724, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3029
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3030
        end;
    end);
    task.wait();
    pcall(function(a_725, b_725, c_725, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3031
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3032
        end;
    end);
    task.wait();
    pcall(function(a_726, b_726, c_726, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3033
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3034
        end;
    end);
    task.wait();
    pcall(function(a_727, b_727, c_727, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3035
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3036
        end;
    end);
    task.wait();
    pcall(function(a_728, b_728, c_728, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3037
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3038
        end;
    end);
    task.wait();
    pcall(function(a_729, b_729, c_729, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3039
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3040
        end;
    end);
    task.wait();
    pcall(function(a_730, b_730, c_730, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3041
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3042
        end;
    end);
    task.wait();
    pcall(function(a_731, b_731, c_731, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3043
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3044
        end;
    end);
    task.wait();
    pcall(function(a_732, b_732, c_732, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3045
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3046
        end;
    end);
    task.wait();
    pcall(function(a_733, b_733, c_733, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3047
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3048
        end;
    end);
    task.wait();
    pcall(function(a_734, b_734, c_734, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3049
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3050
        end;
    end);
    task.wait();
    pcall(function(a_735, b_735, c_735, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3051
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3052
        end;
    end);
    task.wait();
    pcall(function(a_736, b_736, c_736, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3053
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3054
        end;
    end);
    task.wait();
    pcall(function(a_737, b_737, c_737, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3055
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3056
        end;
    end);
    task.wait();
    pcall(function(a_738, b_738, c_738, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3057
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3058
        end;
    end);
    task.wait();
    pcall(function(a_739, b_739, c_739, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3059
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3060
        end;
    end);
    task.wait();
    pcall(function(a_740, b_740, c_740, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3061
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3062
        end;
    end);
    task.wait();
    pcall(function(a_741, b_741, c_741, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3063
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3064
        end;
    end);
    task.wait();
    pcall(function(a_742, b_742, c_742, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3065
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3066
        end;
    end);
    task.wait();
    pcall(function(a_743, b_743, c_743, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3067
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3068
        end;
    end);
    task.wait();
    pcall(function(a_744, b_744, c_744, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3069
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3070
        end;
    end);
    task.wait();
    pcall(function(a_745, b_745, c_745, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3071
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3072
        end;
    end);
    task.wait();
    pcall(function(a_746, b_746, c_746, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3073
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3074
        end;
    end);
    task.wait();
    pcall(function(a_747, b_747, c_747, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3075
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3076
        end;
    end);
    task.wait();
    pcall(function(a_748, b_748, c_748, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3077
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3078
        end;
    end);
    task.wait();
    pcall(function(a_749, b_749, c_749, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3079
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3080
        end;
    end);
    task.wait();
    pcall(function(a_750, b_750, c_750, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3081
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3082
        end;
    end);
    task.wait();
    pcall(function(a_751, b_751, c_751, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3083
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3084
        end;
    end);
    task.wait();
    pcall(function(a_752, b_752, c_752, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3085
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3086
        end;
    end);
    task.wait();
    pcall(function(a_753, b_753, c_753, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3087
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3088
        end;
    end);
    task.wait();
    pcall(function(a_754, b_754, c_754, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3089
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3090
        end;
    end);
    task.wait();
    pcall(function(a_755, b_755, c_755, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3091
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3092
        end;
    end);
    task.wait();
    pcall(function(a_756, b_756, c_756, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3093
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3094
        end;
    end);
    task.wait();
    pcall(function(a_757, b_757, c_757, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3095
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3096
        end;
    end);
    task.wait();
    pcall(function(a_758, b_758, c_758, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3097
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3098
        end;
    end);
    task.wait();
    pcall(function(a_759, b_759, c_759, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3099
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3100
        end;
    end);
    task.wait();
    pcall(function(a_760, b_760, c_760, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3101
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3102
        end;
    end);
    task.wait();
    pcall(function(a_761, b_761, c_761, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3103
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3104
        end;
    end);
    task.wait();
    pcall(function(a_762, b_762, c_762, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3105
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3106
        end;
    end);
    task.wait();
    pcall(function(a_763, b_763, c_763, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3107
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3108
        end;
    end);
    task.wait();
    pcall(function(a_764, b_764, c_764, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3109
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3110
        end;
    end);
    task.wait();
    pcall(function(a_765, b_765, c_765, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3111
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3112
        end;
    end);
    task.wait();
    pcall(function(a_766, b_766, c_766, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3113
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3114
        end;
    end);
    task.wait();
    pcall(function(a_767, b_767, c_767, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3115
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3116
        end;
    end);
    task.wait();
    pcall(function(a_768, b_768, c_768, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3117
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3118
        end;
    end);
    task.wait();
    pcall(function(a_769, b_769, c_769, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3119
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3120
        end;
    end);
    task.wait();
    pcall(function(a_770, b_770, c_770, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3121
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3122
        end;
    end);
    task.wait();
    pcall(function(a_771, b_771, c_771, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3123
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3124
        end;
    end);
    task.wait();
    pcall(function(a_772, b_772, c_772, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3125
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3126
        end;
    end);
    task.wait();
    pcall(function(a_773, b_773, c_773, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3127
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3128
        end;
    end);
    task.wait();
    pcall(function(a_774, b_774, c_774, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3129
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3130
        end;
    end);
    task.wait();
    pcall(function(a_775, b_775, c_775, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3131
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3132
        end;
    end);
    task.wait();
    pcall(function(a_776, b_776, c_776, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3133
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3134
        end;
    end);
    task.wait();
    pcall(function(a_777, b_777, c_777, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3135
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3136
        end;
    end);
    task.wait();
    pcall(function(a_778, b_778, c_778, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3137
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3138
        end;
    end);
    task.wait();
    pcall(function(a_779, b_779, c_779, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3139
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3140
        end;
    end);
    task.wait();
    pcall(function(a_780, b_780, c_780, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3141
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3142
        end;
    end);
    task.wait();
    pcall(function(a_781, b_781, c_781, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3143
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3144
        end;
    end);
    task.wait();
    pcall(function(a_782, b_782, c_782, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3145
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3146
        end;
    end);
    task.wait();
    pcall(function(a_783, b_783, c_783, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3147
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3148
        end;
    end);
    task.wait();
    pcall(function(a_784, b_784, c_784, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3149
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3150
        end;
    end);
    task.wait();
    pcall(function(a_785, b_785, c_785, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3151
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3152
        end;
    end);
    task.wait();
    pcall(function(a_786, b_786, c_786, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3153
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3154
        end;
    end);
    task.wait();
    pcall(function(a_787, b_787, c_787, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3155
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3156
        end;
    end);
    task.wait();
    pcall(function(a_788, b_788, c_788, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3157
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3158
        end;
    end);
    task.wait();
    pcall(function(a_789, b_789, c_789, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3159
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3160
        end;
    end);
    task.wait();
    pcall(function(a_790, b_790, c_790, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3161
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3162
        end;
    end);
    task.wait();
    pcall(function(a_791, b_791, c_791, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3163
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3164
        end;
    end);
    task.wait();
    pcall(function(a_792, b_792, c_792, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3165
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3166
        end;
    end);
    task.wait();
    pcall(function(a_793, b_793, c_793, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3167
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3168
        end;
    end);
    task.wait();
    pcall(function(a_794, b_794, c_794, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3169
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3170
        end;
    end);
    task.wait();
    pcall(function(a_795, b_795, c_795, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3171
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3172
        end;
    end);
    task.wait();
    pcall(function(a_796, b_796, c_796, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3173
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3174
        end;
    end);
    task.wait();
    pcall(function(a_797, b_797, c_797, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3175
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3176
        end;
    end);
    task.wait();
    pcall(function(a_798, b_798, c_798, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3177
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3178
        end;
    end);
    task.wait();
    pcall(function(a_799, b_799, c_799, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3179
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3180
        end;
    end);
    task.wait();
    pcall(function(a_800, b_800, c_800, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3181
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3182
        end;
    end);
    task.wait();
    pcall(function(a_801, b_801, c_801, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3183
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3184
        end;
    end);
    task.wait();
    pcall(function(a_802, b_802, c_802, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3185
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3186
        end;
    end);
    task.wait();
    pcall(function(a_803, b_803, c_803, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3187
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3188
        end;
    end);
    task.wait();
    pcall(function(a_804, b_804, c_804, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3189
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3190
        end;
    end);
    task.wait();
    pcall(function(a_805, b_805, c_805, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3191
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3192
        end;
    end);
    task.wait();
    pcall(function(a_806, b_806, c_806, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3193
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3194
        end;
    end);
    task.wait();
    pcall(function(a_807, b_807, c_807, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3195
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3196
        end;
    end);
    task.wait();
    pcall(function(a_808, b_808, c_808, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3197
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3198
        end;
    end);
    task.wait();
    pcall(function(a_809, b_809, c_809, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3199
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3200
        end;
    end);
    task.wait();
    pcall(function(a_810, b_810, c_810, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3201
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3202
        end;
    end);
    task.wait();
    pcall(function(a_811, b_811, c_811, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3203
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3204
        end;
    end);
    task.wait();
    pcall(function(a_812, b_812, c_812, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3205
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3206
        end;
    end);
    task.wait();
    pcall(function(a_813, b_813, c_813, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3207
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3208
        end;
    end);
    task.wait();
    pcall(function(a_814, b_814, c_814, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3209
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3210
        end;
    end);
    task.wait();
    pcall(function(a_815, b_815, c_815, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3211
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3212
        end;
    end);
    task.wait();
    pcall(function(a_816, b_816, c_816, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3213
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3214
        end;
    end);
    task.wait();
    pcall(function(a_817, b_817, c_817, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3215
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3216
        end;
    end);
    task.wait();
    pcall(function(a_818, b_818, c_818, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3217
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3218
        end;
    end);
    task.wait();
    pcall(function(a_819, b_819, c_819, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3219
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3220
        end;
    end);
    task.wait();
    pcall(function(a_820, b_820, c_820, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3221
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3222
        end;
    end);
    task.wait();
    pcall(function(a_821, b_821, c_821, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3223
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3224
        end;
    end);
    task.wait();
    pcall(function(a_822, b_822, c_822, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3225
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3226
        end;
    end);
    task.wait();
    pcall(function(a_823, b_823, c_823, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3227
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3228
        end;
    end);
    task.wait();
    pcall(function(a_824, b_824, c_824, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3229
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3230
        end;
    end);
    task.wait();
    pcall(function(a_825, b_825, c_825, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3231
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3232
        end;
    end);
    task.wait();
    pcall(function(a_826, b_826, c_826, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3233
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3234
        end;
    end);
    task.wait();
    pcall(function(a_827, b_827, c_827, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3235
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3236
        end;
    end);
    task.wait();
    pcall(function(a_828, b_828, c_828, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3237
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3238
        end;
    end);
    task.wait();
    pcall(function(a_829, b_829, c_829, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3239
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3240
        end;
    end);
    task.wait();
    pcall(function(a_830, b_830, c_830, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3241
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3242
        end;
    end);
    task.wait();
    pcall(function(a_831, b_831, c_831, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3243
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3244
        end;
    end);
    task.wait();
    pcall(function(a_832, b_832, c_832, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3245
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3246
        end;
    end);
    task.wait();
    pcall(function(a_833, b_833, c_833, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3247
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3248
        end;
    end);
    task.wait();
    pcall(function(a_834, b_834, c_834, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3249
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3250
        end;
    end);
    task.wait();
    pcall(function(a_835, b_835, c_835, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3251
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3252
        end;
    end);
    task.wait();
    pcall(function(a_836, b_836, c_836, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3253
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3254
        end;
    end);
    task.wait();
    pcall(function(a_837, b_837, c_837, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3255
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3256
        end;
    end);
    task.wait();
    pcall(function(a_838, b_838, c_838, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3257
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3258
        end;
    end);
    task.wait();
    pcall(function(a_839, b_839, c_839, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3259
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3260
        end;
    end);
    task.wait();
    pcall(function(a_840, b_840, c_840, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3261
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3262
        end;
    end);
    task.wait();
    pcall(function(a_841, b_841, c_841, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3263
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3264
        end;
    end);
    task.wait();
    pcall(function(a_842, b_842, c_842, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3265
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3266
        end;
    end);
    task.wait();
    pcall(function(a_843, b_843, c_843, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3267
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3268
        end;
    end);
    task.wait();
    pcall(function(a_844, b_844, c_844, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3269
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3270
        end;
    end);
    task.wait();
    pcall(function(a_845, b_845, c_845, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3271
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3272
        end;
    end);
    task.wait();
    pcall(function(a_846, b_846, c_846, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3273
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3274
        end;
    end);
    task.wait();
    pcall(function(a_847, b_847, c_847, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3275
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3276
        end;
    end);
    task.wait();
    pcall(function(a_848, b_848, c_848, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3277
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3278
        end;
    end);
    task.wait();
    pcall(function(a_849, b_849, c_849, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3279
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3280
        end;
    end);
    task.wait();
    pcall(function(a_850, b_850, c_850, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3281
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3282
        end;
    end);
    task.wait();
    pcall(function(a_851, b_851, c_851, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3283
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3284
        end;
    end);
    task.wait();
    pcall(function(a_852, b_852, c_852, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3285
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3286
        end;
    end);
    task.wait();
    pcall(function(a_853, b_853, c_853, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3287
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3288
        end;
    end);
    task.wait();
    pcall(function(a_854, b_854, c_854, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3289
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3290
        end;
    end);
    task.wait();
    pcall(function(a_855, b_855, c_855, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3291
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3292
        end;
    end);
    task.wait();
    pcall(function(a_856, b_856, c_856, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3293
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3294
        end;
    end);
    task.wait();
    pcall(function(a_857, b_857, c_857, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3295
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3296
        end;
    end);
    task.wait();
    pcall(function(a_858, b_858, c_858, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3297
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3298
        end;
    end);
    task.wait();
    pcall(function(a_859, b_859, c_859, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3299
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3300
        end;
    end);
    task.wait();
    pcall(function(a_860, b_860, c_860, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3301
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3302
        end;
    end);
    task.wait();
    pcall(function(a_861, b_861, c_861, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3303
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3304
        end;
    end);
    task.wait();
    pcall(function(a_862, b_862, c_862, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3305
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3306
        end;
    end);
    task.wait();
    pcall(function(a_863, b_863, c_863, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3307
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3308
        end;
    end);
    task.wait();
    pcall(function(a_864, b_864, c_864, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3309
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3310
        end;
    end);
    task.wait();
    pcall(function(a_865, b_865, c_865, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3311
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3312
        end;
    end);
    task.wait();
    pcall(function(a_866, b_866, c_866, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3313
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3314
        end;
    end);
    task.wait();
    pcall(function(a_867, b_867, c_867, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3315
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3316
        end;
    end);
    task.wait();
    pcall(function(a_868, b_868, c_868, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3317
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3318
        end;
    end);
    task.wait();
    pcall(function(a_869, b_869, c_869, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3319
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3320
        end;
    end);
    task.wait();
    pcall(function(a_870, b_870, c_870, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3321
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3322
        end;
    end);
    task.wait();
    pcall(function(a_871, b_871, c_871, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3323
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3324
        end;
    end);
    task.wait();
    pcall(function(a_872, b_872, c_872, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3325
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3326
        end;
    end);
    task.wait();
    pcall(function(a_873, b_873, c_873, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3327
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3328
        end;
    end);
    task.wait();
    pcall(function(a_874, b_874, c_874, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3329
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3330
        end;
    end);
    task.wait();
    pcall(function(a_875, b_875, c_875, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3331
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3332
        end;
    end);
    task.wait();
    pcall(function(a_876, b_876, c_876, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3333
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3334
        end;
    end);
    task.wait();
    pcall(function(a_877, b_877, c_877, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3335
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3336
        end;
    end);
    task.wait();
    pcall(function(a_878, b_878, c_878, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3337
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3338
        end;
    end);
    task.wait();
    pcall(function(a_879, b_879, c_879, ...)
        if not HumanoidRootPart then -- didnt run, if id: 3339
        end;
        if HumanoidRootPart:FindFirstChild("BodyClip") then -- ran, if id: 3340
        end;
    end);
end);
task.spawn(function()
    Stepped = RunService.Stepped;
    Stepped:Connect(function(a_1486, b_1486, c_1486, ...)
        pcall(function(a_1487, b_1487, c_1487, ...)
            if not a_3 then -- didnt run, if id: 4783
            end;
            for for_key_1107, for_val_1107 in pairs(a_3:GetDescendants()) do
                if (for_key_1107 ~= nil) then -- ran, if id: 4784

                end;
                if not for_val_1107:IsA("BasePart") then -- didnt run, if id: 4785
                end;
                for_val_1107.CanCollide = false;
            end;
        end);
    end);
end);
task.spawn(function()
    task.wait(0.5);
    if not _G.AutoCollectBrainrot then -- ran, if id: 362
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 363
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 364
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 365
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 366
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 367
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 368
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 369
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 370
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 371
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 372
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 373
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 374
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 375
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 376
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 377
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 378
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 379
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 380
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 381
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 382
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 383
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 384
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 385
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 386
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 387
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 388
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 389
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 390
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 391
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 392
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 393
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 394
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 395
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 396
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 397
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 398
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 399
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 400
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 401
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 402
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 403
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 404
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 405
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 406
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 407
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 408
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 409
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 410
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 411
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 412
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 413
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 414
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 415
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 416
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 417
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 418
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 419
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 420
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 421
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 422
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 423
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 424
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 425
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 426
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 427
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 428
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 429
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 430
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 431
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 432
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 433
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 434
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 435
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 436
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 437
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 438
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 439
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 440
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 441
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 442
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 443
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 444
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 445
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 446
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 447
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 448
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 449
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 450
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 451
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 452
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 453
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 454
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 455
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 456
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 457
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 458
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 459
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 460
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 461
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 462
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 463
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 464
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 465
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 466
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 467
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 468
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 469
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 470
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 471
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 472
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 473
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 474
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 475
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 476
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 477
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 478
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 479
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 480
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 481
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 482
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 483
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 484
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 485
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 486
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 487
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 488
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 489
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 490
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 491
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 492
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 493
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 494
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 495
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 496
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 497
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 498
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 499
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 500
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 501
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 502
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 503
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 504
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 505
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 506
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 507
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 508
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 509
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 510
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 511
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 512
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 513
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 514
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 515
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 516
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 517
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 518
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 519
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 520
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 521
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 522
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 523
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 524
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 525
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 526
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 527
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 528
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 529
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 530
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 531
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 532
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 533
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 534
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 535
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 536
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 537
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 538
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 539
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 540
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 541
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 542
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 543
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 544
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 545
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 546
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 547
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 548
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 549
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 550
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 551
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 552
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 553
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 554
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 555
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 556
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 557
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 558
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 559
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 560
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 561
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 562
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 563
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 564
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 565
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 566
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 567
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 568
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 569
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 570
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 571
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 572
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 573
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 574
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 575
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 576
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 577
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 578
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 579
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 580
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 581
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 582
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 583
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 584
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 585
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 586
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 587
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 588
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 589
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 590
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 591
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 592
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 593
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 594
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 595
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 596
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 597
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 598
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 599
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 600
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 601
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 602
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 603
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 604
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 605
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 606
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 607
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 608
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 609
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 610
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 611
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 612
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 613
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 614
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 615
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 616
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 617
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 618
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 619
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 620
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 621
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 622
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 623
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 624
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 625
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 626
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 627
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 628
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 629
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 630
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 631
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 632
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 633
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 634
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 635
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 636
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 637
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 638
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 639
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 640
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 641
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 642
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 643
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 644
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 645
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 646
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 647
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 648
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 649
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 650
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 651
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 652
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 653
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 654
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 655
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 656
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 657
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 658
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 659
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 660
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 661
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 662
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 663
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 664
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 665
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 666
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 667
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 668
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 669
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 670
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 671
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 672
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 673
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 674
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 675
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 676
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 677
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 678
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 679
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 680
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 681
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 682
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 683
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 684
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 685
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 686
        task.wait(0.5);
    end;
    if not _G.AutoCollectBrainrot then -- ran, if id: 687
    end;
end);
task.spawn(function()
    task.wait(2);
    if not _G.AutoUpgradeBase then -- ran, if id: 27
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 28
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 29
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 30
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 31
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 32
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 33
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 34
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 35
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 36
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 37
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 38
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 39
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 40
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 41
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 42
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 43
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 44
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 45
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 46
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 47
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 48
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 49
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 50
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 51
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 52
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 53
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 54
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 55
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 56
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 57
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 58
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 59
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 60
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 61
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 62
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 63
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 64
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 65
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 66
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 67
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 68
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 69
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 70
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 71
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 72
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 73
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 74
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 75
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 76
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 77
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 78
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 79
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 80
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 81
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 82
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 83
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 84
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 85
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 86
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 87
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 88
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 89
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 90
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 91
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 92
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 93
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 94
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 95
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 96
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 97
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 98
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 99
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 100
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 101
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 102
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 103
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 104
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 105
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 106
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 107
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 108
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 109
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 110
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 111
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 112
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 113
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 114
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 115
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 116
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 117
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 118
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 119
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 120
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 121
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 122
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 123
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 124
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 125
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 126
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 127
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 128
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 129
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 130
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 131
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 132
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 133
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 134
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 135
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 136
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 137
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 138
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 139
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 140
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 141
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 142
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 143
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 144
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 145
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 146
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 147
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 148
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 149
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 150
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 151
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 152
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 153
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 154
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 155
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 156
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 157
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 158
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 159
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 160
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 161
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 162
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 163
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 164
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 165
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 166
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 167
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 168
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 169
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 170
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 171
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 172
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 173
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 174
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 175
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 176
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 177
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 178
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 179
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 180
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 181
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 182
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 183
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 184
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 185
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 186
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 187
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 188
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 189
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 190
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 191
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 192
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 193
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 194
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 195
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 196
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 197
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 198
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 199
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 200
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 201
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 202
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 203
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 204
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 205
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 206
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 207
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 208
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 209
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 210
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 211
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 212
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 213
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 214
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 215
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 216
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 217
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 218
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 219
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 220
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 221
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 222
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 223
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 224
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 225
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 226
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 227
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 228
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 229
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 230
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 231
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 232
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 233
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 234
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 235
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 236
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 237
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 238
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 239
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 240
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 241
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 242
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 243
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 244
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 245
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 246
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 247
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 248
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 249
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 250
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 251
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 252
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 253
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 254
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 255
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 256
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 257
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 258
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 259
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 260
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 261
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 262
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 263
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 264
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 265
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 266
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 267
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 268
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 269
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 270
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 271
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 272
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 273
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 274
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 275
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 276
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 277
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 278
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 279
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 280
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 281
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 282
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 283
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 284
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 285
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 286
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 287
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 288
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 289
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 290
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 291
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 292
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 293
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 294
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 295
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 296
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 297
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 298
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 299
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 300
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 301
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 302
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 303
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 304
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 305
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 306
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 307
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 308
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 309
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 310
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 311
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 312
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 313
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 314
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 315
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 316
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 317
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 318
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 319
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 320
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 321
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 322
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 323
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 324
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 325
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 326
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 327
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 328
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 329
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 330
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 331
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 332
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 333
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 334
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 335
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 336
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 337
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 338
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 339
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 340
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 341
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 342
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 343
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 344
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 345
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 346
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 347
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 348
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 349
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 350
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 351
        task.wait(2);
    end;
    if not _G.AutoUpgradeBase then -- ran, if id: 352
    end;
end);
task.spawn(function()
    task.wait(2);
    if not _G.AutoUpgradeSpeed then -- ran, if id: 714
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 715
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 716
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 717
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 718
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 719
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 720
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 721
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 722
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 723
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 724
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 725
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 726
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 727
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 728
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 729
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 730
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 731
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 732
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 733
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 734
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 735
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 736
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 737
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 738
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 739
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 740
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 741
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 742
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 743
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 744
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 745
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 746
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 747
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 748
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 749
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 750
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 751
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 752
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 753
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 754
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 755
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 756
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 757
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 758
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 759
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 760
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 761
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 762
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 763
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 764
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 765
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 766
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 767
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 768
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 769
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 770
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 771
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 772
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 773
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 774
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 775
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 776
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 777
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 778
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 779
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 780
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 781
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 782
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 783
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 784
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 785
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 786
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 787
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 788
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 789
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 790
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 791
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 792
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 793
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 794
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 795
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 796
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 797
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 798
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 799
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 800
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 801
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 802
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 803
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 804
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 805
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 806
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 807
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 808
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 809
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 810
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 811
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 812
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 813
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 814
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 815
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 816
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 817
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 818
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 819
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 820
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 821
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 822
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 823
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 824
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 825
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 826
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 827
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 828
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 829
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 830
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 831
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 832
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 833
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 834
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 835
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 836
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 837
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 838
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 839
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 840
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 841
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 842
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 843
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 844
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 845
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 846
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 847
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 848
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 849
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 850
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 851
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 852
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 853
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 854
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 855
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 856
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 857
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 858
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 859
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 860
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 861
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 862
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 863
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 864
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 865
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 866
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 867
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 868
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 869
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 870
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 871
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 872
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 873
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 874
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 875
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 876
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 877
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 878
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 879
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 880
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 881
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 882
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 883
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 884
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 885
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 886
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 887
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 888
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 889
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 890
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 891
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 892
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 893
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 894
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 895
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 896
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 897
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 898
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 899
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 900
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 901
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 902
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 903
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 904
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 905
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 906
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 907
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 908
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 909
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 910
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 911
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 912
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 913
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 914
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 915
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 916
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 917
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 918
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 919
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 920
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 921
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 922
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 923
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 924
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 925
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 926
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 927
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 928
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 929
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 930
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 931
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 932
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 933
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 934
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 935
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 936
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 937
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 938
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 939
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 940
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 941
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 942
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 943
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 944
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 945
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 946
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 947
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 948
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 949
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 950
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 951
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 952
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 953
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 954
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 955
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 956
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 957
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 958
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 959
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 960
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 961
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 962
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 963
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 964
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 965
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 966
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 967
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 968
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 969
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 970
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 971
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 972
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 973
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 974
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 975
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 976
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 977
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 978
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 979
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 980
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 981
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 982
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 983
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 984
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 985
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 986
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 987
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 988
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 989
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 990
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 991
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 992
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 993
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 994
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 995
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 996
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 997
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 998
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 999
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1000
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1001
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1002
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1003
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1004
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1005
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1006
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1007
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1008
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1009
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1010
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1011
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1012
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1013
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1014
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1015
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1016
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1017
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1018
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1019
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1020
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1021
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1022
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1023
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1024
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1025
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1026
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1027
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1028
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1029
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1030
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1031
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1032
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1033
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1034
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1035
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1036
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1037
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1038
        task.wait(2);
    end;
    if not _G.AutoUpgradeSpeed then -- ran, if id: 1039
    end;
end);
task.spawn(function()
    task.wait(2);
    if not _G.AutoUpgradeCarry then -- ran, if id: 1377
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1378
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1379
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1380
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1381
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1382
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1383
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1384
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1385
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1386
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1387
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1388
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1389
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1390
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1391
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1392
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1393
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1394
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1395
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1396
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1397
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1398
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1399
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1400
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1401
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1402
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1403
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1404
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1405
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1406
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1407
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1408
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1409
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1410
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1411
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1412
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1413
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1414
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1415
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1416
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1417
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1418
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1419
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1420
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1421
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1422
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1423
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1424
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1425
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1426
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1427
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1428
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1429
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1430
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1431
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1432
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1433
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1434
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1435
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1436
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1437
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1438
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1439
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1440
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1441
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1442
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1443
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1444
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1445
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1446
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1447
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1448
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1449
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1450
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1451
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1452
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1453
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1454
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1455
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1456
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1457
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1458
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1459
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1460
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1461
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1462
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1463
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1464
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1465
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1466
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1467
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1468
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1469
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1470
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1471
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1472
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1473
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1474
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1475
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1476
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1477
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1478
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1479
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1480
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1481
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1482
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1483
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1484
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1485
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1486
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1487
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1488
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1489
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1490
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1491
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1492
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1493
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1494
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1495
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1496
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1497
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1498
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1499
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1500
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1501
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1502
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1503
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1504
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1505
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1506
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1507
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1508
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1509
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1510
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1511
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1512
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1513
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1514
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1515
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1516
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1517
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1518
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1519
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1520
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1521
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1522
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1523
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1524
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1525
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1526
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1527
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1528
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1529
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1530
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1531
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1532
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1533
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1534
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1535
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1536
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1537
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1538
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1539
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1540
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1541
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1542
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1543
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1544
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1545
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1546
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1547
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1548
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1549
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1550
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1551
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1552
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1553
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1554
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1555
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1556
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1557
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1558
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1559
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1560
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1561
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1562
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1563
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1564
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1565
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1566
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1567
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1568
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1569
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1570
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1571
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1572
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1573
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1574
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1575
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1576
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1577
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1578
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1579
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1580
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1581
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1582
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1583
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1584
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1585
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1586
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1587
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1588
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1589
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1590
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1591
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1592
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1593
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1594
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1595
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1596
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1597
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1598
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1599
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1600
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1601
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1602
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1603
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1604
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1605
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1606
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1607
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1608
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1609
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1610
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1611
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1612
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1613
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1614
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1615
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1616
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1617
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1618
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1619
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1620
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1621
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1622
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1623
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1624
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1625
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1626
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1627
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1628
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1629
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1630
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1631
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1632
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1633
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1634
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1635
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1636
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1637
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1638
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1639
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1640
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1641
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1642
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1643
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1644
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1645
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1646
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1647
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1648
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1649
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1650
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1651
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1652
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1653
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1654
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1655
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1656
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1657
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1658
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1659
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1660
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1661
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1662
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1663
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1664
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1665
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1666
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1667
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1668
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1669
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1670
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1671
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1672
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1673
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1674
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1675
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1676
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1677
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1678
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1679
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1680
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1681
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1682
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1683
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1684
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1685
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1686
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1687
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1688
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1689
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1690
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1691
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1692
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1693
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1694
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1695
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1696
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1697
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1698
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1699
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1700
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1701
        task.wait(2);
    end;
    if not _G.AutoUpgradeCarry then -- ran, if id: 1702
    end;
end);
task.spawn(function()
    task.wait(0.3);
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1041
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1042
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1043
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1044
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1045
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1046
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1047
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1048
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1049
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1050
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1051
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1052
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1053
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1054
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1055
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1056
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1057
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1058
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1059
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1060
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1061
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1062
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1063
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1064
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1065
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1066
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1067
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1068
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1069
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1070
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1071
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1072
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1073
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1074
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1075
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1076
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1077
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1078
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1079
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1080
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1081
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1082
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1083
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1084
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1085
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1086
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1087
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1088
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1089
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1090
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1091
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1092
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1093
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1094
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1095
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1096
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1097
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1098
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1099
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1100
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1101
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1102
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1103
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1104
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1105
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1106
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1107
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1108
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1109
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1110
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1111
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1112
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1113
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1114
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1115
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1116
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1117
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1118
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1119
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1120
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1121
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1122
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1123
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1124
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1125
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1126
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1127
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1128
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1129
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1130
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1131
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1132
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1133
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1134
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1135
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1136
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1137
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1138
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1139
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1140
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1141
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1142
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1143
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1144
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1145
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1146
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1147
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1148
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1149
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1150
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1151
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1152
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1153
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1154
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1155
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1156
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1157
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1158
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1159
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1160
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1161
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1162
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1163
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1164
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1165
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1166
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1167
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1168
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1169
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1170
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1171
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1172
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1173
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1174
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1175
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1176
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1177
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1178
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1179
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1180
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1181
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1182
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1183
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1184
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1185
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1186
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1187
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1188
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1189
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1190
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1191
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1192
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1193
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1194
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1195
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1196
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1197
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1198
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1199
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1200
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1201
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1202
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1203
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1204
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1205
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1206
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1207
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1208
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1209
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1210
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1211
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1212
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1213
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1214
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1215
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1216
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1217
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1218
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1219
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1220
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1221
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1222
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1223
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1224
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1225
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1226
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1227
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1228
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1229
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1230
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1231
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1232
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1233
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1234
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1235
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1236
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1237
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1238
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1239
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1240
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1241
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1242
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1243
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1244
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1245
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1246
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1247
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1248
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1249
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1250
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1251
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1252
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1253
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1254
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1255
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1256
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1257
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1258
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1259
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1260
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1261
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1262
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1263
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1264
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1265
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1266
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1267
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1268
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1269
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1270
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1271
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1272
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1273
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1274
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1275
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1276
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1277
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1278
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1279
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1280
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1281
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1282
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1283
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1284
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1285
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1286
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1287
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1288
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1289
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1290
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1291
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1292
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1293
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1294
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1295
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1296
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1297
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1298
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1299
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1300
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1301
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1302
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1303
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1304
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1305
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1306
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1307
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1308
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1309
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1310
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1311
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1312
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1313
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1314
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1315
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1316
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1317
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1318
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1319
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1320
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1321
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1322
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1323
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1324
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1325
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1326
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1327
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1328
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1329
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1330
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1331
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1332
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1333
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1334
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1335
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1336
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1337
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1338
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1339
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1340
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1341
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1342
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1343
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1344
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1345
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1346
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1347
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1348
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1349
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1350
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1351
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1352
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1353
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1354
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1355
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1356
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1357
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1358
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1359
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1360
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1361
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1362
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1363
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1364
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1365
        task.wait(0.3);
    end;
    if not _G.AutoCollectUFOCoins then -- ran, if id: 1366
    end;
end);
task.spawn(function()
    task.wait(0.3);
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3341
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3342
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3343
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3344
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3345
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3346
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3347
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3348
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3349
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3350
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3351
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3352
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3353
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3354
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3355
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3356
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3357
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3358
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3359
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3360
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3361
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3362
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3363
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3364
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3365
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3366
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3367
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3368
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3369
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3370
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3371
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3372
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3373
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3374
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3375
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3376
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3377
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3378
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3379
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3380
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3381
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3382
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3383
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3384
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3385
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3386
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3387
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3388
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3389
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3390
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3391
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3392
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3393
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3394
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3395
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3396
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3397
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3398
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3399
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3400
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3401
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3402
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3403
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3404
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3405
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3406
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3407
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3408
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3409
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3410
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3411
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3412
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3413
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3414
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3415
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3416
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3417
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3418
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3419
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3420
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3421
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3422
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3423
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3424
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3425
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3426
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3427
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3428
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3429
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3430
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3431
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3432
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3433
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3434
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3435
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3436
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3437
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3438
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3439
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3440
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3441
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3442
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3443
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3444
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3445
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3446
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3447
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3448
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3449
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3450
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3451
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3452
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3453
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3454
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3455
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3456
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3457
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3458
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3459
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3460
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3461
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3462
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3463
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3464
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3465
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3466
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3467
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3468
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3469
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3470
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3471
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3472
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3473
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3474
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3475
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3476
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3477
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3478
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3479
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3480
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3481
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3482
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3483
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3484
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3485
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3486
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3487
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3488
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3489
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3490
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3491
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3492
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3493
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3494
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3495
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3496
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3497
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3498
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3499
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3500
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3501
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3502
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3503
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3504
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3505
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3506
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3507
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3508
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3509
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3510
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3511
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3512
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3513
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3514
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3515
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3516
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3517
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3518
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3519
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3520
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3521
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3522
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3523
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3524
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3525
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3526
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3527
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3528
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3529
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3530
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3531
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3532
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3533
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3534
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3535
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3536
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3537
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3538
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3539
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3540
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3541
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3542
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3543
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3544
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3545
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3546
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3547
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3548
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3549
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3550
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3551
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3552
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3553
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3554
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3555
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3556
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3557
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3558
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3559
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3560
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3561
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3562
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3563
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3564
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3565
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3566
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3567
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3568
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3569
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3570
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3571
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3572
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3573
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3574
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3575
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3576
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3577
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3578
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3579
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3580
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3581
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3582
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3583
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3584
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3585
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3586
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3587
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3588
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3589
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3590
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3591
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3592
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3593
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3594
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3595
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3596
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3597
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3598
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3599
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3600
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3601
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3602
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3603
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3604
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3605
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3606
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3607
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3608
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3609
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3610
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3611
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3612
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3613
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3614
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3615
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3616
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3617
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3618
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3619
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3620
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3621
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3622
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3623
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3624
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3625
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3626
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3627
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3628
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3629
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3630
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3631
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3632
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3633
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3634
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3635
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3636
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3637
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3638
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3639
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3640
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3641
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3642
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3643
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3644
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3645
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3646
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3647
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3648
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3649
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3650
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3651
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3652
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3653
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3654
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3655
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3656
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3657
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3658
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3659
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3660
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3661
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3662
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3663
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3664
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3665
        task.wait(0.3);
    end;
    if not _G.AutoCollectRadioactiveCoins then -- ran, if id: 3666
    end;
end);
task.spawn(function()
    task.wait(0.5);
    pcall(function(a_940, b_940, c_940, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3685

        end;
        for for_key_17, for_val_17 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_17 ~= nil) then -- ran, if id: 3686
                Array_7 = {};
                table.insert(Array_7, for_val_17);
            end;
        end;
        for for_key_18, for_val_18 in ipairs(Array_7) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_941, b_941, c_941, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3687

        end;
        for for_key_19, for_val_19 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_19 ~= nil) then -- ran, if id: 3688
                Array_8 = {};
                table.insert(Array_8, for_val_19);
            end;
        end;
        for for_key_20, for_val_20 in ipairs(Array_8) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_942, b_942, c_942, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3689

        end;
        for for_key_21, for_val_21 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_21 ~= nil) then -- ran, if id: 3690
                Array_9 = {};
                table.insert(Array_9, for_val_21);
            end;
        end;
        for for_key_22, for_val_22 in ipairs(Array_9) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_943, b_943, c_943, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3691

        end;
        for for_key_23, for_val_23 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_23 ~= nil) then -- ran, if id: 3692
                Array_10 = {};
                table.insert(Array_10, for_val_23);
            end;
        end;
        for for_key_24, for_val_24 in ipairs(Array_10) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_944, b_944, c_944, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3693

        end;
        for for_key_25, for_val_25 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_25 ~= nil) then -- ran, if id: 3694
                Array_11 = {};
                table.insert(Array_11, for_val_25);
            end;
        end;
        for for_key_26, for_val_26 in ipairs(Array_11) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_945, b_945, c_945, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3695

        end;
        for for_key_27, for_val_27 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_27 ~= nil) then -- ran, if id: 3696
                Array_12 = {};
                table.insert(Array_12, for_val_27);
            end;
        end;
        for for_key_28, for_val_28 in ipairs(Array_12) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_946, b_946, c_946, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3697

        end;
        for for_key_29, for_val_29 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_29 ~= nil) then -- ran, if id: 3698
                Array_13 = {};
                table.insert(Array_13, for_val_29);
            end;
        end;
        for for_key_30, for_val_30 in ipairs(Array_13) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_947, b_947, c_947, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3699

        end;
        for for_key_31, for_val_31 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_31 ~= nil) then -- ran, if id: 3700
                Array_14 = {};
                table.insert(Array_14, for_val_31);
            end;
        end;
        for for_key_32, for_val_32 in ipairs(Array_14) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_948, b_948, c_948, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3701

        end;
        for for_key_33, for_val_33 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_33 ~= nil) then -- ran, if id: 3702
                Array_15 = {};
                table.insert(Array_15, for_val_33);
            end;
        end;
        for for_key_34, for_val_34 in ipairs(Array_15) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_949, b_949, c_949, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3703

        end;
        for for_key_35, for_val_35 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_35 ~= nil) then -- ran, if id: 3704
                Array_16 = {};
                table.insert(Array_16, for_val_35);
            end;
        end;
        for for_key_36, for_val_36 in ipairs(Array_16) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_950, b_950, c_950, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3705

        end;
        for for_key_37, for_val_37 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_37 ~= nil) then -- ran, if id: 3706
                Array_17 = {};
                table.insert(Array_17, for_val_37);
            end;
        end;
        for for_key_38, for_val_38 in ipairs(Array_17) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_951, b_951, c_951, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3707

        end;
        for for_key_39, for_val_39 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_39 ~= nil) then -- ran, if id: 3708
                Array_18 = {};
                table.insert(Array_18, for_val_39);
            end;
        end;
        for for_key_40, for_val_40 in ipairs(Array_18) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_952, b_952, c_952, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3709

        end;
        for for_key_41, for_val_41 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_41 ~= nil) then -- ran, if id: 3710
                Array_19 = {};
                table.insert(Array_19, for_val_41);
            end;
        end;
        for for_key_42, for_val_42 in ipairs(Array_19) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_953, b_953, c_953, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3711

        end;
        for for_key_43, for_val_43 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_43 ~= nil) then -- ran, if id: 3712
                Array_20 = {};
                table.insert(Array_20, for_val_43);
            end;
        end;
        for for_key_44, for_val_44 in ipairs(Array_20) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_954, b_954, c_954, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3713

        end;
        for for_key_45, for_val_45 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_45 ~= nil) then -- ran, if id: 3714
                Array_21 = {};
                table.insert(Array_21, for_val_45);
            end;
        end;
        for for_key_46, for_val_46 in ipairs(Array_21) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_955, b_955, c_955, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3715

        end;
        for for_key_47, for_val_47 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_47 ~= nil) then -- ran, if id: 3716
                Array_22 = {};
                table.insert(Array_22, for_val_47);
            end;
        end;
        for for_key_48, for_val_48 in ipairs(Array_22) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_956, b_956, c_956, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3717

        end;
        for for_key_49, for_val_49 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_49 ~= nil) then -- ran, if id: 3718
                Array_23 = {};
                table.insert(Array_23, for_val_49);
            end;
        end;
        for for_key_50, for_val_50 in ipairs(Array_23) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_957, b_957, c_957, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3719

        end;
        for for_key_51, for_val_51 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_51 ~= nil) then -- ran, if id: 3720
                Array_24 = {};
                table.insert(Array_24, for_val_51);
            end;
        end;
        for for_key_52, for_val_52 in ipairs(Array_24) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_958, b_958, c_958, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3721

        end;
        for for_key_53, for_val_53 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_53 ~= nil) then -- ran, if id: 3722
                Array_25 = {};
                table.insert(Array_25, for_val_53);
            end;
        end;
        for for_key_54, for_val_54 in ipairs(Array_25) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_959, b_959, c_959, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3723

        end;
        for for_key_55, for_val_55 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_55 ~= nil) then -- ran, if id: 3724
                Array_26 = {};
                table.insert(Array_26, for_val_55);
            end;
        end;
        for for_key_56, for_val_56 in ipairs(Array_26) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_960, b_960, c_960, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3725

        end;
        for for_key_57, for_val_57 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_57 ~= nil) then -- ran, if id: 3726
                Array_27 = {};
                table.insert(Array_27, for_val_57);
            end;
        end;
        for for_key_58, for_val_58 in ipairs(Array_27) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_961, b_961, c_961, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3727

        end;
        for for_key_59, for_val_59 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_59 ~= nil) then -- ran, if id: 3728
                Array_28 = {};
                table.insert(Array_28, for_val_59);
            end;
        end;
        for for_key_60, for_val_60 in ipairs(Array_28) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_962, b_962, c_962, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3729

        end;
        for for_key_61, for_val_61 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_61 ~= nil) then -- ran, if id: 3730
                Array_29 = {};
                table.insert(Array_29, for_val_61);
            end;
        end;
        for for_key_62, for_val_62 in ipairs(Array_29) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_963, b_963, c_963, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3731

        end;
        for for_key_63, for_val_63 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_63 ~= nil) then -- ran, if id: 3732
                Array_30 = {};
                table.insert(Array_30, for_val_63);
            end;
        end;
        for for_key_64, for_val_64 in ipairs(Array_30) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_964, b_964, c_964, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3733

        end;
        for for_key_65, for_val_65 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_65 ~= nil) then -- ran, if id: 3734
                Array_31 = {};
                table.insert(Array_31, for_val_65);
            end;
        end;
        for for_key_66, for_val_66 in ipairs(Array_31) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_965, b_965, c_965, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3735

        end;
        for for_key_67, for_val_67 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_67 ~= nil) then -- ran, if id: 3736
                Array_32 = {};
                table.insert(Array_32, for_val_67);
            end;
        end;
        for for_key_68, for_val_68 in ipairs(Array_32) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_966, b_966, c_966, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3737

        end;
        for for_key_69, for_val_69 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_69 ~= nil) then -- ran, if id: 3738
                Array_33 = {};
                table.insert(Array_33, for_val_69);
            end;
        end;
        for for_key_70, for_val_70 in ipairs(Array_33) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_967, b_967, c_967, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3739

        end;
        for for_key_71, for_val_71 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_71 ~= nil) then -- ran, if id: 3740
                Array_34 = {};
                table.insert(Array_34, for_val_71);
            end;
        end;
        for for_key_72, for_val_72 in ipairs(Array_34) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_968, b_968, c_968, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3741

        end;
        for for_key_73, for_val_73 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_73 ~= nil) then -- ran, if id: 3742
                Array_35 = {};
                table.insert(Array_35, for_val_73);
            end;
        end;
        for for_key_74, for_val_74 in ipairs(Array_35) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_969, b_969, c_969, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3743

        end;
        for for_key_75, for_val_75 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_75 ~= nil) then -- ran, if id: 3744
                Array_36 = {};
                table.insert(Array_36, for_val_75);
            end;
        end;
        for for_key_76, for_val_76 in ipairs(Array_36) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_970, b_970, c_970, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3745

        end;
        for for_key_77, for_val_77 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_77 ~= nil) then -- ran, if id: 3746
                Array_37 = {};
                table.insert(Array_37, for_val_77);
            end;
        end;
        for for_key_78, for_val_78 in ipairs(Array_37) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_971, b_971, c_971, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3747

        end;
        for for_key_79, for_val_79 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_79 ~= nil) then -- ran, if id: 3748
                Array_38 = {};
                table.insert(Array_38, for_val_79);
            end;
        end;
        for for_key_80, for_val_80 in ipairs(Array_38) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_972, b_972, c_972, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3749

        end;
        for for_key_81, for_val_81 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_81 ~= nil) then -- ran, if id: 3750
                Array_39 = {};
                table.insert(Array_39, for_val_81);
            end;
        end;
        for for_key_82, for_val_82 in ipairs(Array_39) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_973, b_973, c_973, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3751

        end;
        for for_key_83, for_val_83 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_83 ~= nil) then -- ran, if id: 3752
                Array_40 = {};
                table.insert(Array_40, for_val_83);
            end;
        end;
        for for_key_84, for_val_84 in ipairs(Array_40) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_974, b_974, c_974, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3753

        end;
        for for_key_85, for_val_85 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_85 ~= nil) then -- ran, if id: 3754
                Array_41 = {};
                table.insert(Array_41, for_val_85);
            end;
        end;
        for for_key_86, for_val_86 in ipairs(Array_41) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_975, b_975, c_975, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3755

        end;
        for for_key_87, for_val_87 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_87 ~= nil) then -- ran, if id: 3756
                Array_42 = {};
                table.insert(Array_42, for_val_87);
            end;
        end;
        for for_key_88, for_val_88 in ipairs(Array_42) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_976, b_976, c_976, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3757

        end;
        for for_key_89, for_val_89 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_89 ~= nil) then -- ran, if id: 3758
                Array_43 = {};
                table.insert(Array_43, for_val_89);
            end;
        end;
        for for_key_90, for_val_90 in ipairs(Array_43) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_977, b_977, c_977, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3759

        end;
        for for_key_91, for_val_91 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_91 ~= nil) then -- ran, if id: 3760
                Array_44 = {};
                table.insert(Array_44, for_val_91);
            end;
        end;
        for for_key_92, for_val_92 in ipairs(Array_44) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_978, b_978, c_978, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3761

        end;
        for for_key_93, for_val_93 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_93 ~= nil) then -- ran, if id: 3762
                Array_45 = {};
                table.insert(Array_45, for_val_93);
            end;
        end;
        for for_key_94, for_val_94 in ipairs(Array_45) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_979, b_979, c_979, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3763

        end;
        for for_key_95, for_val_95 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_95 ~= nil) then -- ran, if id: 3764
                Array_46 = {};
                table.insert(Array_46, for_val_95);
            end;
        end;
        for for_key_96, for_val_96 in ipairs(Array_46) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_980, b_980, c_980, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3765

        end;
        for for_key_97, for_val_97 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_97 ~= nil) then -- ran, if id: 3766
                Array_47 = {};
                table.insert(Array_47, for_val_97);
            end;
        end;
        for for_key_98, for_val_98 in ipairs(Array_47) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_981, b_981, c_981, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3767

        end;
        for for_key_99, for_val_99 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_99 ~= nil) then -- ran, if id: 3768
                Array_48 = {};
                table.insert(Array_48, for_val_99);
            end;
        end;
        for for_key_100, for_val_100 in ipairs(Array_48) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_982, b_982, c_982, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3769

        end;
        for for_key_101, for_val_101 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_101 ~= nil) then -- ran, if id: 3770
                Array_49 = {};
                table.insert(Array_49, for_val_101);
            end;
        end;
        for for_key_102, for_val_102 in ipairs(Array_49) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_983, b_983, c_983, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3771

        end;
        for for_key_103, for_val_103 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_103 ~= nil) then -- ran, if id: 3772
                Array_50 = {};
                table.insert(Array_50, for_val_103);
            end;
        end;
        for for_key_104, for_val_104 in ipairs(Array_50) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_984, b_984, c_984, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3773

        end;
        for for_key_105, for_val_105 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_105 ~= nil) then -- ran, if id: 3774
                Array_51 = {};
                table.insert(Array_51, for_val_105);
            end;
        end;
        for for_key_106, for_val_106 in ipairs(Array_51) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_985, b_985, c_985, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3775

        end;
        for for_key_107, for_val_107 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_107 ~= nil) then -- ran, if id: 3776
                Array_52 = {};
                table.insert(Array_52, for_val_107);
            end;
        end;
        for for_key_108, for_val_108 in ipairs(Array_52) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_986, b_986, c_986, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3777

        end;
        for for_key_109, for_val_109 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_109 ~= nil) then -- ran, if id: 3778
                Array_53 = {};
                table.insert(Array_53, for_val_109);
            end;
        end;
        for for_key_110, for_val_110 in ipairs(Array_53) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_987, b_987, c_987, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3779

        end;
        for for_key_111, for_val_111 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_111 ~= nil) then -- ran, if id: 3780
                Array_54 = {};
                table.insert(Array_54, for_val_111);
            end;
        end;
        for for_key_112, for_val_112 in ipairs(Array_54) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_988, b_988, c_988, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3781

        end;
        for for_key_113, for_val_113 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_113 ~= nil) then -- ran, if id: 3782
                Array_55 = {};
                table.insert(Array_55, for_val_113);
            end;
        end;
        for for_key_114, for_val_114 in ipairs(Array_55) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_989, b_989, c_989, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3783

        end;
        for for_key_115, for_val_115 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_115 ~= nil) then -- ran, if id: 3784
                Array_56 = {};
                table.insert(Array_56, for_val_115);
            end;
        end;
        for for_key_116, for_val_116 in ipairs(Array_56) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_990, b_990, c_990, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3785

        end;
        for for_key_117, for_val_117 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_117 ~= nil) then -- ran, if id: 3786
                Array_57 = {};
                table.insert(Array_57, for_val_117);
            end;
        end;
        for for_key_118, for_val_118 in ipairs(Array_57) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_991, b_991, c_991, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3787

        end;
        for for_key_119, for_val_119 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_119 ~= nil) then -- ran, if id: 3788
                Array_58 = {};
                table.insert(Array_58, for_val_119);
            end;
        end;
        for for_key_120, for_val_120 in ipairs(Array_58) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_992, b_992, c_992, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3789

        end;
        for for_key_121, for_val_121 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_121 ~= nil) then -- ran, if id: 3790
                Array_59 = {};
                table.insert(Array_59, for_val_121);
            end;
        end;
        for for_key_122, for_val_122 in ipairs(Array_59) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_993, b_993, c_993, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3791

        end;
        for for_key_123, for_val_123 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_123 ~= nil) then -- ran, if id: 3792
                Array_60 = {};
                table.insert(Array_60, for_val_123);
            end;
        end;
        for for_key_124, for_val_124 in ipairs(Array_60) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_994, b_994, c_994, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3793

        end;
        for for_key_125, for_val_125 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_125 ~= nil) then -- ran, if id: 3794
                Array_61 = {};
                table.insert(Array_61, for_val_125);
            end;
        end;
        for for_key_126, for_val_126 in ipairs(Array_61) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_995, b_995, c_995, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3795

        end;
        for for_key_127, for_val_127 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_127 ~= nil) then -- ran, if id: 3796
                Array_62 = {};
                table.insert(Array_62, for_val_127);
            end;
        end;
        for for_key_128, for_val_128 in ipairs(Array_62) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_996, b_996, c_996, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3797

        end;
        for for_key_129, for_val_129 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_129 ~= nil) then -- ran, if id: 3798
                Array_63 = {};
                table.insert(Array_63, for_val_129);
            end;
        end;
        for for_key_130, for_val_130 in ipairs(Array_63) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_997, b_997, c_997, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3799

        end;
        for for_key_131, for_val_131 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_131 ~= nil) then -- ran, if id: 3800
                Array_64 = {};
                table.insert(Array_64, for_val_131);
            end;
        end;
        for for_key_132, for_val_132 in ipairs(Array_64) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_998, b_998, c_998, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3801

        end;
        for for_key_133, for_val_133 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_133 ~= nil) then -- ran, if id: 3802
                Array_65 = {};
                table.insert(Array_65, for_val_133);
            end;
        end;
        for for_key_134, for_val_134 in ipairs(Array_65) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_999, b_999, c_999, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3803

        end;
        for for_key_135, for_val_135 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_135 ~= nil) then -- ran, if id: 3804
                Array_66 = {};
                table.insert(Array_66, for_val_135);
            end;
        end;
        for for_key_136, for_val_136 in ipairs(Array_66) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1000, b_1000, c_1000, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3805

        end;
        for for_key_137, for_val_137 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_137 ~= nil) then -- ran, if id: 3806
                Array_67 = {};
                table.insert(Array_67, for_val_137);
            end;
        end;
        for for_key_138, for_val_138 in ipairs(Array_67) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1001, b_1001, c_1001, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3807

        end;
        for for_key_139, for_val_139 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_139 ~= nil) then -- ran, if id: 3808
                Array_68 = {};
                table.insert(Array_68, for_val_139);
            end;
        end;
        for for_key_140, for_val_140 in ipairs(Array_68) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1002, b_1002, c_1002, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3809

        end;
        for for_key_141, for_val_141 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_141 ~= nil) then -- ran, if id: 3810
                Array_69 = {};
                table.insert(Array_69, for_val_141);
            end;
        end;
        for for_key_142, for_val_142 in ipairs(Array_69) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1003, b_1003, c_1003, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3811

        end;
        for for_key_143, for_val_143 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_143 ~= nil) then -- ran, if id: 3812
                Array_70 = {};
                table.insert(Array_70, for_val_143);
            end;
        end;
        for for_key_144, for_val_144 in ipairs(Array_70) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1004, b_1004, c_1004, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3813

        end;
        for for_key_145, for_val_145 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_145 ~= nil) then -- ran, if id: 3814
                Array_71 = {};
                table.insert(Array_71, for_val_145);
            end;
        end;
        for for_key_146, for_val_146 in ipairs(Array_71) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1005, b_1005, c_1005, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3815

        end;
        for for_key_147, for_val_147 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_147 ~= nil) then -- ran, if id: 3816
                Array_72 = {};
                table.insert(Array_72, for_val_147);
            end;
        end;
        for for_key_148, for_val_148 in ipairs(Array_72) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1006, b_1006, c_1006, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3817

        end;
        for for_key_149, for_val_149 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_149 ~= nil) then -- ran, if id: 3818
                Array_73 = {};
                table.insert(Array_73, for_val_149);
            end;
        end;
        for for_key_150, for_val_150 in ipairs(Array_73) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1007, b_1007, c_1007, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3819

        end;
        for for_key_151, for_val_151 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_151 ~= nil) then -- ran, if id: 3820
                Array_74 = {};
                table.insert(Array_74, for_val_151);
            end;
        end;
        for for_key_152, for_val_152 in ipairs(Array_74) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1008, b_1008, c_1008, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3821

        end;
        for for_key_153, for_val_153 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_153 ~= nil) then -- ran, if id: 3822
                Array_75 = {};
                table.insert(Array_75, for_val_153);
            end;
        end;
        for for_key_154, for_val_154 in ipairs(Array_75) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1009, b_1009, c_1009, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3823

        end;
        for for_key_155, for_val_155 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_155 ~= nil) then -- ran, if id: 3824
                Array_76 = {};
                table.insert(Array_76, for_val_155);
            end;
        end;
        for for_key_156, for_val_156 in ipairs(Array_76) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1010, b_1010, c_1010, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3825

        end;
        for for_key_157, for_val_157 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_157 ~= nil) then -- ran, if id: 3826
                Array_77 = {};
                table.insert(Array_77, for_val_157);
            end;
        end;
        for for_key_158, for_val_158 in ipairs(Array_77) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1011, b_1011, c_1011, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3827

        end;
        for for_key_159, for_val_159 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_159 ~= nil) then -- ran, if id: 3828
                Array_78 = {};
                table.insert(Array_78, for_val_159);
            end;
        end;
        for for_key_160, for_val_160 in ipairs(Array_78) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1012, b_1012, c_1012, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3829

        end;
        for for_key_161, for_val_161 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_161 ~= nil) then -- ran, if id: 3830
                Array_79 = {};
                table.insert(Array_79, for_val_161);
            end;
        end;
        for for_key_162, for_val_162 in ipairs(Array_79) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1013, b_1013, c_1013, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3831

        end;
        for for_key_163, for_val_163 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_163 ~= nil) then -- ran, if id: 3832
                Array_80 = {};
                table.insert(Array_80, for_val_163);
            end;
        end;
        for for_key_164, for_val_164 in ipairs(Array_80) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1014, b_1014, c_1014, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3833

        end;
        for for_key_165, for_val_165 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_165 ~= nil) then -- ran, if id: 3834
                Array_81 = {};
                table.insert(Array_81, for_val_165);
            end;
        end;
        for for_key_166, for_val_166 in ipairs(Array_81) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1015, b_1015, c_1015, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3835

        end;
        for for_key_167, for_val_167 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_167 ~= nil) then -- ran, if id: 3836
                Array_82 = {};
                table.insert(Array_82, for_val_167);
            end;
        end;
        for for_key_168, for_val_168 in ipairs(Array_82) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1016, b_1016, c_1016, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3837

        end;
        for for_key_169, for_val_169 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_169 ~= nil) then -- ran, if id: 3838
                Array_83 = {};
                table.insert(Array_83, for_val_169);
            end;
        end;
        for for_key_170, for_val_170 in ipairs(Array_83) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1017, b_1017, c_1017, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3839

        end;
        for for_key_171, for_val_171 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_171 ~= nil) then -- ran, if id: 3840
                Array_84 = {};
                table.insert(Array_84, for_val_171);
            end;
        end;
        for for_key_172, for_val_172 in ipairs(Array_84) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1018, b_1018, c_1018, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3841

        end;
        for for_key_173, for_val_173 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_173 ~= nil) then -- ran, if id: 3842
                Array_85 = {};
                table.insert(Array_85, for_val_173);
            end;
        end;
        for for_key_174, for_val_174 in ipairs(Array_85) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1019, b_1019, c_1019, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3843

        end;
        for for_key_175, for_val_175 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_175 ~= nil) then -- ran, if id: 3844
                Array_86 = {};
                table.insert(Array_86, for_val_175);
            end;
        end;
        for for_key_176, for_val_176 in ipairs(Array_86) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1020, b_1020, c_1020, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3845

        end;
        for for_key_177, for_val_177 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_177 ~= nil) then -- ran, if id: 3846
                Array_87 = {};
                table.insert(Array_87, for_val_177);
            end;
        end;
        for for_key_178, for_val_178 in ipairs(Array_87) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1021, b_1021, c_1021, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3847

        end;
        for for_key_179, for_val_179 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_179 ~= nil) then -- ran, if id: 3848
                Array_88 = {};
                table.insert(Array_88, for_val_179);
            end;
        end;
        for for_key_180, for_val_180 in ipairs(Array_88) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1022, b_1022, c_1022, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3849

        end;
        for for_key_181, for_val_181 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_181 ~= nil) then -- ran, if id: 3850
                Array_89 = {};
                table.insert(Array_89, for_val_181);
            end;
        end;
        for for_key_182, for_val_182 in ipairs(Array_89) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1023, b_1023, c_1023, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3851

        end;
        for for_key_183, for_val_183 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_183 ~= nil) then -- ran, if id: 3852
                Array_90 = {};
                table.insert(Array_90, for_val_183);
            end;
        end;
        for for_key_184, for_val_184 in ipairs(Array_90) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1024, b_1024, c_1024, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3853

        end;
        for for_key_185, for_val_185 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_185 ~= nil) then -- ran, if id: 3854
                Array_91 = {};
                table.insert(Array_91, for_val_185);
            end;
        end;
        for for_key_186, for_val_186 in ipairs(Array_91) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1025, b_1025, c_1025, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3855

        end;
        for for_key_187, for_val_187 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_187 ~= nil) then -- ran, if id: 3856
                Array_92 = {};
                table.insert(Array_92, for_val_187);
            end;
        end;
        for for_key_188, for_val_188 in ipairs(Array_92) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1026, b_1026, c_1026, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3857

        end;
        for for_key_189, for_val_189 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_189 ~= nil) then -- ran, if id: 3858
                Array_93 = {};
                table.insert(Array_93, for_val_189);
            end;
        end;
        for for_key_190, for_val_190 in ipairs(Array_93) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1027, b_1027, c_1027, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3859

        end;
        for for_key_191, for_val_191 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_191 ~= nil) then -- ran, if id: 3860
                Array_94 = {};
                table.insert(Array_94, for_val_191);
            end;
        end;
        for for_key_192, for_val_192 in ipairs(Array_94) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1028, b_1028, c_1028, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3861

        end;
        for for_key_193, for_val_193 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_193 ~= nil) then -- ran, if id: 3862
                Array_95 = {};
                table.insert(Array_95, for_val_193);
            end;
        end;
        for for_key_194, for_val_194 in ipairs(Array_95) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1029, b_1029, c_1029, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3863

        end;
        for for_key_195, for_val_195 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_195 ~= nil) then -- ran, if id: 3864
                Array_96 = {};
                table.insert(Array_96, for_val_195);
            end;
        end;
        for for_key_196, for_val_196 in ipairs(Array_96) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1030, b_1030, c_1030, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3865

        end;
        for for_key_197, for_val_197 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_197 ~= nil) then -- ran, if id: 3866
                Array_97 = {};
                table.insert(Array_97, for_val_197);
            end;
        end;
        for for_key_198, for_val_198 in ipairs(Array_97) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1031, b_1031, c_1031, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3867

        end;
        for for_key_199, for_val_199 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_199 ~= nil) then -- ran, if id: 3868
                Array_98 = {};
                table.insert(Array_98, for_val_199);
            end;
        end;
        for for_key_200, for_val_200 in ipairs(Array_98) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1032, b_1032, c_1032, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3869

        end;
        for for_key_201, for_val_201 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_201 ~= nil) then -- ran, if id: 3870
                Array_99 = {};
                table.insert(Array_99, for_val_201);
            end;
        end;
        for for_key_202, for_val_202 in ipairs(Array_99) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1033, b_1033, c_1033, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3871

        end;
        for for_key_203, for_val_203 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_203 ~= nil) then -- ran, if id: 3872
                Array_100 = {};
                table.insert(Array_100, for_val_203);
            end;
        end;
        for for_key_204, for_val_204 in ipairs(Array_100) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1034, b_1034, c_1034, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3873

        end;
        for for_key_205, for_val_205 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_205 ~= nil) then -- ran, if id: 3874
                Array_101 = {};
                table.insert(Array_101, for_val_205);
            end;
        end;
        for for_key_206, for_val_206 in ipairs(Array_101) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1035, b_1035, c_1035, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3875

        end;
        for for_key_207, for_val_207 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_207 ~= nil) then -- ran, if id: 3876
                Array_102 = {};
                table.insert(Array_102, for_val_207);
            end;
        end;
        for for_key_208, for_val_208 in ipairs(Array_102) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1036, b_1036, c_1036, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3877

        end;
        for for_key_209, for_val_209 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_209 ~= nil) then -- ran, if id: 3878
                Array_103 = {};
                table.insert(Array_103, for_val_209);
            end;
        end;
        for for_key_210, for_val_210 in ipairs(Array_103) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1037, b_1037, c_1037, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3879

        end;
        for for_key_211, for_val_211 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_211 ~= nil) then -- ran, if id: 3880
                Array_104 = {};
                table.insert(Array_104, for_val_211);
            end;
        end;
        for for_key_212, for_val_212 in ipairs(Array_104) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1038, b_1038, c_1038, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3881

        end;
        for for_key_213, for_val_213 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_213 ~= nil) then -- ran, if id: 3882
                Array_105 = {};
                table.insert(Array_105, for_val_213);
            end;
        end;
        for for_key_214, for_val_214 in ipairs(Array_105) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1039, b_1039, c_1039, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3883

        end;
        for for_key_215, for_val_215 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_215 ~= nil) then -- ran, if id: 3884
                Array_106 = {};
                table.insert(Array_106, for_val_215);
            end;
        end;
        for for_key_216, for_val_216 in ipairs(Array_106) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1040, b_1040, c_1040, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3885

        end;
        for for_key_217, for_val_217 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_217 ~= nil) then -- ran, if id: 3886
                Array_107 = {};
                table.insert(Array_107, for_val_217);
            end;
        end;
        for for_key_218, for_val_218 in ipairs(Array_107) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1041, b_1041, c_1041, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3887

        end;
        for for_key_219, for_val_219 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_219 ~= nil) then -- ran, if id: 3888
                Array_108 = {};
                table.insert(Array_108, for_val_219);
            end;
        end;
        for for_key_220, for_val_220 in ipairs(Array_108) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1042, b_1042, c_1042, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3889

        end;
        for for_key_221, for_val_221 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_221 ~= nil) then -- ran, if id: 3890
                Array_109 = {};
                table.insert(Array_109, for_val_221);
            end;
        end;
        for for_key_222, for_val_222 in ipairs(Array_109) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1043, b_1043, c_1043, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3891

        end;
        for for_key_223, for_val_223 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_223 ~= nil) then -- ran, if id: 3892
                Array_110 = {};
                table.insert(Array_110, for_val_223);
            end;
        end;
        for for_key_224, for_val_224 in ipairs(Array_110) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1044, b_1044, c_1044, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3893

        end;
        for for_key_225, for_val_225 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_225 ~= nil) then -- ran, if id: 3894
                Array_111 = {};
                table.insert(Array_111, for_val_225);
            end;
        end;
        for for_key_226, for_val_226 in ipairs(Array_111) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1045, b_1045, c_1045, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3895

        end;
        for for_key_227, for_val_227 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_227 ~= nil) then -- ran, if id: 3896
                Array_112 = {};
                table.insert(Array_112, for_val_227);
            end;
        end;
        for for_key_228, for_val_228 in ipairs(Array_112) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1046, b_1046, c_1046, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3897

        end;
        for for_key_229, for_val_229 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_229 ~= nil) then -- ran, if id: 3898
                Array_113 = {};
                table.insert(Array_113, for_val_229);
            end;
        end;
        for for_key_230, for_val_230 in ipairs(Array_113) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1047, b_1047, c_1047, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3899

        end;
        for for_key_231, for_val_231 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_231 ~= nil) then -- ran, if id: 3900
                Array_114 = {};
                table.insert(Array_114, for_val_231);
            end;
        end;
        for for_key_232, for_val_232 in ipairs(Array_114) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1048, b_1048, c_1048, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3901

        end;
        for for_key_233, for_val_233 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_233 ~= nil) then -- ran, if id: 3902
                Array_115 = {};
                table.insert(Array_115, for_val_233);
            end;
        end;
        for for_key_234, for_val_234 in ipairs(Array_115) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1049, b_1049, c_1049, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3903

        end;
        for for_key_235, for_val_235 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_235 ~= nil) then -- ran, if id: 3904
                Array_116 = {};
                table.insert(Array_116, for_val_235);
            end;
        end;
        for for_key_236, for_val_236 in ipairs(Array_116) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1050, b_1050, c_1050, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3905

        end;
        for for_key_237, for_val_237 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_237 ~= nil) then -- ran, if id: 3906
                Array_117 = {};
                table.insert(Array_117, for_val_237);
            end;
        end;
        for for_key_238, for_val_238 in ipairs(Array_117) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1051, b_1051, c_1051, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3907

        end;
        for for_key_239, for_val_239 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_239 ~= nil) then -- ran, if id: 3908
                Array_118 = {};
                table.insert(Array_118, for_val_239);
            end;
        end;
        for for_key_240, for_val_240 in ipairs(Array_118) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1052, b_1052, c_1052, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3909

        end;
        for for_key_241, for_val_241 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_241 ~= nil) then -- ran, if id: 3910
                Array_119 = {};
                table.insert(Array_119, for_val_241);
            end;
        end;
        for for_key_242, for_val_242 in ipairs(Array_119) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1053, b_1053, c_1053, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3911

        end;
        for for_key_243, for_val_243 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_243 ~= nil) then -- ran, if id: 3912
                Array_120 = {};
                table.insert(Array_120, for_val_243);
            end;
        end;
        for for_key_244, for_val_244 in ipairs(Array_120) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1054, b_1054, c_1054, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3913

        end;
        for for_key_245, for_val_245 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_245 ~= nil) then -- ran, if id: 3914
                Array_121 = {};
                table.insert(Array_121, for_val_245);
            end;
        end;
        for for_key_246, for_val_246 in ipairs(Array_121) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1055, b_1055, c_1055, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3915

        end;
        for for_key_247, for_val_247 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_247 ~= nil) then -- ran, if id: 3916
                Array_122 = {};
                table.insert(Array_122, for_val_247);
            end;
        end;
        for for_key_248, for_val_248 in ipairs(Array_122) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1056, b_1056, c_1056, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3917

        end;
        for for_key_249, for_val_249 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_249 ~= nil) then -- ran, if id: 3918
                Array_123 = {};
                table.insert(Array_123, for_val_249);
            end;
        end;
        for for_key_250, for_val_250 in ipairs(Array_123) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1057, b_1057, c_1057, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3919

        end;
        for for_key_251, for_val_251 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_251 ~= nil) then -- ran, if id: 3920
                Array_124 = {};
                table.insert(Array_124, for_val_251);
            end;
        end;
        for for_key_252, for_val_252 in ipairs(Array_124) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1058, b_1058, c_1058, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3921

        end;
        for for_key_253, for_val_253 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_253 ~= nil) then -- ran, if id: 3922
                Array_125 = {};
                table.insert(Array_125, for_val_253);
            end;
        end;
        for for_key_254, for_val_254 in ipairs(Array_125) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1059, b_1059, c_1059, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3923

        end;
        for for_key_255, for_val_255 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_255 ~= nil) then -- ran, if id: 3924
                Array_126 = {};
                table.insert(Array_126, for_val_255);
            end;
        end;
        for for_key_256, for_val_256 in ipairs(Array_126) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1060, b_1060, c_1060, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3925

        end;
        for for_key_257, for_val_257 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_257 ~= nil) then -- ran, if id: 3926
                Array_127 = {};
                table.insert(Array_127, for_val_257);
            end;
        end;
        for for_key_258, for_val_258 in ipairs(Array_127) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1061, b_1061, c_1061, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3927

        end;
        for for_key_259, for_val_259 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_259 ~= nil) then -- ran, if id: 3928
                Array_128 = {};
                table.insert(Array_128, for_val_259);
            end;
        end;
        for for_key_260, for_val_260 in ipairs(Array_128) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1062, b_1062, c_1062, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3929

        end;
        for for_key_261, for_val_261 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_261 ~= nil) then -- ran, if id: 3930
                Array_129 = {};
                table.insert(Array_129, for_val_261);
            end;
        end;
        for for_key_262, for_val_262 in ipairs(Array_129) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1063, b_1063, c_1063, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3931

        end;
        for for_key_263, for_val_263 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_263 ~= nil) then -- ran, if id: 3932
                Array_130 = {};
                table.insert(Array_130, for_val_263);
            end;
        end;
        for for_key_264, for_val_264 in ipairs(Array_130) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1064, b_1064, c_1064, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3933

        end;
        for for_key_265, for_val_265 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_265 ~= nil) then -- ran, if id: 3934
                Array_131 = {};
                table.insert(Array_131, for_val_265);
            end;
        end;
        for for_key_266, for_val_266 in ipairs(Array_131) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1065, b_1065, c_1065, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3935

        end;
        for for_key_267, for_val_267 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_267 ~= nil) then -- ran, if id: 3936
                Array_132 = {};
                table.insert(Array_132, for_val_267);
            end;
        end;
        for for_key_268, for_val_268 in ipairs(Array_132) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1066, b_1066, c_1066, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3937

        end;
        for for_key_269, for_val_269 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_269 ~= nil) then -- ran, if id: 3938
                Array_133 = {};
                table.insert(Array_133, for_val_269);
            end;
        end;
        for for_key_270, for_val_270 in ipairs(Array_133) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1067, b_1067, c_1067, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3939

        end;
        for for_key_271, for_val_271 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_271 ~= nil) then -- ran, if id: 3940
                Array_134 = {};
                table.insert(Array_134, for_val_271);
            end;
        end;
        for for_key_272, for_val_272 in ipairs(Array_134) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1068, b_1068, c_1068, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3941

        end;
        for for_key_273, for_val_273 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_273 ~= nil) then -- ran, if id: 3942
                Array_135 = {};
                table.insert(Array_135, for_val_273);
            end;
        end;
        for for_key_274, for_val_274 in ipairs(Array_135) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1069, b_1069, c_1069, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3943

        end;
        for for_key_275, for_val_275 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_275 ~= nil) then -- ran, if id: 3944
                Array_136 = {};
                table.insert(Array_136, for_val_275);
            end;
        end;
        for for_key_276, for_val_276 in ipairs(Array_136) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1070, b_1070, c_1070, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3945

        end;
        for for_key_277, for_val_277 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_277 ~= nil) then -- ran, if id: 3946
                Array_137 = {};
                table.insert(Array_137, for_val_277);
            end;
        end;
        for for_key_278, for_val_278 in ipairs(Array_137) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1071, b_1071, c_1071, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3947

        end;
        for for_key_279, for_val_279 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_279 ~= nil) then -- ran, if id: 3948
                Array_138 = {};
                table.insert(Array_138, for_val_279);
            end;
        end;
        for for_key_280, for_val_280 in ipairs(Array_138) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1072, b_1072, c_1072, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3949

        end;
        for for_key_281, for_val_281 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_281 ~= nil) then -- ran, if id: 3950
                Array_139 = {};
                table.insert(Array_139, for_val_281);
            end;
        end;
        for for_key_282, for_val_282 in ipairs(Array_139) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1073, b_1073, c_1073, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3951

        end;
        for for_key_283, for_val_283 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_283 ~= nil) then -- ran, if id: 3952
                Array_140 = {};
                table.insert(Array_140, for_val_283);
            end;
        end;
        for for_key_284, for_val_284 in ipairs(Array_140) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1074, b_1074, c_1074, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3953

        end;
        for for_key_285, for_val_285 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_285 ~= nil) then -- ran, if id: 3954
                Array_141 = {};
                table.insert(Array_141, for_val_285);
            end;
        end;
        for for_key_286, for_val_286 in ipairs(Array_141) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1075, b_1075, c_1075, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3955

        end;
        for for_key_287, for_val_287 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_287 ~= nil) then -- ran, if id: 3956
                Array_142 = {};
                table.insert(Array_142, for_val_287);
            end;
        end;
        for for_key_288, for_val_288 in ipairs(Array_142) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1076, b_1076, c_1076, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3957

        end;
        for for_key_289, for_val_289 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_289 ~= nil) then -- ran, if id: 3958
                Array_143 = {};
                table.insert(Array_143, for_val_289);
            end;
        end;
        for for_key_290, for_val_290 in ipairs(Array_143) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1077, b_1077, c_1077, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3959

        end;
        for for_key_291, for_val_291 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_291 ~= nil) then -- ran, if id: 3960
                Array_144 = {};
                table.insert(Array_144, for_val_291);
            end;
        end;
        for for_key_292, for_val_292 in ipairs(Array_144) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1078, b_1078, c_1078, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3961

        end;
        for for_key_293, for_val_293 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_293 ~= nil) then -- ran, if id: 3962
                Array_145 = {};
                table.insert(Array_145, for_val_293);
            end;
        end;
        for for_key_294, for_val_294 in ipairs(Array_145) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1079, b_1079, c_1079, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3963

        end;
        for for_key_295, for_val_295 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_295 ~= nil) then -- ran, if id: 3964
                Array_146 = {};
                table.insert(Array_146, for_val_295);
            end;
        end;
        for for_key_296, for_val_296 in ipairs(Array_146) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1080, b_1080, c_1080, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3965

        end;
        for for_key_297, for_val_297 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_297 ~= nil) then -- ran, if id: 3966
                Array_147 = {};
                table.insert(Array_147, for_val_297);
            end;
        end;
        for for_key_298, for_val_298 in ipairs(Array_147) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1081, b_1081, c_1081, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3967

        end;
        for for_key_299, for_val_299 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_299 ~= nil) then -- ran, if id: 3968
                Array_148 = {};
                table.insert(Array_148, for_val_299);
            end;
        end;
        for for_key_300, for_val_300 in ipairs(Array_148) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1082, b_1082, c_1082, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3969

        end;
        for for_key_301, for_val_301 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_301 ~= nil) then -- ran, if id: 3970
                Array_149 = {};
                table.insert(Array_149, for_val_301);
            end;
        end;
        for for_key_302, for_val_302 in ipairs(Array_149) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1083, b_1083, c_1083, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3971

        end;
        for for_key_303, for_val_303 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_303 ~= nil) then -- ran, if id: 3972
                Array_150 = {};
                table.insert(Array_150, for_val_303);
            end;
        end;
        for for_key_304, for_val_304 in ipairs(Array_150) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1084, b_1084, c_1084, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3973

        end;
        for for_key_305, for_val_305 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_305 ~= nil) then -- ran, if id: 3974
                Array_151 = {};
                table.insert(Array_151, for_val_305);
            end;
        end;
        for for_key_306, for_val_306 in ipairs(Array_151) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1085, b_1085, c_1085, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3975

        end;
        for for_key_307, for_val_307 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_307 ~= nil) then -- ran, if id: 3976
                Array_152 = {};
                table.insert(Array_152, for_val_307);
            end;
        end;
        for for_key_308, for_val_308 in ipairs(Array_152) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1086, b_1086, c_1086, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3977

        end;
        for for_key_309, for_val_309 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_309 ~= nil) then -- ran, if id: 3978
                Array_153 = {};
                table.insert(Array_153, for_val_309);
            end;
        end;
        for for_key_310, for_val_310 in ipairs(Array_153) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1087, b_1087, c_1087, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3979

        end;
        for for_key_311, for_val_311 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_311 ~= nil) then -- ran, if id: 3980
                Array_154 = {};
                table.insert(Array_154, for_val_311);
            end;
        end;
        for for_key_312, for_val_312 in ipairs(Array_154) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1088, b_1088, c_1088, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3981

        end;
        for for_key_313, for_val_313 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_313 ~= nil) then -- ran, if id: 3982
                Array_155 = {};
                table.insert(Array_155, for_val_313);
            end;
        end;
        for for_key_314, for_val_314 in ipairs(Array_155) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1089, b_1089, c_1089, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3983

        end;
        for for_key_315, for_val_315 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_315 ~= nil) then -- ran, if id: 3984
                Array_156 = {};
                table.insert(Array_156, for_val_315);
            end;
        end;
        for for_key_316, for_val_316 in ipairs(Array_156) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1090, b_1090, c_1090, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3985

        end;
        for for_key_317, for_val_317 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_317 ~= nil) then -- ran, if id: 3986
                Array_157 = {};
                table.insert(Array_157, for_val_317);
            end;
        end;
        for for_key_318, for_val_318 in ipairs(Array_157) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1091, b_1091, c_1091, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3987

        end;
        for for_key_319, for_val_319 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_319 ~= nil) then -- ran, if id: 3988
                Array_158 = {};
                table.insert(Array_158, for_val_319);
            end;
        end;
        for for_key_320, for_val_320 in ipairs(Array_158) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1092, b_1092, c_1092, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3989

        end;
        for for_key_321, for_val_321 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_321 ~= nil) then -- ran, if id: 3990
                Array_159 = {};
                table.insert(Array_159, for_val_321);
            end;
        end;
        for for_key_322, for_val_322 in ipairs(Array_159) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1093, b_1093, c_1093, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3991

        end;
        for for_key_323, for_val_323 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_323 ~= nil) then -- ran, if id: 3992
                Array_160 = {};
                table.insert(Array_160, for_val_323);
            end;
        end;
        for for_key_324, for_val_324 in ipairs(Array_160) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1094, b_1094, c_1094, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3993

        end;
        for for_key_325, for_val_325 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_325 ~= nil) then -- ran, if id: 3994
                Array_161 = {};
                table.insert(Array_161, for_val_325);
            end;
        end;
        for for_key_326, for_val_326 in ipairs(Array_161) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1095, b_1095, c_1095, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3995

        end;
        for for_key_327, for_val_327 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_327 ~= nil) then -- ran, if id: 3996
                Array_162 = {};
                table.insert(Array_162, for_val_327);
            end;
        end;
        for for_key_328, for_val_328 in ipairs(Array_162) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1096, b_1096, c_1096, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3997

        end;
        for for_key_329, for_val_329 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_329 ~= nil) then -- ran, if id: 3998
                Array_163 = {};
                table.insert(Array_163, for_val_329);
            end;
        end;
        for for_key_330, for_val_330 in ipairs(Array_163) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1097, b_1097, c_1097, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 3999

        end;
        for for_key_331, for_val_331 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_331 ~= nil) then -- ran, if id: 4000
                Array_164 = {};
                table.insert(Array_164, for_val_331);
            end;
        end;
        for for_key_332, for_val_332 in ipairs(Array_164) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1098, b_1098, c_1098, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4001

        end;
        for for_key_333, for_val_333 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_333 ~= nil) then -- ran, if id: 4002
                Array_165 = {};
                table.insert(Array_165, for_val_333);
            end;
        end;
        for for_key_334, for_val_334 in ipairs(Array_165) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1099, b_1099, c_1099, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4003

        end;
        for for_key_335, for_val_335 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_335 ~= nil) then -- ran, if id: 4004
                Array_166 = {};
                table.insert(Array_166, for_val_335);
            end;
        end;
        for for_key_336, for_val_336 in ipairs(Array_166) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1100, b_1100, c_1100, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4005

        end;
        for for_key_337, for_val_337 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_337 ~= nil) then -- ran, if id: 4006
                Array_167 = {};
                table.insert(Array_167, for_val_337);
            end;
        end;
        for for_key_338, for_val_338 in ipairs(Array_167) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1101, b_1101, c_1101, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4007

        end;
        for for_key_339, for_val_339 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_339 ~= nil) then -- ran, if id: 4008
                Array_168 = {};
                table.insert(Array_168, for_val_339);
            end;
        end;
        for for_key_340, for_val_340 in ipairs(Array_168) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1102, b_1102, c_1102, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4009

        end;
        for for_key_341, for_val_341 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_341 ~= nil) then -- ran, if id: 4010
                Array_169 = {};
                table.insert(Array_169, for_val_341);
            end;
        end;
        for for_key_342, for_val_342 in ipairs(Array_169) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1103, b_1103, c_1103, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4011

        end;
        for for_key_343, for_val_343 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_343 ~= nil) then -- ran, if id: 4012
                Array_170 = {};
                table.insert(Array_170, for_val_343);
            end;
        end;
        for for_key_344, for_val_344 in ipairs(Array_170) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1104, b_1104, c_1104, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4013

        end;
        for for_key_345, for_val_345 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_345 ~= nil) then -- ran, if id: 4014
                Array_171 = {};
                table.insert(Array_171, for_val_345);
            end;
        end;
        for for_key_346, for_val_346 in ipairs(Array_171) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1105, b_1105, c_1105, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4015

        end;
        for for_key_347, for_val_347 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_347 ~= nil) then -- ran, if id: 4016
                Array_172 = {};
                table.insert(Array_172, for_val_347);
            end;
        end;
        for for_key_348, for_val_348 in ipairs(Array_172) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1106, b_1106, c_1106, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4017

        end;
        for for_key_349, for_val_349 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_349 ~= nil) then -- ran, if id: 4018
                Array_173 = {};
                table.insert(Array_173, for_val_349);
            end;
        end;
        for for_key_350, for_val_350 in ipairs(Array_173) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1107, b_1107, c_1107, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4019

        end;
        for for_key_351, for_val_351 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_351 ~= nil) then -- ran, if id: 4020
                Array_174 = {};
                table.insert(Array_174, for_val_351);
            end;
        end;
        for for_key_352, for_val_352 in ipairs(Array_174) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1108, b_1108, c_1108, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4021

        end;
        for for_key_353, for_val_353 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_353 ~= nil) then -- ran, if id: 4022
                Array_175 = {};
                table.insert(Array_175, for_val_353);
            end;
        end;
        for for_key_354, for_val_354 in ipairs(Array_175) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1109, b_1109, c_1109, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4023

        end;
        for for_key_355, for_val_355 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_355 ~= nil) then -- ran, if id: 4024
                Array_176 = {};
                table.insert(Array_176, for_val_355);
            end;
        end;
        for for_key_356, for_val_356 in ipairs(Array_176) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1110, b_1110, c_1110, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4025

        end;
        for for_key_357, for_val_357 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_357 ~= nil) then -- ran, if id: 4026
                Array_177 = {};
                table.insert(Array_177, for_val_357);
            end;
        end;
        for for_key_358, for_val_358 in ipairs(Array_177) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1111, b_1111, c_1111, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4027

        end;
        for for_key_359, for_val_359 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_359 ~= nil) then -- ran, if id: 4028
                Array_178 = {};
                table.insert(Array_178, for_val_359);
            end;
        end;
        for for_key_360, for_val_360 in ipairs(Array_178) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1112, b_1112, c_1112, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4029

        end;
        for for_key_361, for_val_361 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_361 ~= nil) then -- ran, if id: 4030
                Array_179 = {};
                table.insert(Array_179, for_val_361);
            end;
        end;
        for for_key_362, for_val_362 in ipairs(Array_179) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1113, b_1113, c_1113, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4031

        end;
        for for_key_363, for_val_363 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_363 ~= nil) then -- ran, if id: 4032
                Array_180 = {};
                table.insert(Array_180, for_val_363);
            end;
        end;
        for for_key_364, for_val_364 in ipairs(Array_180) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1114, b_1114, c_1114, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4033

        end;
        for for_key_365, for_val_365 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_365 ~= nil) then -- ran, if id: 4034
                Array_181 = {};
                table.insert(Array_181, for_val_365);
            end;
        end;
        for for_key_366, for_val_366 in ipairs(Array_181) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1115, b_1115, c_1115, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4035

        end;
        for for_key_367, for_val_367 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_367 ~= nil) then -- ran, if id: 4036
                Array_182 = {};
                table.insert(Array_182, for_val_367);
            end;
        end;
        for for_key_368, for_val_368 in ipairs(Array_182) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1116, b_1116, c_1116, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4037

        end;
        for for_key_369, for_val_369 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_369 ~= nil) then -- ran, if id: 4038
                Array_183 = {};
                table.insert(Array_183, for_val_369);
            end;
        end;
        for for_key_370, for_val_370 in ipairs(Array_183) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1117, b_1117, c_1117, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4039

        end;
        for for_key_371, for_val_371 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_371 ~= nil) then -- ran, if id: 4040
                Array_184 = {};
                table.insert(Array_184, for_val_371);
            end;
        end;
        for for_key_372, for_val_372 in ipairs(Array_184) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1118, b_1118, c_1118, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4041

        end;
        for for_key_373, for_val_373 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_373 ~= nil) then -- ran, if id: 4042
                Array_185 = {};
                table.insert(Array_185, for_val_373);
            end;
        end;
        for for_key_374, for_val_374 in ipairs(Array_185) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1119, b_1119, c_1119, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4043

        end;
        for for_key_375, for_val_375 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_375 ~= nil) then -- ran, if id: 4044
                Array_186 = {};
                table.insert(Array_186, for_val_375);
            end;
        end;
        for for_key_376, for_val_376 in ipairs(Array_186) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1120, b_1120, c_1120, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4045

        end;
        for for_key_377, for_val_377 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_377 ~= nil) then -- ran, if id: 4046
                Array_187 = {};
                table.insert(Array_187, for_val_377);
            end;
        end;
        for for_key_378, for_val_378 in ipairs(Array_187) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1121, b_1121, c_1121, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4047

        end;
        for for_key_379, for_val_379 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_379 ~= nil) then -- ran, if id: 4048
                Array_188 = {};
                table.insert(Array_188, for_val_379);
            end;
        end;
        for for_key_380, for_val_380 in ipairs(Array_188) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1122, b_1122, c_1122, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4049

        end;
        for for_key_381, for_val_381 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_381 ~= nil) then -- ran, if id: 4050
                Array_189 = {};
                table.insert(Array_189, for_val_381);
            end;
        end;
        for for_key_382, for_val_382 in ipairs(Array_189) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1123, b_1123, c_1123, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4051

        end;
        for for_key_383, for_val_383 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_383 ~= nil) then -- ran, if id: 4052
                Array_190 = {};
                table.insert(Array_190, for_val_383);
            end;
        end;
        for for_key_384, for_val_384 in ipairs(Array_190) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1124, b_1124, c_1124, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4053

        end;
        for for_key_385, for_val_385 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_385 ~= nil) then -- ran, if id: 4054
                Array_191 = {};
                table.insert(Array_191, for_val_385);
            end;
        end;
        for for_key_386, for_val_386 in ipairs(Array_191) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1125, b_1125, c_1125, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4055

        end;
        for for_key_387, for_val_387 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_387 ~= nil) then -- ran, if id: 4056
                Array_192 = {};
                table.insert(Array_192, for_val_387);
            end;
        end;
        for for_key_388, for_val_388 in ipairs(Array_192) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1126, b_1126, c_1126, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4057

        end;
        for for_key_389, for_val_389 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_389 ~= nil) then -- ran, if id: 4058
                Array_193 = {};
                table.insert(Array_193, for_val_389);
            end;
        end;
        for for_key_390, for_val_390 in ipairs(Array_193) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1127, b_1127, c_1127, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4059

        end;
        for for_key_391, for_val_391 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_391 ~= nil) then -- ran, if id: 4060
                Array_194 = {};
                table.insert(Array_194, for_val_391);
            end;
        end;
        for for_key_392, for_val_392 in ipairs(Array_194) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1128, b_1128, c_1128, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4061

        end;
        for for_key_393, for_val_393 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_393 ~= nil) then -- ran, if id: 4062
                Array_195 = {};
                table.insert(Array_195, for_val_393);
            end;
        end;
        for for_key_394, for_val_394 in ipairs(Array_195) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1129, b_1129, c_1129, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4063

        end;
        for for_key_395, for_val_395 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_395 ~= nil) then -- ran, if id: 4064
                Array_196 = {};
                table.insert(Array_196, for_val_395);
            end;
        end;
        for for_key_396, for_val_396 in ipairs(Array_196) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1130, b_1130, c_1130, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4065

        end;
        for for_key_397, for_val_397 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_397 ~= nil) then -- ran, if id: 4066
                Array_197 = {};
                table.insert(Array_197, for_val_397);
            end;
        end;
        for for_key_398, for_val_398 in ipairs(Array_197) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1131, b_1131, c_1131, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4067

        end;
        for for_key_399, for_val_399 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_399 ~= nil) then -- ran, if id: 4068
                Array_198 = {};
                table.insert(Array_198, for_val_399);
            end;
        end;
        for for_key_400, for_val_400 in ipairs(Array_198) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1132, b_1132, c_1132, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4069

        end;
        for for_key_401, for_val_401 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_401 ~= nil) then -- ran, if id: 4070
                Array_199 = {};
                table.insert(Array_199, for_val_401);
            end;
        end;
        for for_key_402, for_val_402 in ipairs(Array_199) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1133, b_1133, c_1133, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4071

        end;
        for for_key_403, for_val_403 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_403 ~= nil) then -- ran, if id: 4072
                Array_200 = {};
                table.insert(Array_200, for_val_403);
            end;
        end;
        for for_key_404, for_val_404 in ipairs(Array_200) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1134, b_1134, c_1134, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4073

        end;
        for for_key_405, for_val_405 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_405 ~= nil) then -- ran, if id: 4074
                Array_201 = {};
                table.insert(Array_201, for_val_405);
            end;
        end;
        for for_key_406, for_val_406 in ipairs(Array_201) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1135, b_1135, c_1135, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4075

        end;
        for for_key_407, for_val_407 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_407 ~= nil) then -- ran, if id: 4076
                Array_202 = {};
                table.insert(Array_202, for_val_407);
            end;
        end;
        for for_key_408, for_val_408 in ipairs(Array_202) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1136, b_1136, c_1136, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4077

        end;
        for for_key_409, for_val_409 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_409 ~= nil) then -- ran, if id: 4078
                Array_203 = {};
                table.insert(Array_203, for_val_409);
            end;
        end;
        for for_key_410, for_val_410 in ipairs(Array_203) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1137, b_1137, c_1137, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4079

        end;
        for for_key_411, for_val_411 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_411 ~= nil) then -- ran, if id: 4080
                Array_204 = {};
                table.insert(Array_204, for_val_411);
            end;
        end;
        for for_key_412, for_val_412 in ipairs(Array_204) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1138, b_1138, c_1138, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4081

        end;
        for for_key_413, for_val_413 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_413 ~= nil) then -- ran, if id: 4082
                Array_205 = {};
                table.insert(Array_205, for_val_413);
            end;
        end;
        for for_key_414, for_val_414 in ipairs(Array_205) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1139, b_1139, c_1139, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4083

        end;
        for for_key_415, for_val_415 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_415 ~= nil) then -- ran, if id: 4084
                Array_206 = {};
                table.insert(Array_206, for_val_415);
            end;
        end;
        for for_key_416, for_val_416 in ipairs(Array_206) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1140, b_1140, c_1140, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4085

        end;
        for for_key_417, for_val_417 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_417 ~= nil) then -- ran, if id: 4086
                Array_207 = {};
                table.insert(Array_207, for_val_417);
            end;
        end;
        for for_key_418, for_val_418 in ipairs(Array_207) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1141, b_1141, c_1141, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4087

        end;
        for for_key_419, for_val_419 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_419 ~= nil) then -- ran, if id: 4088
                Array_208 = {};
                table.insert(Array_208, for_val_419);
            end;
        end;
        for for_key_420, for_val_420 in ipairs(Array_208) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1142, b_1142, c_1142, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4089

        end;
        for for_key_421, for_val_421 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_421 ~= nil) then -- ran, if id: 4090
                Array_209 = {};
                table.insert(Array_209, for_val_421);
            end;
        end;
        for for_key_422, for_val_422 in ipairs(Array_209) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1143, b_1143, c_1143, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4091

        end;
        for for_key_423, for_val_423 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_423 ~= nil) then -- ran, if id: 4092
                Array_210 = {};
                table.insert(Array_210, for_val_423);
            end;
        end;
        for for_key_424, for_val_424 in ipairs(Array_210) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1144, b_1144, c_1144, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4093

        end;
        for for_key_425, for_val_425 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_425 ~= nil) then -- ran, if id: 4094
                Array_211 = {};
                table.insert(Array_211, for_val_425);
            end;
        end;
        for for_key_426, for_val_426 in ipairs(Array_211) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1145, b_1145, c_1145, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4095

        end;
        for for_key_427, for_val_427 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_427 ~= nil) then -- ran, if id: 4096
                Array_212 = {};
                table.insert(Array_212, for_val_427);
            end;
        end;
        for for_key_428, for_val_428 in ipairs(Array_212) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1146, b_1146, c_1146, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4097

        end;
        for for_key_429, for_val_429 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_429 ~= nil) then -- ran, if id: 4098
                Array_213 = {};
                table.insert(Array_213, for_val_429);
            end;
        end;
        for for_key_430, for_val_430 in ipairs(Array_213) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1147, b_1147, c_1147, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4099

        end;
        for for_key_431, for_val_431 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_431 ~= nil) then -- ran, if id: 4100
                Array_214 = {};
                table.insert(Array_214, for_val_431);
            end;
        end;
        for for_key_432, for_val_432 in ipairs(Array_214) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1148, b_1148, c_1148, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4101

        end;
        for for_key_433, for_val_433 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_433 ~= nil) then -- ran, if id: 4102
                Array_215 = {};
                table.insert(Array_215, for_val_433);
            end;
        end;
        for for_key_434, for_val_434 in ipairs(Array_215) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1149, b_1149, c_1149, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4103

        end;
        for for_key_435, for_val_435 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_435 ~= nil) then -- ran, if id: 4104
                Array_216 = {};
                table.insert(Array_216, for_val_435);
            end;
        end;
        for for_key_436, for_val_436 in ipairs(Array_216) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1150, b_1150, c_1150, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4105

        end;
        for for_key_437, for_val_437 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_437 ~= nil) then -- ran, if id: 4106
                Array_217 = {};
                table.insert(Array_217, for_val_437);
            end;
        end;
        for for_key_438, for_val_438 in ipairs(Array_217) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1151, b_1151, c_1151, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4107

        end;
        for for_key_439, for_val_439 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_439 ~= nil) then -- ran, if id: 4108
                Array_218 = {};
                table.insert(Array_218, for_val_439);
            end;
        end;
        for for_key_440, for_val_440 in ipairs(Array_218) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1152, b_1152, c_1152, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4109

        end;
        for for_key_441, for_val_441 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_441 ~= nil) then -- ran, if id: 4110
                Array_219 = {};
                table.insert(Array_219, for_val_441);
            end;
        end;
        for for_key_442, for_val_442 in ipairs(Array_219) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1153, b_1153, c_1153, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4111

        end;
        for for_key_443, for_val_443 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_443 ~= nil) then -- ran, if id: 4112
                Array_220 = {};
                table.insert(Array_220, for_val_443);
            end;
        end;
        for for_key_444, for_val_444 in ipairs(Array_220) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1154, b_1154, c_1154, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4113

        end;
        for for_key_445, for_val_445 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_445 ~= nil) then -- ran, if id: 4114
                Array_221 = {};
                table.insert(Array_221, for_val_445);
            end;
        end;
        for for_key_446, for_val_446 in ipairs(Array_221) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1155, b_1155, c_1155, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4115

        end;
        for for_key_447, for_val_447 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_447 ~= nil) then -- ran, if id: 4116
                Array_222 = {};
                table.insert(Array_222, for_val_447);
            end;
        end;
        for for_key_448, for_val_448 in ipairs(Array_222) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1156, b_1156, c_1156, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4117

        end;
        for for_key_449, for_val_449 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_449 ~= nil) then -- ran, if id: 4118
                Array_223 = {};
                table.insert(Array_223, for_val_449);
            end;
        end;
        for for_key_450, for_val_450 in ipairs(Array_223) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1157, b_1157, c_1157, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4119

        end;
        for for_key_451, for_val_451 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_451 ~= nil) then -- ran, if id: 4120
                Array_224 = {};
                table.insert(Array_224, for_val_451);
            end;
        end;
        for for_key_452, for_val_452 in ipairs(Array_224) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1158, b_1158, c_1158, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4121

        end;
        for for_key_453, for_val_453 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_453 ~= nil) then -- ran, if id: 4122
                Array_225 = {};
                table.insert(Array_225, for_val_453);
            end;
        end;
        for for_key_454, for_val_454 in ipairs(Array_225) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1159, b_1159, c_1159, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4123

        end;
        for for_key_455, for_val_455 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_455 ~= nil) then -- ran, if id: 4124
                Array_226 = {};
                table.insert(Array_226, for_val_455);
            end;
        end;
        for for_key_456, for_val_456 in ipairs(Array_226) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1160, b_1160, c_1160, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4125

        end;
        for for_key_457, for_val_457 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_457 ~= nil) then -- ran, if id: 4126
                Array_227 = {};
                table.insert(Array_227, for_val_457);
            end;
        end;
        for for_key_458, for_val_458 in ipairs(Array_227) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1161, b_1161, c_1161, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4127

        end;
        for for_key_459, for_val_459 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_459 ~= nil) then -- ran, if id: 4128
                Array_228 = {};
                table.insert(Array_228, for_val_459);
            end;
        end;
        for for_key_460, for_val_460 in ipairs(Array_228) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1162, b_1162, c_1162, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4129

        end;
        for for_key_461, for_val_461 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_461 ~= nil) then -- ran, if id: 4130
                Array_229 = {};
                table.insert(Array_229, for_val_461);
            end;
        end;
        for for_key_462, for_val_462 in ipairs(Array_229) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1163, b_1163, c_1163, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4131

        end;
        for for_key_463, for_val_463 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_463 ~= nil) then -- ran, if id: 4132
                Array_230 = {};
                table.insert(Array_230, for_val_463);
            end;
        end;
        for for_key_464, for_val_464 in ipairs(Array_230) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1164, b_1164, c_1164, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4133

        end;
        for for_key_465, for_val_465 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_465 ~= nil) then -- ran, if id: 4134
                Array_231 = {};
                table.insert(Array_231, for_val_465);
            end;
        end;
        for for_key_466, for_val_466 in ipairs(Array_231) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1165, b_1165, c_1165, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4135

        end;
        for for_key_467, for_val_467 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_467 ~= nil) then -- ran, if id: 4136
                Array_232 = {};
                table.insert(Array_232, for_val_467);
            end;
        end;
        for for_key_468, for_val_468 in ipairs(Array_232) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1166, b_1166, c_1166, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4137

        end;
        for for_key_469, for_val_469 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_469 ~= nil) then -- ran, if id: 4138
                Array_233 = {};
                table.insert(Array_233, for_val_469);
            end;
        end;
        for for_key_470, for_val_470 in ipairs(Array_233) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1167, b_1167, c_1167, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4139

        end;
        for for_key_471, for_val_471 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_471 ~= nil) then -- ran, if id: 4140
                Array_234 = {};
                table.insert(Array_234, for_val_471);
            end;
        end;
        for for_key_472, for_val_472 in ipairs(Array_234) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1168, b_1168, c_1168, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4141

        end;
        for for_key_473, for_val_473 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_473 ~= nil) then -- ran, if id: 4142
                Array_235 = {};
                table.insert(Array_235, for_val_473);
            end;
        end;
        for for_key_474, for_val_474 in ipairs(Array_235) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1169, b_1169, c_1169, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4143

        end;
        for for_key_475, for_val_475 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_475 ~= nil) then -- ran, if id: 4144
                Array_236 = {};
                table.insert(Array_236, for_val_475);
            end;
        end;
        for for_key_476, for_val_476 in ipairs(Array_236) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1170, b_1170, c_1170, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4145

        end;
        for for_key_477, for_val_477 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_477 ~= nil) then -- ran, if id: 4146
                Array_237 = {};
                table.insert(Array_237, for_val_477);
            end;
        end;
        for for_key_478, for_val_478 in ipairs(Array_237) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1171, b_1171, c_1171, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4147

        end;
        for for_key_479, for_val_479 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_479 ~= nil) then -- ran, if id: 4148
                Array_238 = {};
                table.insert(Array_238, for_val_479);
            end;
        end;
        for for_key_480, for_val_480 in ipairs(Array_238) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1172, b_1172, c_1172, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4149

        end;
        for for_key_481, for_val_481 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_481 ~= nil) then -- ran, if id: 4150
                Array_239 = {};
                table.insert(Array_239, for_val_481);
            end;
        end;
        for for_key_482, for_val_482 in ipairs(Array_239) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1173, b_1173, c_1173, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4151

        end;
        for for_key_483, for_val_483 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_483 ~= nil) then -- ran, if id: 4152
                Array_240 = {};
                table.insert(Array_240, for_val_483);
            end;
        end;
        for for_key_484, for_val_484 in ipairs(Array_240) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1174, b_1174, c_1174, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4153

        end;
        for for_key_485, for_val_485 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_485 ~= nil) then -- ran, if id: 4154
                Array_241 = {};
                table.insert(Array_241, for_val_485);
            end;
        end;
        for for_key_486, for_val_486 in ipairs(Array_241) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1175, b_1175, c_1175, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4155

        end;
        for for_key_487, for_val_487 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_487 ~= nil) then -- ran, if id: 4156
                Array_242 = {};
                table.insert(Array_242, for_val_487);
            end;
        end;
        for for_key_488, for_val_488 in ipairs(Array_242) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1176, b_1176, c_1176, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4157

        end;
        for for_key_489, for_val_489 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_489 ~= nil) then -- ran, if id: 4158
                Array_243 = {};
                table.insert(Array_243, for_val_489);
            end;
        end;
        for for_key_490, for_val_490 in ipairs(Array_243) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1177, b_1177, c_1177, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4159

        end;
        for for_key_491, for_val_491 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_491 ~= nil) then -- ran, if id: 4160
                Array_244 = {};
                table.insert(Array_244, for_val_491);
            end;
        end;
        for for_key_492, for_val_492 in ipairs(Array_244) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1178, b_1178, c_1178, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4161

        end;
        for for_key_493, for_val_493 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_493 ~= nil) then -- ran, if id: 4162
                Array_245 = {};
                table.insert(Array_245, for_val_493);
            end;
        end;
        for for_key_494, for_val_494 in ipairs(Array_245) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1179, b_1179, c_1179, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4163

        end;
        for for_key_495, for_val_495 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_495 ~= nil) then -- ran, if id: 4164
                Array_246 = {};
                table.insert(Array_246, for_val_495);
            end;
        end;
        for for_key_496, for_val_496 in ipairs(Array_246) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1180, b_1180, c_1180, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4165

        end;
        for for_key_497, for_val_497 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_497 ~= nil) then -- ran, if id: 4166
                Array_247 = {};
                table.insert(Array_247, for_val_497);
            end;
        end;
        for for_key_498, for_val_498 in ipairs(Array_247) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1181, b_1181, c_1181, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4167

        end;
        for for_key_499, for_val_499 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_499 ~= nil) then -- ran, if id: 4168
                Array_248 = {};
                table.insert(Array_248, for_val_499);
            end;
        end;
        for for_key_500, for_val_500 in ipairs(Array_248) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1182, b_1182, c_1182, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4169

        end;
        for for_key_501, for_val_501 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_501 ~= nil) then -- ran, if id: 4170
                Array_249 = {};
                table.insert(Array_249, for_val_501);
            end;
        end;
        for for_key_502, for_val_502 in ipairs(Array_249) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1183, b_1183, c_1183, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4171

        end;
        for for_key_503, for_val_503 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_503 ~= nil) then -- ran, if id: 4172
                Array_250 = {};
                table.insert(Array_250, for_val_503);
            end;
        end;
        for for_key_504, for_val_504 in ipairs(Array_250) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1184, b_1184, c_1184, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4173

        end;
        for for_key_505, for_val_505 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_505 ~= nil) then -- ran, if id: 4174
                Array_251 = {};
                table.insert(Array_251, for_val_505);
            end;
        end;
        for for_key_506, for_val_506 in ipairs(Array_251) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1185, b_1185, c_1185, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4175

        end;
        for for_key_507, for_val_507 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_507 ~= nil) then -- ran, if id: 4176
                Array_252 = {};
                table.insert(Array_252, for_val_507);
            end;
        end;
        for for_key_508, for_val_508 in ipairs(Array_252) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1186, b_1186, c_1186, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4177

        end;
        for for_key_509, for_val_509 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_509 ~= nil) then -- ran, if id: 4178
                Array_253 = {};
                table.insert(Array_253, for_val_509);
            end;
        end;
        for for_key_510, for_val_510 in ipairs(Array_253) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1187, b_1187, c_1187, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4179

        end;
        for for_key_511, for_val_511 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_511 ~= nil) then -- ran, if id: 4180
                Array_254 = {};
                table.insert(Array_254, for_val_511);
            end;
        end;
        for for_key_512, for_val_512 in ipairs(Array_254) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1188, b_1188, c_1188, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4181

        end;
        for for_key_513, for_val_513 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_513 ~= nil) then -- ran, if id: 4182
                Array_255 = {};
                table.insert(Array_255, for_val_513);
            end;
        end;
        for for_key_514, for_val_514 in ipairs(Array_255) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1189, b_1189, c_1189, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4183

        end;
        for for_key_515, for_val_515 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_515 ~= nil) then -- ran, if id: 4184
                Array_256 = {};
                table.insert(Array_256, for_val_515);
            end;
        end;
        for for_key_516, for_val_516 in ipairs(Array_256) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1190, b_1190, c_1190, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4185

        end;
        for for_key_517, for_val_517 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_517 ~= nil) then -- ran, if id: 4186
                Array_257 = {};
                table.insert(Array_257, for_val_517);
            end;
        end;
        for for_key_518, for_val_518 in ipairs(Array_257) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1191, b_1191, c_1191, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4187

        end;
        for for_key_519, for_val_519 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_519 ~= nil) then -- ran, if id: 4188
                Array_258 = {};
                table.insert(Array_258, for_val_519);
            end;
        end;
        for for_key_520, for_val_520 in ipairs(Array_258) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1192, b_1192, c_1192, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4189

        end;
        for for_key_521, for_val_521 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_521 ~= nil) then -- ran, if id: 4190
                Array_259 = {};
                table.insert(Array_259, for_val_521);
            end;
        end;
        for for_key_522, for_val_522 in ipairs(Array_259) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1193, b_1193, c_1193, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4191

        end;
        for for_key_523, for_val_523 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_523 ~= nil) then -- ran, if id: 4192
                Array_260 = {};
                table.insert(Array_260, for_val_523);
            end;
        end;
        for for_key_524, for_val_524 in ipairs(Array_260) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1194, b_1194, c_1194, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4193

        end;
        for for_key_525, for_val_525 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_525 ~= nil) then -- ran, if id: 4194
                Array_261 = {};
                table.insert(Array_261, for_val_525);
            end;
        end;
        for for_key_526, for_val_526 in ipairs(Array_261) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1195, b_1195, c_1195, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4195

        end;
        for for_key_527, for_val_527 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_527 ~= nil) then -- ran, if id: 4196
                Array_262 = {};
                table.insert(Array_262, for_val_527);
            end;
        end;
        for for_key_528, for_val_528 in ipairs(Array_262) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1196, b_1196, c_1196, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4197

        end;
        for for_key_529, for_val_529 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_529 ~= nil) then -- ran, if id: 4198
                Array_263 = {};
                table.insert(Array_263, for_val_529);
            end;
        end;
        for for_key_530, for_val_530 in ipairs(Array_263) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1197, b_1197, c_1197, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4199

        end;
        for for_key_531, for_val_531 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_531 ~= nil) then -- ran, if id: 4200
                Array_264 = {};
                table.insert(Array_264, for_val_531);
            end;
        end;
        for for_key_532, for_val_532 in ipairs(Array_264) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1198, b_1198, c_1198, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4201

        end;
        for for_key_533, for_val_533 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_533 ~= nil) then -- ran, if id: 4202
                Array_265 = {};
                table.insert(Array_265, for_val_533);
            end;
        end;
        for for_key_534, for_val_534 in ipairs(Array_265) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1199, b_1199, c_1199, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4203

        end;
        for for_key_535, for_val_535 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_535 ~= nil) then -- ran, if id: 4204
                Array_266 = {};
                table.insert(Array_266, for_val_535);
            end;
        end;
        for for_key_536, for_val_536 in ipairs(Array_266) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1200, b_1200, c_1200, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4205

        end;
        for for_key_537, for_val_537 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_537 ~= nil) then -- ran, if id: 4206
                Array_267 = {};
                table.insert(Array_267, for_val_537);
            end;
        end;
        for for_key_538, for_val_538 in ipairs(Array_267) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1201, b_1201, c_1201, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4207

        end;
        for for_key_539, for_val_539 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_539 ~= nil) then -- ran, if id: 4208
                Array_268 = {};
                table.insert(Array_268, for_val_539);
            end;
        end;
        for for_key_540, for_val_540 in ipairs(Array_268) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1202, b_1202, c_1202, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4209

        end;
        for for_key_541, for_val_541 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_541 ~= nil) then -- ran, if id: 4210
                Array_269 = {};
                table.insert(Array_269, for_val_541);
            end;
        end;
        for for_key_542, for_val_542 in ipairs(Array_269) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1203, b_1203, c_1203, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4211

        end;
        for for_key_543, for_val_543 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_543 ~= nil) then -- ran, if id: 4212
                Array_270 = {};
                table.insert(Array_270, for_val_543);
            end;
        end;
        for for_key_544, for_val_544 in ipairs(Array_270) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1204, b_1204, c_1204, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4213

        end;
        for for_key_545, for_val_545 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_545 ~= nil) then -- ran, if id: 4214
                Array_271 = {};
                table.insert(Array_271, for_val_545);
            end;
        end;
        for for_key_546, for_val_546 in ipairs(Array_271) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1205, b_1205, c_1205, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4215

        end;
        for for_key_547, for_val_547 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_547 ~= nil) then -- ran, if id: 4216
                Array_272 = {};
                table.insert(Array_272, for_val_547);
            end;
        end;
        for for_key_548, for_val_548 in ipairs(Array_272) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1206, b_1206, c_1206, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4217

        end;
        for for_key_549, for_val_549 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_549 ~= nil) then -- ran, if id: 4218
                Array_273 = {};
                table.insert(Array_273, for_val_549);
            end;
        end;
        for for_key_550, for_val_550 in ipairs(Array_273) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1207, b_1207, c_1207, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4219

        end;
        for for_key_551, for_val_551 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_551 ~= nil) then -- ran, if id: 4220
                Array_274 = {};
                table.insert(Array_274, for_val_551);
            end;
        end;
        for for_key_552, for_val_552 in ipairs(Array_274) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1208, b_1208, c_1208, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4221

        end;
        for for_key_553, for_val_553 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_553 ~= nil) then -- ran, if id: 4222
                Array_275 = {};
                table.insert(Array_275, for_val_553);
            end;
        end;
        for for_key_554, for_val_554 in ipairs(Array_275) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1209, b_1209, c_1209, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4223

        end;
        for for_key_555, for_val_555 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_555 ~= nil) then -- ran, if id: 4224
                Array_276 = {};
                table.insert(Array_276, for_val_555);
            end;
        end;
        for for_key_556, for_val_556 in ipairs(Array_276) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1210, b_1210, c_1210, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4225

        end;
        for for_key_557, for_val_557 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_557 ~= nil) then -- ran, if id: 4226
                Array_277 = {};
                table.insert(Array_277, for_val_557);
            end;
        end;
        for for_key_558, for_val_558 in ipairs(Array_277) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1211, b_1211, c_1211, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4227

        end;
        for for_key_559, for_val_559 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_559 ~= nil) then -- ran, if id: 4228
                Array_278 = {};
                table.insert(Array_278, for_val_559);
            end;
        end;
        for for_key_560, for_val_560 in ipairs(Array_278) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1212, b_1212, c_1212, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4229

        end;
        for for_key_561, for_val_561 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_561 ~= nil) then -- ran, if id: 4230
                Array_279 = {};
                table.insert(Array_279, for_val_561);
            end;
        end;
        for for_key_562, for_val_562 in ipairs(Array_279) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1213, b_1213, c_1213, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4231

        end;
        for for_key_563, for_val_563 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_563 ~= nil) then -- ran, if id: 4232
                Array_280 = {};
                table.insert(Array_280, for_val_563);
            end;
        end;
        for for_key_564, for_val_564 in ipairs(Array_280) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1214, b_1214, c_1214, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4233

        end;
        for for_key_565, for_val_565 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_565 ~= nil) then -- ran, if id: 4234
                Array_281 = {};
                table.insert(Array_281, for_val_565);
            end;
        end;
        for for_key_566, for_val_566 in ipairs(Array_281) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1215, b_1215, c_1215, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4235

        end;
        for for_key_567, for_val_567 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_567 ~= nil) then -- ran, if id: 4236
                Array_282 = {};
                table.insert(Array_282, for_val_567);
            end;
        end;
        for for_key_568, for_val_568 in ipairs(Array_282) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1216, b_1216, c_1216, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4237

        end;
        for for_key_569, for_val_569 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_569 ~= nil) then -- ran, if id: 4238
                Array_283 = {};
                table.insert(Array_283, for_val_569);
            end;
        end;
        for for_key_570, for_val_570 in ipairs(Array_283) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1217, b_1217, c_1217, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4239

        end;
        for for_key_571, for_val_571 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_571 ~= nil) then -- ran, if id: 4240
                Array_284 = {};
                table.insert(Array_284, for_val_571);
            end;
        end;
        for for_key_572, for_val_572 in ipairs(Array_284) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1218, b_1218, c_1218, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4241

        end;
        for for_key_573, for_val_573 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_573 ~= nil) then -- ran, if id: 4242
                Array_285 = {};
                table.insert(Array_285, for_val_573);
            end;
        end;
        for for_key_574, for_val_574 in ipairs(Array_285) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1219, b_1219, c_1219, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4243

        end;
        for for_key_575, for_val_575 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_575 ~= nil) then -- ran, if id: 4244
                Array_286 = {};
                table.insert(Array_286, for_val_575);
            end;
        end;
        for for_key_576, for_val_576 in ipairs(Array_286) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1220, b_1220, c_1220, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4245

        end;
        for for_key_577, for_val_577 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_577 ~= nil) then -- ran, if id: 4246
                Array_287 = {};
                table.insert(Array_287, for_val_577);
            end;
        end;
        for for_key_578, for_val_578 in ipairs(Array_287) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1221, b_1221, c_1221, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4247

        end;
        for for_key_579, for_val_579 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_579 ~= nil) then -- ran, if id: 4248
                Array_288 = {};
                table.insert(Array_288, for_val_579);
            end;
        end;
        for for_key_580, for_val_580 in ipairs(Array_288) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1222, b_1222, c_1222, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4249

        end;
        for for_key_581, for_val_581 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_581 ~= nil) then -- ran, if id: 4250
                Array_289 = {};
                table.insert(Array_289, for_val_581);
            end;
        end;
        for for_key_582, for_val_582 in ipairs(Array_289) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1223, b_1223, c_1223, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4251

        end;
        for for_key_583, for_val_583 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_583 ~= nil) then -- ran, if id: 4252
                Array_290 = {};
                table.insert(Array_290, for_val_583);
            end;
        end;
        for for_key_584, for_val_584 in ipairs(Array_290) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1224, b_1224, c_1224, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4253

        end;
        for for_key_585, for_val_585 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_585 ~= nil) then -- ran, if id: 4254
                Array_291 = {};
                table.insert(Array_291, for_val_585);
            end;
        end;
        for for_key_586, for_val_586 in ipairs(Array_291) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1225, b_1225, c_1225, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4255

        end;
        for for_key_587, for_val_587 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_587 ~= nil) then -- ran, if id: 4256
                Array_292 = {};
                table.insert(Array_292, for_val_587);
            end;
        end;
        for for_key_588, for_val_588 in ipairs(Array_292) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1226, b_1226, c_1226, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4257

        end;
        for for_key_589, for_val_589 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_589 ~= nil) then -- ran, if id: 4258
                Array_293 = {};
                table.insert(Array_293, for_val_589);
            end;
        end;
        for for_key_590, for_val_590 in ipairs(Array_293) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1227, b_1227, c_1227, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4259

        end;
        for for_key_591, for_val_591 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_591 ~= nil) then -- ran, if id: 4260
                Array_294 = {};
                table.insert(Array_294, for_val_591);
            end;
        end;
        for for_key_592, for_val_592 in ipairs(Array_294) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1228, b_1228, c_1228, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4261

        end;
        for for_key_593, for_val_593 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_593 ~= nil) then -- ran, if id: 4262
                Array_295 = {};
                table.insert(Array_295, for_val_593);
            end;
        end;
        for for_key_594, for_val_594 in ipairs(Array_295) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1229, b_1229, c_1229, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4263

        end;
        for for_key_595, for_val_595 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_595 ~= nil) then -- ran, if id: 4264
                Array_296 = {};
                table.insert(Array_296, for_val_595);
            end;
        end;
        for for_key_596, for_val_596 in ipairs(Array_296) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1230, b_1230, c_1230, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4265

        end;
        for for_key_597, for_val_597 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_597 ~= nil) then -- ran, if id: 4266
                Array_297 = {};
                table.insert(Array_297, for_val_597);
            end;
        end;
        for for_key_598, for_val_598 in ipairs(Array_297) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1231, b_1231, c_1231, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4267

        end;
        for for_key_599, for_val_599 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_599 ~= nil) then -- ran, if id: 4268
                Array_298 = {};
                table.insert(Array_298, for_val_599);
            end;
        end;
        for for_key_600, for_val_600 in ipairs(Array_298) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1232, b_1232, c_1232, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4269

        end;
        for for_key_601, for_val_601 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_601 ~= nil) then -- ran, if id: 4270
                Array_299 = {};
                table.insert(Array_299, for_val_601);
            end;
        end;
        for for_key_602, for_val_602 in ipairs(Array_299) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1233, b_1233, c_1233, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4271

        end;
        for for_key_603, for_val_603 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_603 ~= nil) then -- ran, if id: 4272
                Array_300 = {};
                table.insert(Array_300, for_val_603);
            end;
        end;
        for for_key_604, for_val_604 in ipairs(Array_300) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1234, b_1234, c_1234, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4273

        end;
        for for_key_605, for_val_605 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_605 ~= nil) then -- ran, if id: 4274
                Array_301 = {};
                table.insert(Array_301, for_val_605);
            end;
        end;
        for for_key_606, for_val_606 in ipairs(Array_301) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1235, b_1235, c_1235, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4275

        end;
        for for_key_607, for_val_607 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_607 ~= nil) then -- ran, if id: 4276
                Array_302 = {};
                table.insert(Array_302, for_val_607);
            end;
        end;
        for for_key_608, for_val_608 in ipairs(Array_302) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1236, b_1236, c_1236, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4277

        end;
        for for_key_609, for_val_609 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_609 ~= nil) then -- ran, if id: 4278
                Array_303 = {};
                table.insert(Array_303, for_val_609);
            end;
        end;
        for for_key_610, for_val_610 in ipairs(Array_303) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1237, b_1237, c_1237, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4279

        end;
        for for_key_611, for_val_611 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_611 ~= nil) then -- ran, if id: 4280
                Array_304 = {};
                table.insert(Array_304, for_val_611);
            end;
        end;
        for for_key_612, for_val_612 in ipairs(Array_304) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1238, b_1238, c_1238, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4281

        end;
        for for_key_613, for_val_613 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_613 ~= nil) then -- ran, if id: 4282
                Array_305 = {};
                table.insert(Array_305, for_val_613);
            end;
        end;
        for for_key_614, for_val_614 in ipairs(Array_305) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1239, b_1239, c_1239, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4283

        end;
        for for_key_615, for_val_615 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_615 ~= nil) then -- ran, if id: 4284
                Array_306 = {};
                table.insert(Array_306, for_val_615);
            end;
        end;
        for for_key_616, for_val_616 in ipairs(Array_306) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1240, b_1240, c_1240, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4285

        end;
        for for_key_617, for_val_617 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_617 ~= nil) then -- ran, if id: 4286
                Array_307 = {};
                table.insert(Array_307, for_val_617);
            end;
        end;
        for for_key_618, for_val_618 in ipairs(Array_307) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1241, b_1241, c_1241, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4287

        end;
        for for_key_619, for_val_619 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_619 ~= nil) then -- ran, if id: 4288
                Array_308 = {};
                table.insert(Array_308, for_val_619);
            end;
        end;
        for for_key_620, for_val_620 in ipairs(Array_308) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1242, b_1242, c_1242, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4289

        end;
        for for_key_621, for_val_621 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_621 ~= nil) then -- ran, if id: 4290
                Array_309 = {};
                table.insert(Array_309, for_val_621);
            end;
        end;
        for for_key_622, for_val_622 in ipairs(Array_309) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1243, b_1243, c_1243, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4291

        end;
        for for_key_623, for_val_623 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_623 ~= nil) then -- ran, if id: 4292
                Array_310 = {};
                table.insert(Array_310, for_val_623);
            end;
        end;
        for for_key_624, for_val_624 in ipairs(Array_310) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1244, b_1244, c_1244, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4293

        end;
        for for_key_625, for_val_625 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_625 ~= nil) then -- ran, if id: 4294
                Array_311 = {};
                table.insert(Array_311, for_val_625);
            end;
        end;
        for for_key_626, for_val_626 in ipairs(Array_311) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1245, b_1245, c_1245, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4295

        end;
        for for_key_627, for_val_627 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_627 ~= nil) then -- ran, if id: 4296
                Array_312 = {};
                table.insert(Array_312, for_val_627);
            end;
        end;
        for for_key_628, for_val_628 in ipairs(Array_312) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1246, b_1246, c_1246, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4297

        end;
        for for_key_629, for_val_629 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_629 ~= nil) then -- ran, if id: 4298
                Array_313 = {};
                table.insert(Array_313, for_val_629);
            end;
        end;
        for for_key_630, for_val_630 in ipairs(Array_313) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1247, b_1247, c_1247, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4299

        end;
        for for_key_631, for_val_631 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_631 ~= nil) then -- ran, if id: 4300
                Array_314 = {};
                table.insert(Array_314, for_val_631);
            end;
        end;
        for for_key_632, for_val_632 in ipairs(Array_314) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1248, b_1248, c_1248, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4301

        end;
        for for_key_633, for_val_633 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_633 ~= nil) then -- ran, if id: 4302
                Array_315 = {};
                table.insert(Array_315, for_val_633);
            end;
        end;
        for for_key_634, for_val_634 in ipairs(Array_315) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1249, b_1249, c_1249, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4303

        end;
        for for_key_635, for_val_635 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_635 ~= nil) then -- ran, if id: 4304
                Array_316 = {};
                table.insert(Array_316, for_val_635);
            end;
        end;
        for for_key_636, for_val_636 in ipairs(Array_316) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1250, b_1250, c_1250, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4305

        end;
        for for_key_637, for_val_637 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_637 ~= nil) then -- ran, if id: 4306
                Array_317 = {};
                table.insert(Array_317, for_val_637);
            end;
        end;
        for for_key_638, for_val_638 in ipairs(Array_317) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1251, b_1251, c_1251, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4307

        end;
        for for_key_639, for_val_639 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_639 ~= nil) then -- ran, if id: 4308
                Array_318 = {};
                table.insert(Array_318, for_val_639);
            end;
        end;
        for for_key_640, for_val_640 in ipairs(Array_318) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1252, b_1252, c_1252, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4309

        end;
        for for_key_641, for_val_641 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_641 ~= nil) then -- ran, if id: 4310
                Array_319 = {};
                table.insert(Array_319, for_val_641);
            end;
        end;
        for for_key_642, for_val_642 in ipairs(Array_319) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1253, b_1253, c_1253, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4311

        end;
        for for_key_643, for_val_643 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_643 ~= nil) then -- ran, if id: 4312
                Array_320 = {};
                table.insert(Array_320, for_val_643);
            end;
        end;
        for for_key_644, for_val_644 in ipairs(Array_320) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1254, b_1254, c_1254, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4313

        end;
        for for_key_645, for_val_645 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_645 ~= nil) then -- ran, if id: 4314
                Array_321 = {};
                table.insert(Array_321, for_val_645);
            end;
        end;
        for for_key_646, for_val_646 in ipairs(Array_321) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1255, b_1255, c_1255, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4315

        end;
        for for_key_647, for_val_647 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_647 ~= nil) then -- ran, if id: 4316
                Array_322 = {};
                table.insert(Array_322, for_val_647);
            end;
        end;
        for for_key_648, for_val_648 in ipairs(Array_322) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1256, b_1256, c_1256, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4317

        end;
        for for_key_649, for_val_649 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_649 ~= nil) then -- ran, if id: 4318
                Array_323 = {};
                table.insert(Array_323, for_val_649);
            end;
        end;
        for for_key_650, for_val_650 in ipairs(Array_323) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1257, b_1257, c_1257, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4319

        end;
        for for_key_651, for_val_651 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_651 ~= nil) then -- ran, if id: 4320
                Array_324 = {};
                table.insert(Array_324, for_val_651);
            end;
        end;
        for for_key_652, for_val_652 in ipairs(Array_324) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1258, b_1258, c_1258, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4321

        end;
        for for_key_653, for_val_653 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_653 ~= nil) then -- ran, if id: 4322
                Array_325 = {};
                table.insert(Array_325, for_val_653);
            end;
        end;
        for for_key_654, for_val_654 in ipairs(Array_325) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1259, b_1259, c_1259, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4323

        end;
        for for_key_655, for_val_655 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_655 ~= nil) then -- ran, if id: 4324
                Array_326 = {};
                table.insert(Array_326, for_val_655);
            end;
        end;
        for for_key_656, for_val_656 in ipairs(Array_326) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1260, b_1260, c_1260, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4325

        end;
        for for_key_657, for_val_657 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_657 ~= nil) then -- ran, if id: 4326
                Array_327 = {};
                table.insert(Array_327, for_val_657);
            end;
        end;
        for for_key_658, for_val_658 in ipairs(Array_327) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1261, b_1261, c_1261, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4327

        end;
        for for_key_659, for_val_659 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_659 ~= nil) then -- ran, if id: 4328
                Array_328 = {};
                table.insert(Array_328, for_val_659);
            end;
        end;
        for for_key_660, for_val_660 in ipairs(Array_328) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1262, b_1262, c_1262, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4329

        end;
        for for_key_661, for_val_661 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_661 ~= nil) then -- ran, if id: 4330
                Array_329 = {};
                table.insert(Array_329, for_val_661);
            end;
        end;
        for for_key_662, for_val_662 in ipairs(Array_329) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1263, b_1263, c_1263, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4331

        end;
        for for_key_663, for_val_663 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_663 ~= nil) then -- ran, if id: 4332
                Array_330 = {};
                table.insert(Array_330, for_val_663);
            end;
        end;
        for for_key_664, for_val_664 in ipairs(Array_330) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1264, b_1264, c_1264, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4333

        end;
        for for_key_665, for_val_665 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_665 ~= nil) then -- ran, if id: 4334
                Array_331 = {};
                table.insert(Array_331, for_val_665);
            end;
        end;
        for for_key_666, for_val_666 in ipairs(Array_331) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1265, b_1265, c_1265, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4335

        end;
        for for_key_667, for_val_667 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_667 ~= nil) then -- ran, if id: 4336
                Array_332 = {};
                table.insert(Array_332, for_val_667);
            end;
        end;
        for for_key_668, for_val_668 in ipairs(Array_332) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1266, b_1266, c_1266, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4337

        end;
        for for_key_669, for_val_669 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_669 ~= nil) then -- ran, if id: 4338
                Array_333 = {};
                table.insert(Array_333, for_val_669);
            end;
        end;
        for for_key_670, for_val_670 in ipairs(Array_333) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1267, b_1267, c_1267, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4339

        end;
        for for_key_671, for_val_671 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_671 ~= nil) then -- ran, if id: 4340
                Array_334 = {};
                table.insert(Array_334, for_val_671);
            end;
        end;
        for for_key_672, for_val_672 in ipairs(Array_334) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1268, b_1268, c_1268, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4341

        end;
        for for_key_673, for_val_673 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_673 ~= nil) then -- ran, if id: 4342
                Array_335 = {};
                table.insert(Array_335, for_val_673);
            end;
        end;
        for for_key_674, for_val_674 in ipairs(Array_335) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1269, b_1269, c_1269, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4343

        end;
        for for_key_675, for_val_675 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_675 ~= nil) then -- ran, if id: 4344
                Array_336 = {};
                table.insert(Array_336, for_val_675);
            end;
        end;
        for for_key_676, for_val_676 in ipairs(Array_336) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1270, b_1270, c_1270, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4345

        end;
        for for_key_677, for_val_677 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_677 ~= nil) then -- ran, if id: 4346
                Array_337 = {};
                table.insert(Array_337, for_val_677);
            end;
        end;
        for for_key_678, for_val_678 in ipairs(Array_337) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1271, b_1271, c_1271, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4347

        end;
        for for_key_679, for_val_679 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_679 ~= nil) then -- ran, if id: 4348
                Array_338 = {};
                table.insert(Array_338, for_val_679);
            end;
        end;
        for for_key_680, for_val_680 in ipairs(Array_338) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1272, b_1272, c_1272, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4349

        end;
        for for_key_681, for_val_681 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_681 ~= nil) then -- ran, if id: 4350
                Array_339 = {};
                table.insert(Array_339, for_val_681);
            end;
        end;
        for for_key_682, for_val_682 in ipairs(Array_339) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1273, b_1273, c_1273, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4351

        end;
        for for_key_683, for_val_683 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_683 ~= nil) then -- ran, if id: 4352
                Array_340 = {};
                table.insert(Array_340, for_val_683);
            end;
        end;
        for for_key_684, for_val_684 in ipairs(Array_340) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1274, b_1274, c_1274, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4353

        end;
        for for_key_685, for_val_685 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_685 ~= nil) then -- ran, if id: 4354
                Array_341 = {};
                table.insert(Array_341, for_val_685);
            end;
        end;
        for for_key_686, for_val_686 in ipairs(Array_341) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1275, b_1275, c_1275, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4355

        end;
        for for_key_687, for_val_687 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_687 ~= nil) then -- ran, if id: 4356
                Array_342 = {};
                table.insert(Array_342, for_val_687);
            end;
        end;
        for for_key_688, for_val_688 in ipairs(Array_342) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1276, b_1276, c_1276, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4357

        end;
        for for_key_689, for_val_689 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_689 ~= nil) then -- ran, if id: 4358
                Array_343 = {};
                table.insert(Array_343, for_val_689);
            end;
        end;
        for for_key_690, for_val_690 in ipairs(Array_343) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1277, b_1277, c_1277, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4359

        end;
        for for_key_691, for_val_691 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_691 ~= nil) then -- ran, if id: 4360
                Array_344 = {};
                table.insert(Array_344, for_val_691);
            end;
        end;
        for for_key_692, for_val_692 in ipairs(Array_344) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1278, b_1278, c_1278, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4361

        end;
        for for_key_693, for_val_693 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_693 ~= nil) then -- ran, if id: 4362
                Array_345 = {};
                table.insert(Array_345, for_val_693);
            end;
        end;
        for for_key_694, for_val_694 in ipairs(Array_345) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1279, b_1279, c_1279, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4363

        end;
        for for_key_695, for_val_695 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_695 ~= nil) then -- ran, if id: 4364
                Array_346 = {};
                table.insert(Array_346, for_val_695);
            end;
        end;
        for for_key_696, for_val_696 in ipairs(Array_346) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1280, b_1280, c_1280, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4365

        end;
        for for_key_697, for_val_697 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_697 ~= nil) then -- ran, if id: 4366
                Array_347 = {};
                table.insert(Array_347, for_val_697);
            end;
        end;
        for for_key_698, for_val_698 in ipairs(Array_347) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1281, b_1281, c_1281, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4367

        end;
        for for_key_699, for_val_699 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_699 ~= nil) then -- ran, if id: 4368
                Array_348 = {};
                table.insert(Array_348, for_val_699);
            end;
        end;
        for for_key_700, for_val_700 in ipairs(Array_348) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1282, b_1282, c_1282, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4369

        end;
        for for_key_701, for_val_701 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_701 ~= nil) then -- ran, if id: 4370
                Array_349 = {};
                table.insert(Array_349, for_val_701);
            end;
        end;
        for for_key_702, for_val_702 in ipairs(Array_349) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1283, b_1283, c_1283, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4371

        end;
        for for_key_703, for_val_703 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_703 ~= nil) then -- ran, if id: 4372
                Array_350 = {};
                table.insert(Array_350, for_val_703);
            end;
        end;
        for for_key_704, for_val_704 in ipairs(Array_350) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1284, b_1284, c_1284, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4373

        end;
        for for_key_705, for_val_705 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_705 ~= nil) then -- ran, if id: 4374
                Array_351 = {};
                table.insert(Array_351, for_val_705);
            end;
        end;
        for for_key_706, for_val_706 in ipairs(Array_351) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1285, b_1285, c_1285, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4375

        end;
        for for_key_707, for_val_707 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_707 ~= nil) then -- ran, if id: 4376
                Array_352 = {};
                table.insert(Array_352, for_val_707);
            end;
        end;
        for for_key_708, for_val_708 in ipairs(Array_352) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1286, b_1286, c_1286, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4377

        end;
        for for_key_709, for_val_709 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_709 ~= nil) then -- ran, if id: 4378
                Array_353 = {};
                table.insert(Array_353, for_val_709);
            end;
        end;
        for for_key_710, for_val_710 in ipairs(Array_353) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1287, b_1287, c_1287, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4379

        end;
        for for_key_711, for_val_711 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_711 ~= nil) then -- ran, if id: 4380
                Array_354 = {};
                table.insert(Array_354, for_val_711);
            end;
        end;
        for for_key_712, for_val_712 in ipairs(Array_354) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1288, b_1288, c_1288, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4381

        end;
        for for_key_713, for_val_713 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_713 ~= nil) then -- ran, if id: 4382
                Array_355 = {};
                table.insert(Array_355, for_val_713);
            end;
        end;
        for for_key_714, for_val_714 in ipairs(Array_355) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1289, b_1289, c_1289, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4383

        end;
        for for_key_715, for_val_715 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_715 ~= nil) then -- ran, if id: 4384
                Array_356 = {};
                table.insert(Array_356, for_val_715);
            end;
        end;
        for for_key_716, for_val_716 in ipairs(Array_356) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1290, b_1290, c_1290, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4385

        end;
        for for_key_717, for_val_717 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_717 ~= nil) then -- ran, if id: 4386
                Array_357 = {};
                table.insert(Array_357, for_val_717);
            end;
        end;
        for for_key_718, for_val_718 in ipairs(Array_357) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1291, b_1291, c_1291, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4387

        end;
        for for_key_719, for_val_719 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_719 ~= nil) then -- ran, if id: 4388
                Array_358 = {};
                table.insert(Array_358, for_val_719);
            end;
        end;
        for for_key_720, for_val_720 in ipairs(Array_358) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1292, b_1292, c_1292, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4389

        end;
        for for_key_721, for_val_721 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_721 ~= nil) then -- ran, if id: 4390
                Array_359 = {};
                table.insert(Array_359, for_val_721);
            end;
        end;
        for for_key_722, for_val_722 in ipairs(Array_359) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1293, b_1293, c_1293, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4391

        end;
        for for_key_723, for_val_723 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_723 ~= nil) then -- ran, if id: 4392
                Array_360 = {};
                table.insert(Array_360, for_val_723);
            end;
        end;
        for for_key_724, for_val_724 in ipairs(Array_360) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1294, b_1294, c_1294, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4393

        end;
        for for_key_725, for_val_725 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_725 ~= nil) then -- ran, if id: 4394
                Array_361 = {};
                table.insert(Array_361, for_val_725);
            end;
        end;
        for for_key_726, for_val_726 in ipairs(Array_361) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1295, b_1295, c_1295, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4395

        end;
        for for_key_727, for_val_727 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_727 ~= nil) then -- ran, if id: 4396
                Array_362 = {};
                table.insert(Array_362, for_val_727);
            end;
        end;
        for for_key_728, for_val_728 in ipairs(Array_362) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1296, b_1296, c_1296, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4397

        end;
        for for_key_729, for_val_729 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_729 ~= nil) then -- ran, if id: 4398
                Array_363 = {};
                table.insert(Array_363, for_val_729);
            end;
        end;
        for for_key_730, for_val_730 in ipairs(Array_363) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1297, b_1297, c_1297, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4399

        end;
        for for_key_731, for_val_731 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_731 ~= nil) then -- ran, if id: 4400
                Array_364 = {};
                table.insert(Array_364, for_val_731);
            end;
        end;
        for for_key_732, for_val_732 in ipairs(Array_364) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1298, b_1298, c_1298, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4401

        end;
        for for_key_733, for_val_733 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_733 ~= nil) then -- ran, if id: 4402
                Array_365 = {};
                table.insert(Array_365, for_val_733);
            end;
        end;
        for for_key_734, for_val_734 in ipairs(Array_365) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1299, b_1299, c_1299, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4403

        end;
        for for_key_735, for_val_735 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_735 ~= nil) then -- ran, if id: 4404
                Array_366 = {};
                table.insert(Array_366, for_val_735);
            end;
        end;
        for for_key_736, for_val_736 in ipairs(Array_366) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1300, b_1300, c_1300, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4405

        end;
        for for_key_737, for_val_737 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_737 ~= nil) then -- ran, if id: 4406
                Array_367 = {};
                table.insert(Array_367, for_val_737);
            end;
        end;
        for for_key_738, for_val_738 in ipairs(Array_367) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1301, b_1301, c_1301, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4407

        end;
        for for_key_739, for_val_739 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_739 ~= nil) then -- ran, if id: 4408
                Array_368 = {};
                table.insert(Array_368, for_val_739);
            end;
        end;
        for for_key_740, for_val_740 in ipairs(Array_368) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1302, b_1302, c_1302, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4409

        end;
        for for_key_741, for_val_741 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_741 ~= nil) then -- ran, if id: 4410
                Array_369 = {};
                table.insert(Array_369, for_val_741);
            end;
        end;
        for for_key_742, for_val_742 in ipairs(Array_369) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1303, b_1303, c_1303, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4411

        end;
        for for_key_743, for_val_743 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_743 ~= nil) then -- ran, if id: 4412
                Array_370 = {};
                table.insert(Array_370, for_val_743);
            end;
        end;
        for for_key_744, for_val_744 in ipairs(Array_370) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1304, b_1304, c_1304, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4413

        end;
        for for_key_745, for_val_745 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_745 ~= nil) then -- ran, if id: 4414
                Array_371 = {};
                table.insert(Array_371, for_val_745);
            end;
        end;
        for for_key_746, for_val_746 in ipairs(Array_371) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1305, b_1305, c_1305, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4415

        end;
        for for_key_747, for_val_747 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_747 ~= nil) then -- ran, if id: 4416
                Array_372 = {};
                table.insert(Array_372, for_val_747);
            end;
        end;
        for for_key_748, for_val_748 in ipairs(Array_372) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1306, b_1306, c_1306, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4417

        end;
        for for_key_749, for_val_749 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_749 ~= nil) then -- ran, if id: 4418
                Array_373 = {};
                table.insert(Array_373, for_val_749);
            end;
        end;
        for for_key_750, for_val_750 in ipairs(Array_373) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1307, b_1307, c_1307, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4419

        end;
        for for_key_751, for_val_751 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_751 ~= nil) then -- ran, if id: 4420
                Array_374 = {};
                table.insert(Array_374, for_val_751);
            end;
        end;
        for for_key_752, for_val_752 in ipairs(Array_374) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1308, b_1308, c_1308, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4421

        end;
        for for_key_753, for_val_753 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_753 ~= nil) then -- ran, if id: 4422
                Array_375 = {};
                table.insert(Array_375, for_val_753);
            end;
        end;
        for for_key_754, for_val_754 in ipairs(Array_375) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1309, b_1309, c_1309, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4423

        end;
        for for_key_755, for_val_755 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_755 ~= nil) then -- ran, if id: 4424
                Array_376 = {};
                table.insert(Array_376, for_val_755);
            end;
        end;
        for for_key_756, for_val_756 in ipairs(Array_376) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1310, b_1310, c_1310, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4425

        end;
        for for_key_757, for_val_757 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_757 ~= nil) then -- ran, if id: 4426
                Array_377 = {};
                table.insert(Array_377, for_val_757);
            end;
        end;
        for for_key_758, for_val_758 in ipairs(Array_377) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1311, b_1311, c_1311, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4427

        end;
        for for_key_759, for_val_759 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_759 ~= nil) then -- ran, if id: 4428
                Array_378 = {};
                table.insert(Array_378, for_val_759);
            end;
        end;
        for for_key_760, for_val_760 in ipairs(Array_378) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1312, b_1312, c_1312, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4429

        end;
        for for_key_761, for_val_761 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_761 ~= nil) then -- ran, if id: 4430
                Array_379 = {};
                table.insert(Array_379, for_val_761);
            end;
        end;
        for for_key_762, for_val_762 in ipairs(Array_379) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1313, b_1313, c_1313, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4431

        end;
        for for_key_763, for_val_763 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_763 ~= nil) then -- ran, if id: 4432
                Array_380 = {};
                table.insert(Array_380, for_val_763);
            end;
        end;
        for for_key_764, for_val_764 in ipairs(Array_380) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1314, b_1314, c_1314, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4433

        end;
        for for_key_765, for_val_765 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_765 ~= nil) then -- ran, if id: 4434
                Array_381 = {};
                table.insert(Array_381, for_val_765);
            end;
        end;
        for for_key_766, for_val_766 in ipairs(Array_381) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1315, b_1315, c_1315, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4435

        end;
        for for_key_767, for_val_767 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_767 ~= nil) then -- ran, if id: 4436
                Array_382 = {};
                table.insert(Array_382, for_val_767);
            end;
        end;
        for for_key_768, for_val_768 in ipairs(Array_382) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1316, b_1316, c_1316, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4437

        end;
        for for_key_769, for_val_769 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_769 ~= nil) then -- ran, if id: 4438
                Array_383 = {};
                table.insert(Array_383, for_val_769);
            end;
        end;
        for for_key_770, for_val_770 in ipairs(Array_383) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1317, b_1317, c_1317, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4439

        end;
        for for_key_771, for_val_771 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_771 ~= nil) then -- ran, if id: 4440
                Array_384 = {};
                table.insert(Array_384, for_val_771);
            end;
        end;
        for for_key_772, for_val_772 in ipairs(Array_384) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1318, b_1318, c_1318, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4441

        end;
        for for_key_773, for_val_773 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_773 ~= nil) then -- ran, if id: 4442
                Array_385 = {};
                table.insert(Array_385, for_val_773);
            end;
        end;
        for for_key_774, for_val_774 in ipairs(Array_385) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1319, b_1319, c_1319, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4443

        end;
        for for_key_775, for_val_775 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_775 ~= nil) then -- ran, if id: 4444
                Array_386 = {};
                table.insert(Array_386, for_val_775);
            end;
        end;
        for for_key_776, for_val_776 in ipairs(Array_386) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1320, b_1320, c_1320, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4445

        end;
        for for_key_777, for_val_777 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_777 ~= nil) then -- ran, if id: 4446
                Array_387 = {};
                table.insert(Array_387, for_val_777);
            end;
        end;
        for for_key_778, for_val_778 in ipairs(Array_387) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1321, b_1321, c_1321, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4447

        end;
        for for_key_779, for_val_779 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_779 ~= nil) then -- ran, if id: 4448
                Array_388 = {};
                table.insert(Array_388, for_val_779);
            end;
        end;
        for for_key_780, for_val_780 in ipairs(Array_388) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1322, b_1322, c_1322, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4449

        end;
        for for_key_781, for_val_781 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_781 ~= nil) then -- ran, if id: 4450
                Array_389 = {};
                table.insert(Array_389, for_val_781);
            end;
        end;
        for for_key_782, for_val_782 in ipairs(Array_389) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1323, b_1323, c_1323, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4451

        end;
        for for_key_783, for_val_783 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_783 ~= nil) then -- ran, if id: 4452
                Array_390 = {};
                table.insert(Array_390, for_val_783);
            end;
        end;
        for for_key_784, for_val_784 in ipairs(Array_390) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1324, b_1324, c_1324, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4453

        end;
        for for_key_785, for_val_785 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_785 ~= nil) then -- ran, if id: 4454
                Array_391 = {};
                table.insert(Array_391, for_val_785);
            end;
        end;
        for for_key_786, for_val_786 in ipairs(Array_391) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1325, b_1325, c_1325, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4455

        end;
        for for_key_787, for_val_787 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_787 ~= nil) then -- ran, if id: 4456
                Array_392 = {};
                table.insert(Array_392, for_val_787);
            end;
        end;
        for for_key_788, for_val_788 in ipairs(Array_392) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1326, b_1326, c_1326, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4457

        end;
        for for_key_789, for_val_789 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_789 ~= nil) then -- ran, if id: 4458
                Array_393 = {};
                table.insert(Array_393, for_val_789);
            end;
        end;
        for for_key_790, for_val_790 in ipairs(Array_393) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1327, b_1327, c_1327, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4459

        end;
        for for_key_791, for_val_791 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_791 ~= nil) then -- ran, if id: 4460
                Array_394 = {};
                table.insert(Array_394, for_val_791);
            end;
        end;
        for for_key_792, for_val_792 in ipairs(Array_394) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1328, b_1328, c_1328, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4461

        end;
        for for_key_793, for_val_793 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_793 ~= nil) then -- ran, if id: 4462
                Array_395 = {};
                table.insert(Array_395, for_val_793);
            end;
        end;
        for for_key_794, for_val_794 in ipairs(Array_395) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1329, b_1329, c_1329, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4463

        end;
        for for_key_795, for_val_795 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_795 ~= nil) then -- ran, if id: 4464
                Array_396 = {};
                table.insert(Array_396, for_val_795);
            end;
        end;
        for for_key_796, for_val_796 in ipairs(Array_396) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1330, b_1330, c_1330, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4465

        end;
        for for_key_797, for_val_797 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_797 ~= nil) then -- ran, if id: 4466
                Array_397 = {};
                table.insert(Array_397, for_val_797);
            end;
        end;
        for for_key_798, for_val_798 in ipairs(Array_397) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1331, b_1331, c_1331, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4467

        end;
        for for_key_799, for_val_799 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_799 ~= nil) then -- ran, if id: 4468
                Array_398 = {};
                table.insert(Array_398, for_val_799);
            end;
        end;
        for for_key_800, for_val_800 in ipairs(Array_398) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1332, b_1332, c_1332, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4469

        end;
        for for_key_801, for_val_801 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_801 ~= nil) then -- ran, if id: 4470
                Array_399 = {};
                table.insert(Array_399, for_val_801);
            end;
        end;
        for for_key_802, for_val_802 in ipairs(Array_399) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1333, b_1333, c_1333, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4471

        end;
        for for_key_803, for_val_803 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_803 ~= nil) then -- ran, if id: 4472
                Array_400 = {};
                table.insert(Array_400, for_val_803);
            end;
        end;
        for for_key_804, for_val_804 in ipairs(Array_400) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1334, b_1334, c_1334, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4473

        end;
        for for_key_805, for_val_805 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_805 ~= nil) then -- ran, if id: 4474
                Array_401 = {};
                table.insert(Array_401, for_val_805);
            end;
        end;
        for for_key_806, for_val_806 in ipairs(Array_401) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1335, b_1335, c_1335, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4475

        end;
        for for_key_807, for_val_807 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_807 ~= nil) then -- ran, if id: 4476
                Array_402 = {};
                table.insert(Array_402, for_val_807);
            end;
        end;
        for for_key_808, for_val_808 in ipairs(Array_402) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1336, b_1336, c_1336, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4477

        end;
        for for_key_809, for_val_809 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_809 ~= nil) then -- ran, if id: 4478
                Array_403 = {};
                table.insert(Array_403, for_val_809);
            end;
        end;
        for for_key_810, for_val_810 in ipairs(Array_403) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1337, b_1337, c_1337, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4479

        end;
        for for_key_811, for_val_811 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_811 ~= nil) then -- ran, if id: 4480
                Array_404 = {};
                table.insert(Array_404, for_val_811);
            end;
        end;
        for for_key_812, for_val_812 in ipairs(Array_404) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1338, b_1338, c_1338, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4481

        end;
        for for_key_813, for_val_813 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_813 ~= nil) then -- ran, if id: 4482
                Array_405 = {};
                table.insert(Array_405, for_val_813);
            end;
        end;
        for for_key_814, for_val_814 in ipairs(Array_405) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1339, b_1339, c_1339, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4483

        end;
        for for_key_815, for_val_815 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_815 ~= nil) then -- ran, if id: 4484
                Array_406 = {};
                table.insert(Array_406, for_val_815);
            end;
        end;
        for for_key_816, for_val_816 in ipairs(Array_406) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1340, b_1340, c_1340, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4485

        end;
        for for_key_817, for_val_817 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_817 ~= nil) then -- ran, if id: 4486
                Array_407 = {};
                table.insert(Array_407, for_val_817);
            end;
        end;
        for for_key_818, for_val_818 in ipairs(Array_407) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1341, b_1341, c_1341, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4487

        end;
        for for_key_819, for_val_819 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_819 ~= nil) then -- ran, if id: 4488
                Array_408 = {};
                table.insert(Array_408, for_val_819);
            end;
        end;
        for for_key_820, for_val_820 in ipairs(Array_408) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1342, b_1342, c_1342, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4489

        end;
        for for_key_821, for_val_821 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_821 ~= nil) then -- ran, if id: 4490
                Array_409 = {};
                table.insert(Array_409, for_val_821);
            end;
        end;
        for for_key_822, for_val_822 in ipairs(Array_409) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1343, b_1343, c_1343, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4491

        end;
        for for_key_823, for_val_823 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_823 ~= nil) then -- ran, if id: 4492
                Array_410 = {};
                table.insert(Array_410, for_val_823);
            end;
        end;
        for for_key_824, for_val_824 in ipairs(Array_410) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1344, b_1344, c_1344, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4493

        end;
        for for_key_825, for_val_825 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_825 ~= nil) then -- ran, if id: 4494
                Array_411 = {};
                table.insert(Array_411, for_val_825);
            end;
        end;
        for for_key_826, for_val_826 in ipairs(Array_411) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1345, b_1345, c_1345, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4495

        end;
        for for_key_827, for_val_827 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_827 ~= nil) then -- ran, if id: 4496
                Array_412 = {};
                table.insert(Array_412, for_val_827);
            end;
        end;
        for for_key_828, for_val_828 in ipairs(Array_412) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1346, b_1346, c_1346, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4497

        end;
        for for_key_829, for_val_829 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_829 ~= nil) then -- ran, if id: 4498
                Array_413 = {};
                table.insert(Array_413, for_val_829);
            end;
        end;
        for for_key_830, for_val_830 in ipairs(Array_413) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1347, b_1347, c_1347, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4499

        end;
        for for_key_831, for_val_831 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_831 ~= nil) then -- ran, if id: 4500
                Array_414 = {};
                table.insert(Array_414, for_val_831);
            end;
        end;
        for for_key_832, for_val_832 in ipairs(Array_414) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1348, b_1348, c_1348, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4501

        end;
        for for_key_833, for_val_833 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_833 ~= nil) then -- ran, if id: 4502
                Array_415 = {};
                table.insert(Array_415, for_val_833);
            end;
        end;
        for for_key_834, for_val_834 in ipairs(Array_415) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1349, b_1349, c_1349, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4503

        end;
        for for_key_835, for_val_835 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_835 ~= nil) then -- ran, if id: 4504
                Array_416 = {};
                table.insert(Array_416, for_val_835);
            end;
        end;
        for for_key_836, for_val_836 in ipairs(Array_416) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1350, b_1350, c_1350, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4505

        end;
        for for_key_837, for_val_837 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_837 ~= nil) then -- ran, if id: 4506
                Array_417 = {};
                table.insert(Array_417, for_val_837);
            end;
        end;
        for for_key_838, for_val_838 in ipairs(Array_417) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1351, b_1351, c_1351, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4507

        end;
        for for_key_839, for_val_839 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_839 ~= nil) then -- ran, if id: 4508
                Array_418 = {};
                table.insert(Array_418, for_val_839);
            end;
        end;
        for for_key_840, for_val_840 in ipairs(Array_418) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1352, b_1352, c_1352, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4509

        end;
        for for_key_841, for_val_841 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_841 ~= nil) then -- ran, if id: 4510
                Array_419 = {};
                table.insert(Array_419, for_val_841);
            end;
        end;
        for for_key_842, for_val_842 in ipairs(Array_419) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1353, b_1353, c_1353, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4511

        end;
        for for_key_843, for_val_843 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_843 ~= nil) then -- ran, if id: 4512
                Array_420 = {};
                table.insert(Array_420, for_val_843);
            end;
        end;
        for for_key_844, for_val_844 in ipairs(Array_420) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1354, b_1354, c_1354, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4513

        end;
        for for_key_845, for_val_845 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_845 ~= nil) then -- ran, if id: 4514
                Array_421 = {};
                table.insert(Array_421, for_val_845);
            end;
        end;
        for for_key_846, for_val_846 in ipairs(Array_421) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1355, b_1355, c_1355, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4515

        end;
        for for_key_847, for_val_847 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_847 ~= nil) then -- ran, if id: 4516
                Array_422 = {};
                table.insert(Array_422, for_val_847);
            end;
        end;
        for for_key_848, for_val_848 in ipairs(Array_422) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1356, b_1356, c_1356, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4517

        end;
        for for_key_849, for_val_849 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_849 ~= nil) then -- ran, if id: 4518
                Array_423 = {};
                table.insert(Array_423, for_val_849);
            end;
        end;
        for for_key_850, for_val_850 in ipairs(Array_423) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1357, b_1357, c_1357, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4519

        end;
        for for_key_851, for_val_851 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_851 ~= nil) then -- ran, if id: 4520
                Array_424 = {};
                table.insert(Array_424, for_val_851);
            end;
        end;
        for for_key_852, for_val_852 in ipairs(Array_424) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1358, b_1358, c_1358, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4521

        end;
        for for_key_853, for_val_853 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_853 ~= nil) then -- ran, if id: 4522
                Array_425 = {};
                table.insert(Array_425, for_val_853);
            end;
        end;
        for for_key_854, for_val_854 in ipairs(Array_425) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1359, b_1359, c_1359, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4523

        end;
        for for_key_855, for_val_855 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_855 ~= nil) then -- ran, if id: 4524
                Array_426 = {};
                table.insert(Array_426, for_val_855);
            end;
        end;
        for for_key_856, for_val_856 in ipairs(Array_426) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1360, b_1360, c_1360, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4525

        end;
        for for_key_857, for_val_857 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_857 ~= nil) then -- ran, if id: 4526
                Array_427 = {};
                table.insert(Array_427, for_val_857);
            end;
        end;
        for for_key_858, for_val_858 in ipairs(Array_427) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1361, b_1361, c_1361, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4527

        end;
        for for_key_859, for_val_859 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_859 ~= nil) then -- ran, if id: 4528
                Array_428 = {};
                table.insert(Array_428, for_val_859);
            end;
        end;
        for for_key_860, for_val_860 in ipairs(Array_428) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1362, b_1362, c_1362, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4529

        end;
        for for_key_861, for_val_861 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_861 ~= nil) then -- ran, if id: 4530
                Array_429 = {};
                table.insert(Array_429, for_val_861);
            end;
        end;
        for for_key_862, for_val_862 in ipairs(Array_429) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1363, b_1363, c_1363, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4531

        end;
        for for_key_863, for_val_863 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_863 ~= nil) then -- ran, if id: 4532
                Array_430 = {};
                table.insert(Array_430, for_val_863);
            end;
        end;
        for for_key_864, for_val_864 in ipairs(Array_430) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1364, b_1364, c_1364, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4533

        end;
        for for_key_865, for_val_865 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_865 ~= nil) then -- ran, if id: 4534
                Array_431 = {};
                table.insert(Array_431, for_val_865);
            end;
        end;
        for for_key_866, for_val_866 in ipairs(Array_431) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1365, b_1365, c_1365, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4535

        end;
        for for_key_867, for_val_867 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_867 ~= nil) then -- ran, if id: 4536
                Array_432 = {};
                table.insert(Array_432, for_val_867);
            end;
        end;
        for for_key_868, for_val_868 in ipairs(Array_432) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1366, b_1366, c_1366, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4537

        end;
        for for_key_869, for_val_869 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_869 ~= nil) then -- ran, if id: 4538
                Array_433 = {};
                table.insert(Array_433, for_val_869);
            end;
        end;
        for for_key_870, for_val_870 in ipairs(Array_433) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1367, b_1367, c_1367, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4539

        end;
        for for_key_871, for_val_871 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_871 ~= nil) then -- ran, if id: 4540
                Array_434 = {};
                table.insert(Array_434, for_val_871);
            end;
        end;
        for for_key_872, for_val_872 in ipairs(Array_434) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1368, b_1368, c_1368, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4541

        end;
        for for_key_873, for_val_873 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_873 ~= nil) then -- ran, if id: 4542
                Array_435 = {};
                table.insert(Array_435, for_val_873);
            end;
        end;
        for for_key_874, for_val_874 in ipairs(Array_435) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1369, b_1369, c_1369, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4543

        end;
        for for_key_875, for_val_875 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_875 ~= nil) then -- ran, if id: 4544
                Array_436 = {};
                table.insert(Array_436, for_val_875);
            end;
        end;
        for for_key_876, for_val_876 in ipairs(Array_436) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1370, b_1370, c_1370, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4545

        end;
        for for_key_877, for_val_877 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_877 ~= nil) then -- ran, if id: 4546
                Array_437 = {};
                table.insert(Array_437, for_val_877);
            end;
        end;
        for for_key_878, for_val_878 in ipairs(Array_437) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1371, b_1371, c_1371, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4547

        end;
        for for_key_879, for_val_879 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_879 ~= nil) then -- ran, if id: 4548
                Array_438 = {};
                table.insert(Array_438, for_val_879);
            end;
        end;
        for for_key_880, for_val_880 in ipairs(Array_438) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1372, b_1372, c_1372, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4549

        end;
        for for_key_881, for_val_881 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_881 ~= nil) then -- ran, if id: 4550
                Array_439 = {};
                table.insert(Array_439, for_val_881);
            end;
        end;
        for for_key_882, for_val_882 in ipairs(Array_439) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1373, b_1373, c_1373, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4551

        end;
        for for_key_883, for_val_883 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_883 ~= nil) then -- ran, if id: 4552
                Array_440 = {};
                table.insert(Array_440, for_val_883);
            end;
        end;
        for for_key_884, for_val_884 in ipairs(Array_440) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1374, b_1374, c_1374, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4553

        end;
        for for_key_885, for_val_885 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_885 ~= nil) then -- ran, if id: 4554
                Array_441 = {};
                table.insert(Array_441, for_val_885);
            end;
        end;
        for for_key_886, for_val_886 in ipairs(Array_441) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1375, b_1375, c_1375, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4555

        end;
        for for_key_887, for_val_887 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_887 ~= nil) then -- ran, if id: 4556
                Array_442 = {};
                table.insert(Array_442, for_val_887);
            end;
        end;
        for for_key_888, for_val_888 in ipairs(Array_442) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1376, b_1376, c_1376, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4557

        end;
        for for_key_889, for_val_889 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_889 ~= nil) then -- ran, if id: 4558
                Array_443 = {};
                table.insert(Array_443, for_val_889);
            end;
        end;
        for for_key_890, for_val_890 in ipairs(Array_443) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1377, b_1377, c_1377, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4559

        end;
        for for_key_891, for_val_891 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_891 ~= nil) then -- ran, if id: 4560
                Array_444 = {};
                table.insert(Array_444, for_val_891);
            end;
        end;
        for for_key_892, for_val_892 in ipairs(Array_444) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1378, b_1378, c_1378, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4561

        end;
        for for_key_893, for_val_893 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_893 ~= nil) then -- ran, if id: 4562
                Array_445 = {};
                table.insert(Array_445, for_val_893);
            end;
        end;
        for for_key_894, for_val_894 in ipairs(Array_445) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1379, b_1379, c_1379, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4563

        end;
        for for_key_895, for_val_895 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_895 ~= nil) then -- ran, if id: 4564
                Array_446 = {};
                table.insert(Array_446, for_val_895);
            end;
        end;
        for for_key_896, for_val_896 in ipairs(Array_446) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1380, b_1380, c_1380, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4565

        end;
        for for_key_897, for_val_897 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_897 ~= nil) then -- ran, if id: 4566
                Array_447 = {};
                table.insert(Array_447, for_val_897);
            end;
        end;
        for for_key_898, for_val_898 in ipairs(Array_447) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1381, b_1381, c_1381, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4567

        end;
        for for_key_899, for_val_899 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_899 ~= nil) then -- ran, if id: 4568
                Array_448 = {};
                table.insert(Array_448, for_val_899);
            end;
        end;
        for for_key_900, for_val_900 in ipairs(Array_448) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1382, b_1382, c_1382, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4569

        end;
        for for_key_901, for_val_901 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_901 ~= nil) then -- ran, if id: 4570
                Array_449 = {};
                table.insert(Array_449, for_val_901);
            end;
        end;
        for for_key_902, for_val_902 in ipairs(Array_449) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1383, b_1383, c_1383, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4571

        end;
        for for_key_903, for_val_903 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_903 ~= nil) then -- ran, if id: 4572
                Array_450 = {};
                table.insert(Array_450, for_val_903);
            end;
        end;
        for for_key_904, for_val_904 in ipairs(Array_450) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1384, b_1384, c_1384, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4573

        end;
        for for_key_905, for_val_905 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_905 ~= nil) then -- ran, if id: 4574
                Array_451 = {};
                table.insert(Array_451, for_val_905);
            end;
        end;
        for for_key_906, for_val_906 in ipairs(Array_451) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1385, b_1385, c_1385, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4575

        end;
        for for_key_907, for_val_907 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_907 ~= nil) then -- ran, if id: 4576
                Array_452 = {};
                table.insert(Array_452, for_val_907);
            end;
        end;
        for for_key_908, for_val_908 in ipairs(Array_452) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1386, b_1386, c_1386, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4577

        end;
        for for_key_909, for_val_909 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_909 ~= nil) then -- ran, if id: 4578
                Array_453 = {};
                table.insert(Array_453, for_val_909);
            end;
        end;
        for for_key_910, for_val_910 in ipairs(Array_453) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1387, b_1387, c_1387, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4579

        end;
        for for_key_911, for_val_911 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_911 ~= nil) then -- ran, if id: 4580
                Array_454 = {};
                table.insert(Array_454, for_val_911);
            end;
        end;
        for for_key_912, for_val_912 in ipairs(Array_454) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1388, b_1388, c_1388, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4581

        end;
        for for_key_913, for_val_913 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_913 ~= nil) then -- ran, if id: 4582
                Array_455 = {};
                table.insert(Array_455, for_val_913);
            end;
        end;
        for for_key_914, for_val_914 in ipairs(Array_455) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1389, b_1389, c_1389, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4583

        end;
        for for_key_915, for_val_915 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_915 ~= nil) then -- ran, if id: 4584
                Array_456 = {};
                table.insert(Array_456, for_val_915);
            end;
        end;
        for for_key_916, for_val_916 in ipairs(Array_456) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1390, b_1390, c_1390, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4585

        end;
        for for_key_917, for_val_917 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_917 ~= nil) then -- ran, if id: 4586
                Array_457 = {};
                table.insert(Array_457, for_val_917);
            end;
        end;
        for for_key_918, for_val_918 in ipairs(Array_457) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1391, b_1391, c_1391, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4587

        end;
        for for_key_919, for_val_919 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_919 ~= nil) then -- ran, if id: 4588
                Array_458 = {};
                table.insert(Array_458, for_val_919);
            end;
        end;
        for for_key_920, for_val_920 in ipairs(Array_458) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1392, b_1392, c_1392, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4589

        end;
        for for_key_921, for_val_921 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_921 ~= nil) then -- ran, if id: 4590
                Array_459 = {};
                table.insert(Array_459, for_val_921);
            end;
        end;
        for for_key_922, for_val_922 in ipairs(Array_459) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1393, b_1393, c_1393, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4591

        end;
        for for_key_923, for_val_923 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_923 ~= nil) then -- ran, if id: 4592
                Array_460 = {};
                table.insert(Array_460, for_val_923);
            end;
        end;
        for for_key_924, for_val_924 in ipairs(Array_460) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1394, b_1394, c_1394, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4593

        end;
        for for_key_925, for_val_925 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_925 ~= nil) then -- ran, if id: 4594
                Array_461 = {};
                table.insert(Array_461, for_val_925);
            end;
        end;
        for for_key_926, for_val_926 in ipairs(Array_461) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1395, b_1395, c_1395, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4595

        end;
        for for_key_927, for_val_927 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_927 ~= nil) then -- ran, if id: 4596
                Array_462 = {};
                table.insert(Array_462, for_val_927);
            end;
        end;
        for for_key_928, for_val_928 in ipairs(Array_462) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1396, b_1396, c_1396, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4597

        end;
        for for_key_929, for_val_929 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_929 ~= nil) then -- ran, if id: 4598
                Array_463 = {};
                table.insert(Array_463, for_val_929);
            end;
        end;
        for for_key_930, for_val_930 in ipairs(Array_463) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1397, b_1397, c_1397, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4599

        end;
        for for_key_931, for_val_931 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_931 ~= nil) then -- ran, if id: 4600
                Array_464 = {};
                table.insert(Array_464, for_val_931);
            end;
        end;
        for for_key_932, for_val_932 in ipairs(Array_464) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1398, b_1398, c_1398, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4601

        end;
        for for_key_933, for_val_933 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_933 ~= nil) then -- ran, if id: 4602
                Array_465 = {};
                table.insert(Array_465, for_val_933);
            end;
        end;
        for for_key_934, for_val_934 in ipairs(Array_465) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1399, b_1399, c_1399, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4603

        end;
        for for_key_935, for_val_935 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_935 ~= nil) then -- ran, if id: 4604
                Array_466 = {};
                table.insert(Array_466, for_val_935);
            end;
        end;
        for for_key_936, for_val_936 in ipairs(Array_466) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1400, b_1400, c_1400, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4605

        end;
        for for_key_937, for_val_937 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_937 ~= nil) then -- ran, if id: 4606
                Array_467 = {};
                table.insert(Array_467, for_val_937);
            end;
        end;
        for for_key_938, for_val_938 in ipairs(Array_467) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1401, b_1401, c_1401, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4607

        end;
        for for_key_939, for_val_939 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_939 ~= nil) then -- ran, if id: 4608
                Array_468 = {};
                table.insert(Array_468, for_val_939);
            end;
        end;
        for for_key_940, for_val_940 in ipairs(Array_468) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1402, b_1402, c_1402, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4609

        end;
        for for_key_941, for_val_941 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_941 ~= nil) then -- ran, if id: 4610
                Array_469 = {};
                table.insert(Array_469, for_val_941);
            end;
        end;
        for for_key_942, for_val_942 in ipairs(Array_469) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1403, b_1403, c_1403, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4611

        end;
        for for_key_943, for_val_943 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_943 ~= nil) then -- ran, if id: 4612
                Array_470 = {};
                table.insert(Array_470, for_val_943);
            end;
        end;
        for for_key_944, for_val_944 in ipairs(Array_470) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1404, b_1404, c_1404, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4613

        end;
        for for_key_945, for_val_945 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_945 ~= nil) then -- ran, if id: 4614
                Array_471 = {};
                table.insert(Array_471, for_val_945);
            end;
        end;
        for for_key_946, for_val_946 in ipairs(Array_471) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1405, b_1405, c_1405, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4615

        end;
        for for_key_947, for_val_947 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_947 ~= nil) then -- ran, if id: 4616
                Array_472 = {};
                table.insert(Array_472, for_val_947);
            end;
        end;
        for for_key_948, for_val_948 in ipairs(Array_472) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1406, b_1406, c_1406, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4617

        end;
        for for_key_949, for_val_949 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_949 ~= nil) then -- ran, if id: 4618
                Array_473 = {};
                table.insert(Array_473, for_val_949);
            end;
        end;
        for for_key_950, for_val_950 in ipairs(Array_473) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1407, b_1407, c_1407, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4619

        end;
        for for_key_951, for_val_951 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_951 ~= nil) then -- ran, if id: 4620
                Array_474 = {};
                table.insert(Array_474, for_val_951);
            end;
        end;
        for for_key_952, for_val_952 in ipairs(Array_474) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1408, b_1408, c_1408, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4621

        end;
        for for_key_953, for_val_953 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_953 ~= nil) then -- ran, if id: 4622
                Array_475 = {};
                table.insert(Array_475, for_val_953);
            end;
        end;
        for for_key_954, for_val_954 in ipairs(Array_475) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1409, b_1409, c_1409, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4623

        end;
        for for_key_955, for_val_955 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_955 ~= nil) then -- ran, if id: 4624
                Array_476 = {};
                table.insert(Array_476, for_val_955);
            end;
        end;
        for for_key_956, for_val_956 in ipairs(Array_476) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1410, b_1410, c_1410, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4625

        end;
        for for_key_957, for_val_957 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_957 ~= nil) then -- ran, if id: 4626
                Array_477 = {};
                table.insert(Array_477, for_val_957);
            end;
        end;
        for for_key_958, for_val_958 in ipairs(Array_477) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1411, b_1411, c_1411, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4627

        end;
        for for_key_959, for_val_959 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_959 ~= nil) then -- ran, if id: 4628
                Array_478 = {};
                table.insert(Array_478, for_val_959);
            end;
        end;
        for for_key_960, for_val_960 in ipairs(Array_478) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1412, b_1412, c_1412, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4629

        end;
        for for_key_961, for_val_961 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_961 ~= nil) then -- ran, if id: 4630
                Array_479 = {};
                table.insert(Array_479, for_val_961);
            end;
        end;
        for for_key_962, for_val_962 in ipairs(Array_479) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1413, b_1413, c_1413, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4631

        end;
        for for_key_963, for_val_963 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_963 ~= nil) then -- ran, if id: 4632
                Array_480 = {};
                table.insert(Array_480, for_val_963);
            end;
        end;
        for for_key_964, for_val_964 in ipairs(Array_480) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1414, b_1414, c_1414, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4633

        end;
        for for_key_965, for_val_965 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_965 ~= nil) then -- ran, if id: 4634
                Array_481 = {};
                table.insert(Array_481, for_val_965);
            end;
        end;
        for for_key_966, for_val_966 in ipairs(Array_481) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1415, b_1415, c_1415, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4635

        end;
        for for_key_967, for_val_967 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_967 ~= nil) then -- ran, if id: 4636
                Array_482 = {};
                table.insert(Array_482, for_val_967);
            end;
        end;
        for for_key_968, for_val_968 in ipairs(Array_482) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1416, b_1416, c_1416, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4637

        end;
        for for_key_969, for_val_969 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_969 ~= nil) then -- ran, if id: 4638
                Array_483 = {};
                table.insert(Array_483, for_val_969);
            end;
        end;
        for for_key_970, for_val_970 in ipairs(Array_483) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1417, b_1417, c_1417, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4639

        end;
        for for_key_971, for_val_971 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_971 ~= nil) then -- ran, if id: 4640
                Array_484 = {};
                table.insert(Array_484, for_val_971);
            end;
        end;
        for for_key_972, for_val_972 in ipairs(Array_484) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1418, b_1418, c_1418, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4641

        end;
        for for_key_973, for_val_973 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_973 ~= nil) then -- ran, if id: 4642
                Array_485 = {};
                table.insert(Array_485, for_val_973);
            end;
        end;
        for for_key_974, for_val_974 in ipairs(Array_485) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1419, b_1419, c_1419, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4643

        end;
        for for_key_975, for_val_975 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_975 ~= nil) then -- ran, if id: 4644
                Array_486 = {};
                table.insert(Array_486, for_val_975);
            end;
        end;
        for for_key_976, for_val_976 in ipairs(Array_486) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1420, b_1420, c_1420, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4645

        end;
        for for_key_977, for_val_977 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_977 ~= nil) then -- ran, if id: 4646
                Array_487 = {};
                table.insert(Array_487, for_val_977);
            end;
        end;
        for for_key_978, for_val_978 in ipairs(Array_487) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1421, b_1421, c_1421, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4647

        end;
        for for_key_979, for_val_979 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_979 ~= nil) then -- ran, if id: 4648
                Array_488 = {};
                table.insert(Array_488, for_val_979);
            end;
        end;
        for for_key_980, for_val_980 in ipairs(Array_488) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1422, b_1422, c_1422, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4649

        end;
        for for_key_981, for_val_981 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_981 ~= nil) then -- ran, if id: 4650
                Array_489 = {};
                table.insert(Array_489, for_val_981);
            end;
        end;
        for for_key_982, for_val_982 in ipairs(Array_489) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1423, b_1423, c_1423, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4651

        end;
        for for_key_983, for_val_983 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_983 ~= nil) then -- ran, if id: 4652
                Array_490 = {};
                table.insert(Array_490, for_val_983);
            end;
        end;
        for for_key_984, for_val_984 in ipairs(Array_490) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1424, b_1424, c_1424, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4653

        end;
        for for_key_985, for_val_985 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_985 ~= nil) then -- ran, if id: 4654
                Array_491 = {};
                table.insert(Array_491, for_val_985);
            end;
        end;
        for for_key_986, for_val_986 in ipairs(Array_491) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1425, b_1425, c_1425, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4655

        end;
        for for_key_987, for_val_987 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_987 ~= nil) then -- ran, if id: 4656
                Array_492 = {};
                table.insert(Array_492, for_val_987);
            end;
        end;
        for for_key_988, for_val_988 in ipairs(Array_492) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1426, b_1426, c_1426, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4657

        end;
        for for_key_989, for_val_989 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_989 ~= nil) then -- ran, if id: 4658
                Array_493 = {};
                table.insert(Array_493, for_val_989);
            end;
        end;
        for for_key_990, for_val_990 in ipairs(Array_493) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1427, b_1427, c_1427, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4659

        end;
        for for_key_991, for_val_991 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_991 ~= nil) then -- ran, if id: 4660
                Array_494 = {};
                table.insert(Array_494, for_val_991);
            end;
        end;
        for for_key_992, for_val_992 in ipairs(Array_494) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1428, b_1428, c_1428, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4661

        end;
        for for_key_993, for_val_993 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_993 ~= nil) then -- ran, if id: 4662
                Array_495 = {};
                table.insert(Array_495, for_val_993);
            end;
        end;
        for for_key_994, for_val_994 in ipairs(Array_495) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1429, b_1429, c_1429, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4663

        end;
        for for_key_995, for_val_995 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_995 ~= nil) then -- ran, if id: 4664
                Array_496 = {};
                table.insert(Array_496, for_val_995);
            end;
        end;
        for for_key_996, for_val_996 in ipairs(Array_496) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1430, b_1430, c_1430, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4665

        end;
        for for_key_997, for_val_997 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_997 ~= nil) then -- ran, if id: 4666
                Array_497 = {};
                table.insert(Array_497, for_val_997);
            end;
        end;
        for for_key_998, for_val_998 in ipairs(Array_497) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1431, b_1431, c_1431, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4667

        end;
        for for_key_999, for_val_999 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_999 ~= nil) then -- ran, if id: 4668
                Array_498 = {};
                table.insert(Array_498, for_val_999);
            end;
        end;
        for for_key_1000, for_val_1000 in ipairs(Array_498) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1432, b_1432, c_1432, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4669

        end;
        for for_key_1001, for_val_1001 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1001 ~= nil) then -- ran, if id: 4670
                Array_499 = {};
                table.insert(Array_499, for_val_1001);
            end;
        end;
        for for_key_1002, for_val_1002 in ipairs(Array_499) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1433, b_1433, c_1433, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4671

        end;
        for for_key_1003, for_val_1003 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1003 ~= nil) then -- ran, if id: 4672
                Array_500 = {};
                table.insert(Array_500, for_val_1003);
            end;
        end;
        for for_key_1004, for_val_1004 in ipairs(Array_500) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1434, b_1434, c_1434, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4673

        end;
        for for_key_1005, for_val_1005 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1005 ~= nil) then -- ran, if id: 4674
                Array_501 = {};
                table.insert(Array_501, for_val_1005);
            end;
        end;
        for for_key_1006, for_val_1006 in ipairs(Array_501) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1435, b_1435, c_1435, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4675

        end;
        for for_key_1007, for_val_1007 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1007 ~= nil) then -- ran, if id: 4676
                Array_502 = {};
                table.insert(Array_502, for_val_1007);
            end;
        end;
        for for_key_1008, for_val_1008 in ipairs(Array_502) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1436, b_1436, c_1436, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4677

        end;
        for for_key_1009, for_val_1009 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1009 ~= nil) then -- ran, if id: 4678
                Array_503 = {};
                table.insert(Array_503, for_val_1009);
            end;
        end;
        for for_key_1010, for_val_1010 in ipairs(Array_503) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1437, b_1437, c_1437, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4679

        end;
        for for_key_1011, for_val_1011 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1011 ~= nil) then -- ran, if id: 4680
                Array_504 = {};
                table.insert(Array_504, for_val_1011);
            end;
        end;
        for for_key_1012, for_val_1012 in ipairs(Array_504) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1438, b_1438, c_1438, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4681

        end;
        for for_key_1013, for_val_1013 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1013 ~= nil) then -- ran, if id: 4682
                Array_505 = {};
                table.insert(Array_505, for_val_1013);
            end;
        end;
        for for_key_1014, for_val_1014 in ipairs(Array_505) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1439, b_1439, c_1439, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4683

        end;
        for for_key_1015, for_val_1015 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1015 ~= nil) then -- ran, if id: 4684
                Array_506 = {};
                table.insert(Array_506, for_val_1015);
            end;
        end;
        for for_key_1016, for_val_1016 in ipairs(Array_506) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1440, b_1440, c_1440, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4685

        end;
        for for_key_1017, for_val_1017 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1017 ~= nil) then -- ran, if id: 4686
                Array_507 = {};
                table.insert(Array_507, for_val_1017);
            end;
        end;
        for for_key_1018, for_val_1018 in ipairs(Array_507) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1441, b_1441, c_1441, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4687

        end;
        for for_key_1019, for_val_1019 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1019 ~= nil) then -- ran, if id: 4688
                Array_508 = {};
                table.insert(Array_508, for_val_1019);
            end;
        end;
        for for_key_1020, for_val_1020 in ipairs(Array_508) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1442, b_1442, c_1442, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4689

        end;
        for for_key_1021, for_val_1021 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1021 ~= nil) then -- ran, if id: 4690
                Array_509 = {};
                table.insert(Array_509, for_val_1021);
            end;
        end;
        for for_key_1022, for_val_1022 in ipairs(Array_509) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1443, b_1443, c_1443, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4691

        end;
        for for_key_1023, for_val_1023 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1023 ~= nil) then -- ran, if id: 4692
                Array_510 = {};
                table.insert(Array_510, for_val_1023);
            end;
        end;
        for for_key_1024, for_val_1024 in ipairs(Array_510) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1444, b_1444, c_1444, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4693

        end;
        for for_key_1025, for_val_1025 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1025 ~= nil) then -- ran, if id: 4694
                Array_511 = {};
                table.insert(Array_511, for_val_1025);
            end;
        end;
        for for_key_1026, for_val_1026 in ipairs(Array_511) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1445, b_1445, c_1445, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4695

        end;
        for for_key_1027, for_val_1027 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1027 ~= nil) then -- ran, if id: 4696
                Array_512 = {};
                table.insert(Array_512, for_val_1027);
            end;
        end;
        for for_key_1028, for_val_1028 in ipairs(Array_512) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1446, b_1446, c_1446, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4697

        end;
        for for_key_1029, for_val_1029 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1029 ~= nil) then -- ran, if id: 4698
                Array_513 = {};
                table.insert(Array_513, for_val_1029);
            end;
        end;
        for for_key_1030, for_val_1030 in ipairs(Array_513) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1447, b_1447, c_1447, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4699

        end;
        for for_key_1031, for_val_1031 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1031 ~= nil) then -- ran, if id: 4700
                Array_514 = {};
                table.insert(Array_514, for_val_1031);
            end;
        end;
        for for_key_1032, for_val_1032 in ipairs(Array_514) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1448, b_1448, c_1448, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4701

        end;
        for for_key_1033, for_val_1033 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1033 ~= nil) then -- ran, if id: 4702
                Array_515 = {};
                table.insert(Array_515, for_val_1033);
            end;
        end;
        for for_key_1034, for_val_1034 in ipairs(Array_515) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1449, b_1449, c_1449, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4703

        end;
        for for_key_1035, for_val_1035 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1035 ~= nil) then -- ran, if id: 4704
                Array_516 = {};
                table.insert(Array_516, for_val_1035);
            end;
        end;
        for for_key_1036, for_val_1036 in ipairs(Array_516) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1450, b_1450, c_1450, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4705

        end;
        for for_key_1037, for_val_1037 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1037 ~= nil) then -- ran, if id: 4706
                Array_517 = {};
                table.insert(Array_517, for_val_1037);
            end;
        end;
        for for_key_1038, for_val_1038 in ipairs(Array_517) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1451, b_1451, c_1451, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4707

        end;
        for for_key_1039, for_val_1039 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1039 ~= nil) then -- ran, if id: 4708
                Array_518 = {};
                table.insert(Array_518, for_val_1039);
            end;
        end;
        for for_key_1040, for_val_1040 in ipairs(Array_518) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1452, b_1452, c_1452, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4709

        end;
        for for_key_1041, for_val_1041 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1041 ~= nil) then -- ran, if id: 4710
                Array_519 = {};
                table.insert(Array_519, for_val_1041);
            end;
        end;
        for for_key_1042, for_val_1042 in ipairs(Array_519) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1453, b_1453, c_1453, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4711

        end;
        for for_key_1043, for_val_1043 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1043 ~= nil) then -- ran, if id: 4712
                Array_520 = {};
                table.insert(Array_520, for_val_1043);
            end;
        end;
        for for_key_1044, for_val_1044 in ipairs(Array_520) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1454, b_1454, c_1454, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4713

        end;
        for for_key_1045, for_val_1045 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1045 ~= nil) then -- ran, if id: 4714
                Array_521 = {};
                table.insert(Array_521, for_val_1045);
            end;
        end;
        for for_key_1046, for_val_1046 in ipairs(Array_521) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1455, b_1455, c_1455, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4715

        end;
        for for_key_1047, for_val_1047 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1047 ~= nil) then -- ran, if id: 4716
                Array_522 = {};
                table.insert(Array_522, for_val_1047);
            end;
        end;
        for for_key_1048, for_val_1048 in ipairs(Array_522) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1456, b_1456, c_1456, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4717

        end;
        for for_key_1049, for_val_1049 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1049 ~= nil) then -- ran, if id: 4718
                Array_523 = {};
                table.insert(Array_523, for_val_1049);
            end;
        end;
        for for_key_1050, for_val_1050 in ipairs(Array_523) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1457, b_1457, c_1457, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4719

        end;
        for for_key_1051, for_val_1051 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1051 ~= nil) then -- ran, if id: 4720
                Array_524 = {};
                table.insert(Array_524, for_val_1051);
            end;
        end;
        for for_key_1052, for_val_1052 in ipairs(Array_524) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1458, b_1458, c_1458, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4721

        end;
        for for_key_1053, for_val_1053 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1053 ~= nil) then -- ran, if id: 4722
                Array_525 = {};
                table.insert(Array_525, for_val_1053);
            end;
        end;
        for for_key_1054, for_val_1054 in ipairs(Array_525) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1459, b_1459, c_1459, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4723

        end;
        for for_key_1055, for_val_1055 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1055 ~= nil) then -- ran, if id: 4724
                Array_526 = {};
                table.insert(Array_526, for_val_1055);
            end;
        end;
        for for_key_1056, for_val_1056 in ipairs(Array_526) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1460, b_1460, c_1460, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4725

        end;
        for for_key_1057, for_val_1057 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1057 ~= nil) then -- ran, if id: 4726
                Array_527 = {};
                table.insert(Array_527, for_val_1057);
            end;
        end;
        for for_key_1058, for_val_1058 in ipairs(Array_527) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1461, b_1461, c_1461, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4727

        end;
        for for_key_1059, for_val_1059 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1059 ~= nil) then -- ran, if id: 4728
                Array_528 = {};
                table.insert(Array_528, for_val_1059);
            end;
        end;
        for for_key_1060, for_val_1060 in ipairs(Array_528) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1462, b_1462, c_1462, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4729

        end;
        for for_key_1061, for_val_1061 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1061 ~= nil) then -- ran, if id: 4730
                Array_529 = {};
                table.insert(Array_529, for_val_1061);
            end;
        end;
        for for_key_1062, for_val_1062 in ipairs(Array_529) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1463, b_1463, c_1463, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4731

        end;
        for for_key_1063, for_val_1063 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1063 ~= nil) then -- ran, if id: 4732
                Array_530 = {};
                table.insert(Array_530, for_val_1063);
            end;
        end;
        for for_key_1064, for_val_1064 in ipairs(Array_530) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1464, b_1464, c_1464, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4733

        end;
        for for_key_1065, for_val_1065 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1065 ~= nil) then -- ran, if id: 4734
                Array_531 = {};
                table.insert(Array_531, for_val_1065);
            end;
        end;
        for for_key_1066, for_val_1066 in ipairs(Array_531) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1465, b_1465, c_1465, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4735

        end;
        for for_key_1067, for_val_1067 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1067 ~= nil) then -- ran, if id: 4736
                Array_532 = {};
                table.insert(Array_532, for_val_1067);
            end;
        end;
        for for_key_1068, for_val_1068 in ipairs(Array_532) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1466, b_1466, c_1466, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4737

        end;
        for for_key_1069, for_val_1069 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1069 ~= nil) then -- ran, if id: 4738
                Array_533 = {};
                table.insert(Array_533, for_val_1069);
            end;
        end;
        for for_key_1070, for_val_1070 in ipairs(Array_533) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1467, b_1467, c_1467, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4739

        end;
        for for_key_1071, for_val_1071 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1071 ~= nil) then -- ran, if id: 4740
                Array_534 = {};
                table.insert(Array_534, for_val_1071);
            end;
        end;
        for for_key_1072, for_val_1072 in ipairs(Array_534) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1468, b_1468, c_1468, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4741

        end;
        for for_key_1073, for_val_1073 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1073 ~= nil) then -- ran, if id: 4742
                Array_535 = {};
                table.insert(Array_535, for_val_1073);
            end;
        end;
        for for_key_1074, for_val_1074 in ipairs(Array_535) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1469, b_1469, c_1469, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4743

        end;
        for for_key_1075, for_val_1075 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1075 ~= nil) then -- ran, if id: 4744
                Array_536 = {};
                table.insert(Array_536, for_val_1075);
            end;
        end;
        for for_key_1076, for_val_1076 in ipairs(Array_536) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1470, b_1470, c_1470, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4745

        end;
        for for_key_1077, for_val_1077 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1077 ~= nil) then -- ran, if id: 4746
                Array_537 = {};
                table.insert(Array_537, for_val_1077);
            end;
        end;
        for for_key_1078, for_val_1078 in ipairs(Array_537) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1471, b_1471, c_1471, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4747

        end;
        for for_key_1079, for_val_1079 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1079 ~= nil) then -- ran, if id: 4748
                Array_538 = {};
                table.insert(Array_538, for_val_1079);
            end;
        end;
        for for_key_1080, for_val_1080 in ipairs(Array_538) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1472, b_1472, c_1472, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4749

        end;
        for for_key_1081, for_val_1081 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1081 ~= nil) then -- ran, if id: 4750
                Array_539 = {};
                table.insert(Array_539, for_val_1081);
            end;
        end;
        for for_key_1082, for_val_1082 in ipairs(Array_539) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1473, b_1473, c_1473, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4751

        end;
        for for_key_1083, for_val_1083 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1083 ~= nil) then -- ran, if id: 4752
                Array_540 = {};
                table.insert(Array_540, for_val_1083);
            end;
        end;
        for for_key_1084, for_val_1084 in ipairs(Array_540) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1474, b_1474, c_1474, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4753

        end;
        for for_key_1085, for_val_1085 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1085 ~= nil) then -- ran, if id: 4754
                Array_541 = {};
                table.insert(Array_541, for_val_1085);
            end;
        end;
        for for_key_1086, for_val_1086 in ipairs(Array_541) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1475, b_1475, c_1475, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4755

        end;
        for for_key_1087, for_val_1087 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1087 ~= nil) then -- ran, if id: 4756
                Array_542 = {};
                table.insert(Array_542, for_val_1087);
            end;
        end;
        for for_key_1088, for_val_1088 in ipairs(Array_542) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1476, b_1476, c_1476, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4757

        end;
        for for_key_1089, for_val_1089 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1089 ~= nil) then -- ran, if id: 4758
                Array_543 = {};
                table.insert(Array_543, for_val_1089);
            end;
        end;
        for for_key_1090, for_val_1090 in ipairs(Array_543) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1477, b_1477, c_1477, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4759

        end;
        for for_key_1091, for_val_1091 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1091 ~= nil) then -- ran, if id: 4760
                Array_544 = {};
                table.insert(Array_544, for_val_1091);
            end;
        end;
        for for_key_1092, for_val_1092 in ipairs(Array_544) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1478, b_1478, c_1478, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4761

        end;
        for for_key_1093, for_val_1093 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1093 ~= nil) then -- ran, if id: 4762
                Array_545 = {};
                table.insert(Array_545, for_val_1093);
            end;
        end;
        for for_key_1094, for_val_1094 in ipairs(Array_545) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1479, b_1479, c_1479, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4763

        end;
        for for_key_1095, for_val_1095 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1095 ~= nil) then -- ran, if id: 4764
                Array_546 = {};
                table.insert(Array_546, for_val_1095);
            end;
        end;
        for for_key_1096, for_val_1096 in ipairs(Array_546) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1480, b_1480, c_1480, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4765

        end;
        for for_key_1097, for_val_1097 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1097 ~= nil) then -- ran, if id: 4766
                Array_547 = {};
                table.insert(Array_547, for_val_1097);
            end;
        end;
        for for_key_1098, for_val_1098 in ipairs(Array_547) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1481, b_1481, c_1481, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4767

        end;
        for for_key_1099, for_val_1099 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1099 ~= nil) then -- ran, if id: 4768
                Array_548 = {};
                table.insert(Array_548, for_val_1099);
            end;
        end;
        for for_key_1100, for_val_1100 in ipairs(Array_548) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1482, b_1482, c_1482, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4769

        end;
        for for_key_1101, for_val_1101 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1101 ~= nil) then -- ran, if id: 4770
                Array_549 = {};
                table.insert(Array_549, for_val_1101);
            end;
        end;
        for for_key_1102, for_val_1102 in ipairs(Array_549) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1483, b_1483, c_1483, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4771

        end;
        for for_key_1103, for_val_1103 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1103 ~= nil) then -- ran, if id: 4772
                Array_550 = {};
                table.insert(Array_550, for_val_1103);
            end;
        end;
        for for_key_1104, for_val_1104 in ipairs(Array_550) do
        end;
    end);
    task.wait(0.5);
    pcall(function(a_1484, b_1484, c_1484, ...)
        if workspace:FindFirstChild("ActiveLuckyBlocks") then -- ran, if id: 4773

        end;
        for for_key_1105, for_val_1105 in pairs(ActiveLuckyBlocks:GetChildren()) do
            if (for_key_1105 ~= nil) then -- ran, if id: 4774
                Array_551 = {};
                table.insert(Array_551, for_val_1105);
            end;
        end;
        for for_key_1106, for_val_1106 in ipairs(Array_551) do
        end;
    end);
    task.wait(0.5);
    while false do

    end;
end);
task.wait(0.8);
StarterGui = game.StarterGui;
StarterGui:SetCore("SendNotification", {
    ["Icon"] = "rbxassetid://6031097225",
    ["Text"] = "GUI loaded successfully! Enjoy your experience!",
    ["Title"] = "\226\156\168 Snapix Loaded",
    ["Duration"] = 5
});